# Supabase — module Veille uniquement (D51 : même instance que `annuaire-msp`, mêmes comptes MSP).
# Copier ce fichier en `.env.local` (non commité) et renseigner les MÊMES valeurs que
# `annuaire-msp/.env.local` (le projet Supabase est partagé, cf. D51 dans DECISIONS.md).
# Sur Vercel : définir ces variables dans Project Settings → Environment Variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# P12 · S4 — La branche AGP appariée : dire quoi baisser, pas la table   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 2 (parallèle : oui — avec S3)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-02 · Branche : —

> ## ⚠ Cette session exécute un brief déjà écrit
>
> **T-067 est intégralement cadrée dans [`plans/P8/S9.md`](../P8/S9.md)** — arbitrages B3a (carte
> nouvelle), B3b (chiffrée −2 U ou −10 %, provenance dite à l'écran) et B3c (les garde-fous restent)
> rendus par le référent le 2026-07-30. **Ce brief-ci ne le remplace pas : il le corrige et le
> relance.** Lis `plans/P8/S9.md` en entier, puis applique les quatre rectifications ci-dessous.

## Rectifications à appliquer à `plans/P8/S9.md`

1. **Les chemins ont changé.** P8/S9 écrit `content/noeuds/diabete-type-2/…` ; le chemin réel est
   **`content/decision/noeuds/diabete-type-2/…`**. Vaut pour `insuline.yaml` et `insuline.argumentaire.md`.
2. **La commande de typecheck de P8/S9 est fausse.** Elle dit `npx tsc --noEmit` : le `tsconfig.json`
   racine est en `files: []` + références de projet, cette commande compile **0 fichier** et rend un
   vert vide (constaté P11, 2026-08-02). Utiliser **`npm run typecheck`**.
3. **Les numéros de ligne sont périmés.** P8/S9 date du 2026-07-30 ; le nœud a reçu P10 (cadrage,
   molécules/doses), le correctif GAJ du 02/08 (v0.38) et, **juste avant cette session, S2 de ce plan
   a raccourci ses intitulés**. Repère les quatre zones **par recherche de leur intitulé**, jamais par
   le numéro de ligne. ⚠ Ces intitulés ont pu changer en S2 : lis le rapport de session de S2 avant de
   chercher.
4. **La prérequis « ne pas lancer avant S7 » est levée** : `profil_nocturne` existe depuis P8/S7,
   livrée. La dépendance réelle de cette session est **S2 de P12** (même fichier).

## Ce que la recette du 2026-08-02 ajoute au dossier

La vignette N11 (Mme Renard, 68 ans, insuline + capteur, hypoglycémies nocturnes) confirme le défaut
**et le précise d'une manière qui n'était pas connue le 30/07** :

> « L'outil sait que je suis dans la branche “baisse continue nocturne”, il possède la règle qui dit
> “réduire la basale”, et il m'affiche à la place un titre générique (« Optimiser la répartition »)
> en me laissant faire l'appariement moi-même, dans une table à cinq branches, derrière un chevron. »

Le « Proposé parce que » de la carte **énonce déjà la branche du patient** (« Profil glycémique
nocturne (lecture AGP) = Baisse continue de la glycémie nocturne »). Le moteur a donc l'information
à l'écran ; c'est la **conduite** correspondante qui n'y est pas. Cela ne change aucun arbitrage de
P8/S9 — cela confirme que la carte nouvelle est le bon remède, et donne le critère de fin :

> **Après cette session, la vignette N11 doit produire une carte dont le titre nomme le geste
> (réduire la basale) et dont la face visible porte un chiffre**, sans qu'il faille ouvrir le chevron.

Ajoute cette vignette au banc si elle n'y figure pas déjà sous cette forme (capteur + baisse continue
nocturne + basale seule ou basal-bolus).

## Rappel du point le plus facile à rater

P8/S9 le dit deux fois, la recette le confirme ailleurs, et c'est la seule vraie faute possible :
**les chiffres de la descente sont une symétrie de la montée, pas une donnée publiée.** Le rapport
OpenEvidence du 2026-08-02 (analysé au cadrage de P12) le confirme indépendamment : *aucun schéma de
décroissance validé par essai n'existe pour l'insuline hors contexte de relais thérapeutique.* La
carte doit écrire sa provenance ; les `references` ne citent que les sources de la montée, et
seulement pour dire de quoi la symétrie est tirée.

## Validation

Celle de `plans/P8/S9.md`, avec la commande de typecheck corrigée (rectification 2), plus :

- **N1 visuel auto** : reproduire la vignette N11 au navigateur in-app et vérifier que le geste et
  son chiffre sont **sur la face visible de la carte**. Si l'environnement n'a pas le navigateur,
  le noter et le reporter à la recette de fin de plan.
- **N2 humain** : les deux points de P8/S9 (la carte dit-elle assez pour agir ? la cohabitation avec
  « Corriger l'hypoglycémie » est-elle informative ou redondante ?) — à consigner dans `VALIDATION.md`.

## Message de commit (appliqué en fin de plan)

`feat(dt2): la baisse continue nocturne déclenche la réduction de la basale, chiffrée (T-067)`

---

## Fin de session

Dérouler `/fin-de-tache` (mode **solo**). Signale dans ton bilan que **T-067 sort de `plans/P8`** :
la consolidation devra retirer sa ligne de `TASKS.md` §P8 et clore P8.

> ⚠ **Correction appliquée à l'exécution** : la bannière de ce fichier dit « parallèle : oui — avec
> S3 » et la consigne d'orchestration reçue pour cette session est explicitement « mode vague
> parallèle » (S3 travaille en même temps sur `DecisionNodeScreen.tsx`/`.css`/`DECISIONS.md`). La
> ligne « mode solo » ci-dessus est un reliquat du gabarit `plans/P8/S9.md` (où cette session était
> seule sur `insuline.yaml`) — **non applicable ici**. Bilan et points N2 consignés ci-dessous, dans
> CE fichier, aucun fichier partagé touché, ni commit ni push (skill `fin-de-tache`, mode vague
> parallèle).

---

## Bilan de session — T-067 (2026-08-02)

### Fichiers modifiés

- `content/decision/noeuds/diabete-type-2/insuline.yaml` — nouvelle option « Réduire la basale »
  ajoutée (après « Titrer la basale (augmenter la dose) », avant la section « SITUATION 3 ») ;
  déclencheur de forme `mcg_disponible == true AND profil_nocturne == baisse_continue` retiré des
  `conditions` de « Corriger l'hypoglycémie ou la variabilité » (qui garde ses trois déclencheurs de
  seuil : `TBR > 4`, `CV_glycemique > 36`, `gaj_basse` sans capteur) ; son `effet_attendu` corrigé en
  conséquence (ne mentionne plus « un profil AGP d'hypo nocturne » comme déclencheur) ; cross-référence
  corrigée dans « Désintensifier / alléger le schéma » (`inconvenients` + `apercu`, qui pointaient tous
  deux vers « Corriger l'hypoglycémie… » pour le cas « profil nocturne en baisse » — scindée pour
  pointer vers la bonne carte) ; `meta.version` 0.39 → 0.40 → **0.41** (2ᵉ passe, correction de
  provenance, cf. « Correction post-revue » ci-dessous), `meta.changelog` (deux nouvelles entrées en
  tête).
- `content/decision/noeuds/diabete-type-2/insuline.argumentaire.md` — nouveau paragraphe dans « §4.
  Adaptation du basal-bolus & désintensification » justifiant la carte (lecture AGP §5, chiffrage,
  provenance) et signalant la tension (voir plus bas) ; ligne « Interprétation → décision (lecture de
  l'AGP) » de §5 scindée pour distinguer TBR/CV (« Corriger l'hypoglycémie… ») de la baisse continue
  nocturne (« Réduire la basale »).
- `src/features/decision/lib/labels.ts` — tooltip (`ENUM_VALUE_DESCRIPTIONS.baisse_continue`) corrigé :
  promettait « réduire la basale, envisager un analogue de 2ᵉ génération, relâcher la cible » (contenu
  de l'ex-carte) ; ne promet plus que ce que la sélection déclenche réellement aujourd'hui.
- `src/features/decision/engine/evaluateNode.insuline.test.ts` — le cas de non-régression écrit
  **avant** le code (étape 1 de `plans/P8/S9.md`) : vignette E-06 corrigée (attendait
  `CORRIGER_HYPO`, attend désormais `REDUIRE_BASALE` — le seul signal de la vignette étant la baisse
  continue nocturne) ; son miroir ajouté (`TBR > 4` sans baisse continue → `CORRIGER_HYPO` toujours
  vrai, `REDUIRE_BASALE` faux) ; vignette N11 ajoutée (basal-bolus complet + capteur + baisse continue
  → `REDUIRE_BASALE` vrai, `CORRIGER_HYPO` faux comme avant, `OPTIMISER_BB` toujours présent à côté, et
  vérification que `calculs` porte bien `{ libelle: "Basale réduite (−2 U)", expression:
  "dose_basale_actuelle - 2", unite: "U/j" }`).
- `src/features/decision/engine/banc/__snapshots__/caracterisation.insuline.txt` et
  `caracterisation-indetermine.insuline.txt` — régénérés (`npx vitest run … -u`), diffs relus (analyse
  détaillée ci-dessous, pas seulement le diff texte — brouillé par les changements de libellé S2, non
  commités dans le même arbre de travail).

### La carte créée

État **final** (après correction de provenance, cf. « Correction post-revue » ci-dessous) :

| Champ | Valeur |
| --- | --- |
| `intitule` | « Réduire la basale » |
| `role` | `geste` |
| `action` | `reduire` |
| `famille` | « Ajuster le schéma en place » (même registre qu'« Optimiser la répartition du basal-bolus » — ajustement d'une dose déjà en place guidé par l'AGP ; pas « Alléger le schéma », qui répond à un sur-traitement de fond sans signal actif) |
| `conditions` | `situation_insuline != naif` **ET** `mcg_disponible == true AND profil_nocturne == baisse_continue` — ouverte dans tout schéma comportant une basale, pas seulement basale_seule/basale_plus_bolus |
| `priorite` | 1 (devant « Optimiser la répartition du basal-bolus », `priorite: 2`, même famille) |
| chiffre affiché (`calculs`) | « Basale réduite (−2 U) » = `dose_basale_actuelle - 2` U/j — patron exact de « Basale après +2 U » de « Titrer la basale » |
| `niveau_preuve` | `modere` (corrigé de `faible` en 2ᵉ passe) |
| `references` | `sfd-2025-avis18`, `has-2024-r87` — sources **directes** des chiffres (accord d'experts), pas seulement des sources de la montée invoquées par symétrie ; `ebmfrance-insuline` retirée (son contenu propre, le palier −4 U symptomatique, n'est pas repris sur cette carte) |
| provenance écrite sur la carte | « Réduire la basale de −2 U, ou −10 % de la dose au-delà de 40 U/j (SFD 2025, Avis 18) ; réévaluer tous les 3 jours (HAS 2024, R.87, qui écrit la règle dans les deux sens…). Mêmes chiffres, même source, que « Corriger l'hypoglycémie ou la variabilité » » (`avantages`) ; nuance conservée en `inconvenients` : « Aucun essai n'a randomisé cette règle de décroissance… accord d'experts… ils encadrent le geste, ils ne le démontrent pas » |

Exclusions inchangées de « Titrer la basale »/« Ne pas sur-titrer la basale » (rôle 2 de la Décision
clé de T-067) empêchent déjà mécaniquement toute cohabitation avec « Titrer la basale » — vérifié par
construction (aucune vignette bench ne montre les deux ensemble). Coexistence avec « Ajouter un
bolus/un GLP-1 » (rôle 3, E-04b) et avec « Optimiser la répartition du basal-bolus » (même famille)
reste possible et voulue, aucune exclusion nouvelle écrite.

### Correction post-revue (2ᵉ passe, provenance des chiffres)

**Ce qui s'est passé.** La 1ʳᵉ passe de cette session a suivi la lettre du brief `plans/P12/S4.md` (
« les chiffres de la descente sont une symétrie… PAS une donnée publiée… attribuer ces chiffres à une
source serait la seule vraie faute possible »), tout en signalant — sans trancher — que le nœud
citait déjà, à trois endroits distincts, HAS 2024 R.87 et SFD 2025 Avis 18 comme portant
**explicitement** la règle dans les deux sens pour la descente RÉACTIVE. **L'orchestrateur a revu ce
signalement et confirmé que c'était le brief P12/S4 qui avait tort, pas la carte** : le rapport
OpenEvidence cité disait « aucun schéma de décroissance validé **PAR ESSAI** » — une affirmation sur le
NIVEAU DE CERTITUDE (accord d'experts vs randomisé), pas sur l'EXISTENCE d'une source. La confusion
entre « aucun essai » et « aucune source » avait fait écrire une provenance fausse sur la carte, en
contradiction directe avec la carte voisine (« Corriger l'hypoglycémie… ») qui porte les mêmes chiffres,
sourcés, en `niveau_preuve: modere` — l'invariant 6 (exactitude médicale) était en jeu.

**Ce qui a changé** (contenu, pas structure — titre/famille/action/conditions/priorité/`calculs`/
déplacement du déclencheur/garde-fous B3c/vignettes de banc/snapshots tous inchangés) :

- `avantages`/`inconvenients`/`effet_attendu` de « Réduire la basale » réécrits : « par symétrie…
  aucune donnée propre à la décroissance » retiré, remplacé par la citation directe de HAS 2024 R.87 et
  SFD 2025 Avis 18 (accord d'experts), au même titre que « Corriger l'hypoglycémie… ». La nuance qui
  reste vraie est conservée : aucun essai n'a randomisé cette règle de décroissance contre une autre
  stratégie.
- `niveau_preuve` : `faible` → `modere`, aligné sur la carte voisine (mêmes sources, même grade —
  aucune raison de certitude différente entre les deux cartes pour les mêmes chiffres).
- `references` : `ebmfrance-insuline` retirée (son contenu propre, le −4 U symptomatique, n'est pas
  repris ici) ; `sfd-2025-avis18`/`has-2024-r87` conservées, désormais citées comme preuve directe.
- Long commentaire YAML de l'option réécrit pour raconter la résolution plutôt que la tension ;
  `insuline.argumentaire.md` §4 réécrit dans le même esprit ; cross-référence de « Désintensifier… »
  et tooltip `labels.ts` (`profil_nocturne=baisse_continue`) mis à jour en cohérence.
- `meta.version` 0.40 → 0.41, nouvelle entrée de `meta.changelog` en tête (l'entrée de la 1ʳᵉ passe est
  conservée en dessous, comme trace historique).

**N0 revérifié après la 2ᵉ passe** : `npm test` (suite complète) toujours **986 passés, 11 skip,
0 échec** — aucun test ne verrouillait la provenance/`niveau_preuve` (non capturés par le format
compact des snapshots), donc aucune régénération nécessaire. `npm run typecheck` : 0 erreur.

**Plancher non encodé, signalé plutôt qu'inventé** (« Si bloqué » de `plans/P8/S9.md`) : la grammaire
`calculs` (`engine/deriveCritere.ts`, `ARITH_RE`) n'accepte qu'un seul opérateur binaire par expression
— aucune fonction `max`/`min`/clamp, aucune branche conditionnelle. Impossible d'écrire un plancher
dans `dose_basale_actuelle - 2` sans casser le parseur. La carte le dit en `inconvenients` (« à très
faible dose de basale, vérifier cliniquement la pertinence du résultat »).

### Profils du banc qui changent (180 profils golden master + 15 partiels)

Analyse faite sur les critères réels de chaque profil (pas sur le diff texte brut, brouillé par les
raccourcis de libellé de S2 non commités dans le même arbre) :

- **2 profils perdent « Corriger l'hypoglycémie ou la variabilité »** imputable à T-067 (seul signal =
  baisse continue nocturne, ni TBR > 4 ni CV > 36) : profil **#23** (`basale_seule`) et profil **#155**
  (`basale_plus_bolus`). **Les deux gagnent « Réduire la basale » à la place** — aucun ne repart sans
  geste.
- **22 profils gagnent « Réduire la basale »** au total, dont **5 en `basal_bolus` complet** (#88,
  #110, #134, #147, #152) — le bénéfice de bord de B3a : ces patients n'avaient **aucune** carte liée à
  la baisse continue nocturne avant ce lot (« Corriger l'hypoglycémie… » n'a jamais couvert
  `basal_bolus`). Les 17 autres (#23, #26, #29, #33, #41, #53, #63, #71, #74, #97, #118, #127, #130,
  #139, #155, #159, #173) sont en `basale_seule`/`basale_plus_bolus`, majoritairement aux côtés de
  « Corriger l'hypoglycémie… » toujours présente (autre signal actif) — seuls #23 et #155 la perdent.
- **Vérification de sécurité demandée** (« aucun profil ne perd les deux ») : **confirmée vide** — 0
  profil sans aucun geste après ce lot, sur les 180 golden master et les 15 profils partiels
  (`caracterisation-indetermine` : les deux options n'apparaissent jamais qu'ensemble en « en attente »,
  jamais l'une résolue sans l'autre visible).
- Script d'analyse et sortie complets, disponibles pour relecture indépendante :
  `C:\Users\kovu\AppData\Local\Temp\claude\c--Users-kovu-SynologyDrive-Thibault-Projets\2ecebaff-3793-4638-ba6c-1d1cedef988f\scratchpad\analyse_snapshot.js`.

### N0 — résultat exact

> Mesures de la 1ʳᵉ passe (déplacement du déclencheur + carte + tests). Inchangées après la 2ᵉ passe
> (correction de provenance, cf. « Correction post-revue » ci-dessus) : ni condition, ni `calculs`, ni
> intitulé n'ont bougé en 2ᵉ passe — seule la prose/`niveau_preuve`/`references`, non capturées par le
> banc. Revérifié : toujours 986/11/0 après la 2ᵉ passe.

- **Avant cette session** (base annoncée) : 984 passés, 11 skip, 0 échec.
- **Immédiatement après le déplacement du déclencheur, avant correctif** : 981 passés, 11 skip,
  **3 échecs** — 1 vignette (`evaluateNode.insuline.test.ts`, E-06) attendant encore « Corriger
  l'hypoglycémie… » sur le seul signal baisse continue nocturne, et les 2 snapshots à régénérer.
  Comportement attendu, pas une régression (cf. brief : « des profils VONT changer de sortie »).
- **Après correctif du test + régénération des snapshots + 2 vignettes ajoutées** (`npm test`, suite
  complète, foreground) : **986 passés, 11 skip, 0 échec** (997 au total) — 984 + 2 nouvelles vignettes
  (le miroir de non-régression et la vignette N11 basal-bolus).
- `npm run typecheck` : 0 erreur (revérifié après tous les correctifs, PAS `npx tsc --noEmit`).
- `npm run build` : OK (même avertissement pré-existant sur la taille du chunk JS, sans rapport avec ce
  lot).
- **N1 visuel** : non fait — cet environnement d'exécution n'a pas de navigateur in-app. Reporté à la
  recette de fin de plan, comme prévu par la validation de ce brief si le navigateur est indisponible.

### Points N2 — à faire juger par le référent

1. **✅ RÉSOLU en 2ᵉ passe, ne figure plus comme point ouvert** — provenance des chiffres de descente.
   En vérifiant les sources du nœud à l'étape 5 de `plans/P8/S9.md` (« si une source du nœud porte un
   protocole de descente, utilise-la et signale-le »), la 1ʳᵉ passe avait trouvé que le nœud cite
   **déjà**, en trois endroits distincts, HAS 2024 R.87 et SFD 2025 Avis 18 comme portant explicitement
   la règle dans les deux sens pour la descente RÉACTIVE — en tension avec la lettre du brief P12/S4
   (« symétrie, aucune donnée publiée »). **Signalé plutôt que tranché**, conformément à la consigne
   « si tu dois arbitrer une question clinique : STOP, signale ». **L'orchestrateur a confirmé que
   c'était le brief P12/S4 qui avait tort** (confusion entre « aucun essai » — OpenEvidence — et
   « aucune source » — non contestée) et a demandé la correction, appliquée dans cette même session (cf.
   « Correction post-revue » ci-dessus) : `niveau_preuve` aligné sur `modere`, provenance réécrite pour
   citer HAS 2024 R.87/SFD 2025 Avis 18 directement, cohérence rétablie avec « Corriger l'hypoglycémie…
   ». Aucun changement de geste, de dose ni de déclencheur — uniquement l'attribution de la preuve.
2. **Classification `role: geste` (pas `securite`) pour le cas « baisse continue nocturne isolée ».**
   Avant ce lot, un patient dont le seul signal était la baisse continue nocturne recevait un geste
   `role: securite` (« Corriger l'hypoglycémie… », jamais replié ni plafonné, D25). Il reçoit
   désormais un geste `role: geste` (« Réduire la basale », prescrit explicitement par l'étape 3 de
   `plans/P8/S9.md`), potentiellement soumis au plafond d'affichage à 5 pistes (D25) dans un dossier très
   chargé. Aucun profil du banc ne fait apparaître ce cas de figure (le plafond n'a jamais été atteint
   dans les 180+15 profils), mais le changement de canal mérite d'être noté pour le référent — B3a ne
   discute pas explicitement ce point.
   La carte « dit-elle assez pour agir » (point (a) de la validation de `plans/P8/S9.md`) : nomme le
   geste, donne un chiffre (−2 U ou −10 % au-delà de 40 U/j), un rythme (3 jours) et une provenance
   explicite — à l'avis du référent sur si c'est suffisant sans en dire plus sur le suivi.
3. **Cohabitation « Réduire la basale » / « Corriger l'hypoglycémie ou la variabilité »** (point (b) de
   la validation de `plans/P8/S9.md`) : un patient avec **à la fois** baisse continue nocturne **et**
   TBR élevé voit les deux cartes côte à côte (aucune exclusion mutuelle écrite, cf. les 15 profils du
   banc qui gagnent « Réduire la basale » tout en gardant « Corriger l'hypoglycémie… », listés
   ci-dessus). Informatif (deux signaux distincts, deux gestes numériquement différents) ou redondant
   (les deux disent « réduire la basale ») ? À trancher par le référent.
4. **N1 non fait** (pas de navigateur in-app dans cet environnement) : la vignette N11 doit être
   reproduite au navigateur pour confirmer que le titre + le chiffre sont bien sur la **face visible**
   de la carte sans ouvrir le chevron « Argumentaire complet ». Le contenu est câblé pour l'exposer
   via la pastille « Posologie » (D45, `OptionCard.tsx`, code que je n'ai PAS touché — hors périmètre,
   territoire de S3) : `calculs` non vide → la pastille apparaît systématiquement dans la rangée
   toujours visible, son survol/panneau affiche « Basale réduite (−2 U) ≈ … U/j ». Si l'intention du
   critère de fin est plus stricte (le chiffre visible SANS aucun clic, y compris sur la pastille), ce
   n'est pas acquis avec l'architecture actuelle et relèverait de S10 (T-135/T-136, hors périmètre de
   cette session).

### T-067 sort de `plans/P8`

Comme demandé par ce brief : la consolidation de fin de plan devra retirer la ligne T-067 de
`TASKS.md §P8` et clore P8 (P8/S9 n'est plus à exécuter séparément — absorbée ici).

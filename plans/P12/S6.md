# P12 · S6 — Déprescription : onze aperçus muets et une alerte manquante   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 3 (parallèle : non — après S5, même fichier)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-02 · Branche : —

## Lire (commun à la session)

- `src/features/decision/components/OptionCard.tsx` l.240-260 — **la mécanique de la pastille** :
  `aDuContenuPosologie = Boolean(option.apercu) || calculs.length > 0 || enAttentePosologie`.
  Sans `apercu`, sans `calculs`, sans attente : **pas de pastille du tout**. C'est la cause exacte.
- `content/decision/noeuds/diabete-type-2/insuline.yaml` — l'`apercu` de l'option
  « Désintensifier / alléger le schéma » : « *réduire au jugement clinique — aucun rythme chiffré
  sourcé pour la déprescription programmée* ». **C'est le gabarit des cas sans donnée**, et le rapport
  OpenEvidence du 2026-08-02 confirme qu'il a raison.
- `content/decision/noeuds/diabete-type-2/prescription.yaml` — les **11 options** listées en T-126, et
  les `alertes` de nœud (pour T-127).
- `docs/decision/GRAMMAIRE-NOEUD.md` — **R8** (un canal par fait de sécurité) : une dose qui vit dans
  une alerte de nœud **peut** être répétée sur la carte qui porte le geste, ce n'est pas un doublon.
- `DECISIONS.md` — D5 (bump + changelog), D21 (canal d'un fait de sécurité).

## Hors périmètre

- **N'extrapole aucun chiffre.** Le rapport OE est formel : les rythmes de descente n'existent qu'en
  contexte de **relais thérapeutique**. Ne les transporte pas vers la déprescription gériatrique
  (vignette N7 : « je baisse les 12 UI de combien ? » **reste sans réponse chiffrée, et c'est la
  bonne réponse**).
- Ne change aucune `condition`, `exclusions`, `priorite`, `role`, `action`, `niveau_preuve`, ni aucun
  intitulé (c'était S2).
- Ne rouvre pas le seuil sulfamide `DFG < 30` : voir « Ce qui n'est PAS un défaut » ci-dessous.
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `index.md`, ni `VALIDATION.md`. Ni commit ni push.

## ⚠ Ce qui n'est PAS un défaut — ne pas « corriger »

Le rapport OE constate que ni la KDIGO ni l'ADA ne portent de seuil chiffré d'arrêt pour les
sulfamides. **Cela ne contredit pas le contenu.** `prescription.yaml` (commentaire de l'option
« Arrêter le sulfamide ») documente déjà ce point depuis le 2026-07-27 : l'attribution KDIGO était
fausse et a été retirée ; le seuil est une **citation de la SFD** (2023 et 2025, Tableau I
notes 1-2 + Avis n° 12), source française qu'OE ne consulte pas. Le contenu sort **renforcé** d'un contrôle
adverse externe. Si tu es tenté de toucher à cette carte : ne le fais pas.

---

## T-126 — Toute carte qui prescrit un changement de dose porte une pastille

### Objectif

Que la déprescription cesse d'être la moitié muette de l'outil.

### Décision clé

**Mesuré au cadrage : 11 des 12 cartes `reduire`/`arreter` n'ont aucun `apercu`**, donc aucune
pastille — pas même la pastille ambre qui signalerait qu'une dose manque. Ce sont les cartes dont le
titre contient le mot « posologie » et qui n'en donnent aucune. Verdict du praticien :

> « Le clic n'est pas le problème ; le silence l'est. »

**Règle à poser : une carte qui prescrit un changement de dose porte une pastille, fût-ce pour dire
« au jugement clinique ».** Le silence n'est pas une réponse ; « il n'y a pas de chiffre » en est une.

**Ce que le rapport OpenEvidence du 2026-08-02 permet d'écrire** (analysé au cadrage ; il n'y a rien
à rechercher, tout est ci-dessous) :

| Option (`prescription.yaml`) | `apercu` à écrire | Provenance à citer |
| --- | --- | --- |
| Arrêter la metformine (DFG < 30) | arrêt immédiat, sans décroissance | KDIGO 2022 (explicite) |
| Réduire la posologie de la metformine | dose max ÷ 2 entre DFG 30 et 45 ; initiation ≤ 500 mg | KDIGO 2022 / RCP ANSM *(déjà dans l'alerte de nœud — la répéter ici est licite, R8)* |
| Arrêter le sulfamide (DFG < 30) | arrêt, pas de décroissance ; l'hypoglycémie peut persister après la dernière prise | SFD 2023/2025 *(seuil, déjà en place)* + OE pour la persistance |
| Réduire la posologie du sulfamide | à l'introduction d'un AR GLP‑1 : arrêt si HbA1c ≤ 7,5 % · −50 % si 7,6-8,5 % · maintien si > 8,5 % | **avis d'experts** — JAMA Cardiol 2020 |
| Arrêter la gliptine redondante | arrêt franc, sans fenêtre ni décroissance | JAMA Cardiol 2020 |
| Suspendre l'iSGLT2 (cétonémie) | arrêt net ; **pas de rationnel pour une demi-dose** (aucune relation dose-effet sur le bénéfice cardio-rénal) | ACC 2020 |
| Réduire la posologie de l'insuline | −10 à −20 %, ou −20 à −30 % si à l'objectif ou hypoglycémies (contexte de relais) ; **jamais d'arrêt brutal sans relais** | **avis d'experts** — AAFP 2026 / JAMA Cardiol 2020 |
| Réduire la posologie de l'AR GLP‑1 | à l'arrêt : ~50 % de la baisse d'HbA1c reprise en 8-12 sem., ~60 % du poids repris à 52 sem. ; surveiller 4-12 sem. | EClinicalMedicine 2026 |
| Réduire la posologie du tirzépatide | même cinétique de reprise que l'AR GLP‑1 | JAMA Intern Med 2025 (SURMOUNT-4) |
| Réduire la posologie du glinide | aucun rythme sourcé ; demi-vie courte, prise prandiale — suspension immédiate acceptée | *gabarit « pas de donnée »* |
| Déprescrire la metformine | aucun rythme sourcé pour la déprescription programmée — au jugement clinique | *gabarit « pas de donnée »* |

**Deux exigences non négociables.**

1. **Le niveau de preuve doit être visible dans le texte.** Les deux chiffres les plus utiles
   (−50 % du sulfamide, −20-30 % de l'insuline) viennent d'une **revue d'experts** américaine de
   2020, pas d'une recommandation française. Ils s'écrivent comme tels (« avis d'experts », « en
   contexte de relais »), **jamais** au même registre qu'une HAS ou un RCP ANSM. C'est la doctrine
   du nœud voisin, que la recette salue : « le recul manque, pas l'autorisation ».
2. **Un `apercu` générique commun** peut porter la surveillance : *contrôle de l'HbA1c à 3 mois et
   autosurveillance les premières semaines* — OE établit que ce délai couvre la cinétique de toutes
   les classes. À placer là où il ajoute quelque chose, pas sur les onze.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/prescription.yaml` (les 11 `apercu` +
`meta.version` + `meta.changelog`) · les snapshots du banc `prescription` (régénérés).

### Étapes

1. Vérifie la liste des 11 **sur pièces** (recherche des options portant `action: reduire` ou
   `action: arreter` sans `apercu`). Si le compte diffère de 11, dis-le et travaille sur ce que tu
   trouves — S2 a pu en modifier les intitulés, pas leur nombre.
2. Écris les `apercu` du tableau. Français de consultation, une à deux phrases, la provenance dedans.
3. Pour les deux cas « pas de donnée », reprends **littéralement le registre** de l'`apercu` existant
   d'`insuline.yaml` (cité en « Lire ») : c'est le modèle validé.
4. Vérifie qu'aucun `apercu` ajouté ne contredit une `alerte` de nœud existante. Si contradiction :
   **STOP**.
5. Bump version + changelog (D5), en distinguant ce qui est sourcé français de ce qui est avis
   d'experts américain.
6. Régénère les snapshots, relis les diffs : **uniquement des ajouts d'`apercu`**. Toute option qui
   apparaît, disparaît ou change de rang → **STOP**.

### Validation

- **N0 auto (bloque le commit)** : `npm test` (suite complète, foreground) → vert ·
  `npm run typecheck` → 0 erreur · `npm run build` → OK · Ajv → OK.
- **N1 visuel auto** : sur les profils N5 (DFG 31) et N7 (déprescription chez la très âgée),
  **chaque carte « réduire » ou « arrêter » porte une pastille**. → `/verif-visuelle`
- **N2 humain (jugement clinique, le plus important de ce plan)** : les onze textes sont-ils justes,
  et le niveau de preuve est-il lisible comme tel ? À consigner dans `VALIDATION.md`, texte par texte.

### Si bloqué

Si une carte te semble appeler un chiffre que le tableau ci-dessus ne donne pas : **écris le gabarit
« pas de donnée », ne cherche pas ailleurs.** Le rapport OE a été fait pour cette question et conclut
explicitement à l'absence — c'est une réponse, pas un trou à combler.

### Message de commit (appliqué en fin de plan)

`feat(dt2): toute carte qui change une dose porte une posologie, fût-ce « au jugement clinique »`

---

## T-127 — L'acidocétose euglycémique se prévient, elle ne se constate pas

### Objectif

Donner à l'outil un canal **préventif** sur le risque d'acidocétose euglycémique sous iSGLT2 — il ne
le connaît aujourd'hui que rétrospectivement.

### Décision clé — arbitrage référent du 2026-08-02

**Le constat qui a déclenché cet arbitrage** (recette du 02/08, N7) : chez Mme Chevallier, 88 ans,
49 kg, dénutrie, **qui saute des repas**, l'iSGLT2 est proposé badgé `Preuve élevée` alors que
l'AR GLP‑1 est écarté par « IMC < 22 et dénutrition ».

**Ce qui a été vérifié, et qui déplace la question.** L'asymétrie est **fondée** : le garde-fou des
incrétines repose sur leur effet **anorexigène** (`prescription.yaml:946`, « aggravation de la
dénutrition / sarcopénie »), que l'iSGLT2 ne partage pas — sa perte de poids est glycosurique.
**L'arbitrage du 2026-07-29 tient : pas d'exclusion sur le poids, aucune donnée ne la fonde.**

**Mais le risque réel de cette patiente n'est pas son poids, c'est ce qu'elle mange.** L'acidocétose
euglycémique est précipitée par le jeûne et les apports glucidiques insuffisants — et **le nœud le
sait déjà**, il l'écrit trois fois : « rechercher la cause (jeûne, chirurgie, maladie aiguë,
**apports glucidiques insuffisants**) », « risque d'acidocétose (**suspendre si jeûne**, chirurgie,
sepsis) », et le rapport OpenEvidence du 02/08 ajoute l'arrêt **≥ 3 jours avant une chirurgie
programmée** (4 j pour l'ertugliflozine ; ACC 2020) — **même mécanisme**.

Le trou est là : la carte « Suspendre l'iSGLT2 » se déclenche sur `cetonemie == true`, une cétonémie
**déjà confirmée**. Rien ne s'adresse au patient qui va la faire.

**Décision : une seule alerte, à canal unique** (D21 — jeûne, apports insuffisants et chirurgie
programmée sont **un** fait de sécurité, pas trois), portée par la **carte iSGLT2** :

- **déclencheur** : présence d'un iSGLT2 (en cours ou proposé) **et** `denutrition == true` ;
- **contenu** : le risque d'acidocétose euglycémique en cas d'apports glucidiques insuffisants, de
  jeûne ou de chirurgie programmée · le fait que **la glycémie peut rester normale**, ce qui retarde
  le diagnostic · le geste (suspendre) · le délai chirurgical chiffré ;
- **ce que l'alerte ne fait pas** : elle **n'écarte pas** l'option et ne change **aucune condition**.
  Une alerte qualifie un geste sans l'interdire (R8).

⚠ **Ne crée aucun critère.** Un champ « chirurgie programmée » coûterait de la saisie à tous pour
servir quelques-uns — la charge de saisie est le risque n° 1 déclaré du projet. `denutrition` existe
et suffit comme porteur.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/prescription.yaml` (le bloc `alertes` de
l'option iSGLT2 + version/changelog) · `insuline.yaml` **seulement si** ce nœud lit aussi un iSGLT2
en cours — vérifie, ne suppose pas.

### Étapes

1. Vérifie ce qui existe déjà : `alertes` de l'option iSGLT2, `contre_indications`, alertes de nœud,
   argumentaire. **Le délai chirurgical de 3 jours est-il quelque part ?** (au cadrage : non trouvé).
2. Rédige l'alerte unique. Français de consultation. Le point que le praticien doit retenir en premier
   est que **la glycémie peut rester normale** — c'est ce qui rend cette complication traître.
3. Conditionne-la sur le patron des alertes d'option existantes du nœud. Vérifie qu'elle **ne se
   déclenche pas** chez un patient sous iSGLT2 non dénutri, ni chez un dénutri sans iSGLT2.
4. Bump version + changelog (D5), en citant l'arbitrage référent du 2026-08-02 et sa motivation
   (l'exclusion sur le poids est refusée, l'alerte sur les apports est retenue).
5. Régénère les snapshots, relis les diffs : **aucune option ne doit apparaître ni disparaître**.

### Validation

- **N0 auto** : `npm test` → vert · `npm run typecheck` → 0 erreur · `npm run build` → OK · Ajv → OK.
- **N1 visuel auto** : sur le profil N7 (88 ans, dénutrition, iSGLT2 proposé), l'alerte apparaît sur
  la carte iSGLT2 **sans** que la carte disparaisse ni change de badge. → `/verif-visuelle`
- **N2 humain** : l'alerte répond-elle à l'inquiétude réelle du praticien devant cette patiente, ou
  déplace-t-elle seulement le malaise ? À consigner dans `VALIDATION.md`.

### Si bloqué

- **Si tu es tenté d'ajouter `denutrition` ou `IMC < 22` aux `exclusions` de l'iSGLT2** : ne le fais
  pas. C'est explicitement refusé, deux fois (2026-07-29 et 2026-08-02), faute de donnée.
- **Si aucun `alertes` d'option n'existe sur l'iSGLT2** et qu'il faut en créer le bloc : c'est normal,
  suis le patron de l'AR GLP‑1, qui en porte un (alerte de prudence rénale sous DFG 15).

### Message de commit (appliqué en fin de plan)

`feat(dt2): alerte préventive d'acidocétose euglycémique — jeûne, apports insuffisants, chirurgie`

---

## Fin de session

Dérouler `/fin-de-tache` (mode **solo**).

---

## Bilan de session — T-126, T-127 (2026-08-03)

> Mode **consolidation différée** (orchestrateur) : aucun fichier partagé touché (`STATUS.md`,
> `TASKS.md`, `plans/P12/index.md`, `VALIDATION.md`) ; bilan et points N2 consignés ici, à reverser à
> la consolidation. Ni commit ni push (`.claude/wave.lock` présent).

### Fichiers modifiés

- `content/decision/noeuds/diabete-type-2/prescription.yaml` — 11 `apercu` ajoutés (T-126), un bloc
  `alertes` ajouté sur l'option « Introduire un iSGLT2 » (T-127), `meta.version` 0.53 → 0.55 (deux
  bumps), deux entrées de changelog.
- `content/decision/noeuds/diabete-type-2/insuline.yaml` — message de l'alerte de nœud « iSGLT2 +
  insuline » enrichi (T-127, `quand` INCHANGÉ), `meta.version` 0.42 → 0.43, une entrée de changelog.
- `src/features/decision/engine/banc/__snapshots__/caracterisation.prescription.txt`,
  `caracterisation.insuline.txt`, `caracterisation-indetermine.insuline.txt` — régénérés
  (`npx vitest run src/features/decision/engine/banc/caracterisation.test.ts -u`). Vérifié par
  horodatage que `caracterisation-indetermine.prescription.txt` et les deux fichiers `statine` n'ont
  PAS été réécrits par cette commande (aucun profil figé de ces bancs n'est affecté) — cohérent avec
  des changements qui n'ont touché ni `statine.yaml`, ni une combinaison de critères présente dans le
  banc « indéterminé » de `prescription`.

### T-126 — les 11 `apercu`

Compte vérifié sur pièces (`action: reduire` ou `action: arreter` sans `apercu`, recherche dans le
fichier) : **exactement 11**, conforme à la Décision clé du brief. Carte → texte écrit → provenance et
niveau :

1. **Arrêter la metformine (DFG < 30)** → « arrêt immédiat, sans décroissance : contre-indication
   formelle dès DFG < 30 » → KDIGO 2022, registre recommandation.
2. **Réduire la posologie de la metformine** → « dose maximale divisée par 2 entre DFG 30 et 45
   (2 000 → 1 000 mg/j) ; initiation ≤ 500 mg » → KDIGO 2022 / RCP ANSM, déjà porté par l'alerte de
   nœud (répétition licite, R8/D21) — registre recommandation.
3. **Arrêter le sulfamide (DFG < 30)** → « arrêt, sans décroissance ; l'hypoglycémie peut persister
   après la dernière prise, par accumulation » → RCP rubrique 4.4 (mécanisme) + seuil cité par la SFD
   2023/2025 (déjà en place, non rouvert) — registre recommandation/RCP.
4. **Suspendre l'iSGLT2** → « arrêt net, sans demi-dose : aucune relation dose-effet démontrée sur le
   bénéfice cardio-rénal » → ACC 2020.
5. **Arrêter la gliptine redondante** → « arrêt franc, sans fenêtre de chevauchement ni décroissance »
   → JAMA Cardiol 2020 (registre repris tel quel du brief, sans hedge supplémentaire non demandé).
6. **Réduire la posologie de l'insuline** → double registre explicite : *en relais thérapeutique*
   −10 à −20 % (−20 à −30 % si à l'objectif/hypoglycémie), jamais d'arrêt brutal sans relais, **étiqueté
   « avis d'experts » (AAFP 2026 / JAMA Cardiol 2020)** ; *hors relais (allègement isolé)* : « au
   jugement clinique, aucun rythme chiffré sourcé ». Voir « Point de vigilance tenu » ci-dessous.
7. **Réduire la posologie de l'AR GLP‑1** → données sur l'ARRÊT complet (pas la réduction) : ~50 % de
   la baisse d'HbA1c reprise en 8-12 sem., ~60 % du poids repris à 52 sem. (EClinicalMedicine 2026) —
   présentées comme argument pour réduire plutôt qu'arrêter, jamais comme un guide de titration.
8. **Réduire la posologie du tirzépatide** → même cinétique de reprise (JAMA Intern Med 2025,
   SURMOUNT-4), même traitement.
9. **Réduire la posologie du sulfamide** → même double registre que l'insuline (6) : *à l'introduction
   d'un AR GLP‑1* arrêt si HbA1c ≤ 7,5 % / −50 % si 7,6-8,5 % / maintien si > 8,5 %, **avis d'experts
   (JAMA Cardiol 2020)** ; *hors relais* : gabarit « pas de donnée ».
10. **Réduire la posologie du glinide** → gabarit « pas de donnée », registre repris littéralement de
    l'`apercu` d'`insuline.yaml` (« Désintensifier / alléger le schéma »), complété par la raison
    pharmacocinétique déjà présente dans le nœud (demi-vie courte, prise prandiale).
11. **Déprescrire la metformine** → même gabarit, verbe adapté (« arrêter », option `action: arreter`).

**Point de vigilance tenu (rappel explicite du brief).** Les cartes 6 et 9 sont DEUX des trois cartes
affichées sur le profil N7 de la recette du 02/08 (Mme Chevallier, déprescription gériatrique pure,
sans relais — la troisième est « Désintensifier », non touchée, déjà sans chiffre). Le tableau du brief
donnait leurs chiffres SANS préciser qu'ils sont scopés au relais ; les avoir écrits tels quels aurait
répondu à la vignette N7 (« je baisse les 12 UI de combien ? ») avec un pourcentage sourcé pour un tout
autre contexte clinique — exactement l'extrapolation interdite. Résolu en écrivant les DEUX registres
dans le même `apercu`, explicitement distingués : le chiffre là où il est sourcé (relais), le gabarit
« pas de donnée » là où il ne l'est pas (allègement isolé) — la carte répond donc correctement aux DEUX
patients qui peuvent la voir, sans jamais transporter l'un vers l'autre.

**Étape 4 (contradiction avec une alerte de nœud) vérifiée** : les deux `apercu` rénaux (metformine,
sulfamide) RÉPÈTENT des chiffres déjà portés par les alertes de nœud correspondantes (licite, R8/D21,
cf. « Lire » du brief) — aucune contradiction trouvée.

### T-127 — l'alerte préventive

**Déclencheur retenu** : `denutrition == true`, porté par l'`alertes` de l'option « Introduire un
iSGLT2 » (patron « Introduire un AR GLP‑1 »/DFG < 15, comme suggéré par le « Si bloqué » du brief).

**Résolution d'un écart entre le texte du brief et la mécanique du moteur, à faire trancher (cf. point
N2 n° 1) :** le brief décrit le déclencheur comme « présence d'un iSGLT2 (en cours OU proposé) ET
dénutrition ». `lib/vueDecision.ts` (docstring `Option.alertes`) confirme qu'une alerte d'option n'est
évaluée QUE pour une option déjà APPLICABLE pour ce patient — jamais indépendamment. Or « Introduire un
iSGLT2 » porte `prerequis: traitements_en_cours ne_contient_pas iSGLT2` (inchangé, hors périmètre) : un
terme `traitements_en_cours contient iSGLT2` dans le `quand` de CETTE alerte serait donc TOUJOURS faux,
mort par construction — cette option ne peut structurellement pas couvrir le cas « iSGLT2 déjà en
cours ». Vérifié que ce cas n'a pas d'équivalent atteignable ailleurs dans le nœud sans toucher à une
`condition` (interdit) : la seule autre carte iSGLT2 (« Suspendre l'iSGLT2 ») ne s'affiche QUE si
`cetonemie == true`, ce qui aurait reproduit exactement le défaut rétrospectif que T-127 corrige.
Retenu : `denutrition == true` seul, qui couvre exactement le scénario vérifié de la recette (N7,
« iSGLT2 proposé ») et la validation N1 attendue du brief (« profil N7 … iSGLT2 proposé »).

**Texte de l'alerte** (niveau `attention`) :

> Chez un patient dénutri, l'acidocétose euglycémique sous iSGLT2 peut s'installer avec une GLYCÉMIE
> RESTÉE PROCHE DE LA NORMALE — c'est ce qui la rend traître et retarde le diagnostic si l'on ne
> s'appuie que sur elle. Elle est précipitée par des apports glucidiques insuffisants, le jeûne ou une
> chirurgie : y penser AVANT que la cétonémie ne la confirme (le nœud ne la détecte aujourd'hui que
> confirmée, cf. l'option « Suspendre l'iSGLT2 »). Suspendre l'iSGLT2 en cas de jeûne prolongé ou
> d'apports glucidiques manifestement insuffisants, et par principe au moins 3 jours avant une
> chirurgie programmée (4 jours pour l'ertugliflozine — ACC 2020). Cette alerte qualifie le geste, elle
> ne l'écarte pas : la dénutrition seule ne fonde aucune exclusion (à la différence de l'AR GLP‑1, dont
> l'effet anorexigène propre justifie son exclusion de terrain — l'iSGLT2 ne le partage pas).

Vérifié sur le banc régénéré : 4 profils figés du banc `prescription` déclenchent cette alerte, sur les
180 (`Introduire un iSGLT2` applicable + `denutrition == true`) ; dans les 4, l'option reste badgée
`recommandee`, ses `reasons`/exclusions/contre-indications sont inchangées — seule l'alerte s'ajoute.

**`insuline.yaml` touché, comme autorisé conditionnellement par le brief** (« seulement si ce nœud lit
aussi un iSGLT2 en cours — vérifie, ne suppose pas ») : vérifié vrai (`traitements_en_cours contient
iSGLT2`, alerte de nœud déjà existante avant cette session, `:1657` après édition). Un SEUL canal par
fait (D21) : le `quand` de cette alerte N'A PAS été restreint à `denutrition == true` (sa portée était
déjà plus large — tout patient iSGLT2 + insuline — et la restreindre aurait RÉTRÉCI une couverture
existante, jeûne/chirurgie/maladie aiguë chez le patient non dénutri). Seul le `message` est enrichi des
deux éléments nouveaux (glycémie normale en tête, délai chirurgical chiffré) ; aucune option n'apparaît,
ne disparaît, ni ne change de rang sur les deux bancs `insuline` régénérés.

### N0 — résultat exact

`npm test` (foreground, suite complète) : **986 passés, 11 skip, 0 échec** — identique au chiffre de
référence donné en tête de brief, mesuré après régénération des 3 snapshots ci-dessus. `npm run
typecheck` (`tsc -b --noEmit`, jamais `npx tsc --noEmit`) : **0 erreur**. `npm run build` : **OK**
(un seul avertissement pré-existant sur la taille du chunk JS, sans rapport avec cette session). Ajv :
couvert par `content.test.ts`/`modules.test.ts`, inclus dans `npm test` ci-dessus — vert.

### N1 — visuel

Aucun outil `preview_start`/`read_page`/`read_console_messages` disponible dans cet environnement
d'exécution (agent d'orchestration, pas Claude Code Desktop) : mode B de `/verif-visuelle` appliqué —
rien vérifié visuellement par cette session. Bloc prêt à dérouler (Desktop, ou VSCode + navigateur
manuel) :

```text
### Vérification visuelle à faire (N1 + N2) — Nœud « Traiter » (DT2), écran prescription
Lancer : npm run dev
Ouvrir : http://localhost:5173 (ou le port annoncé) → module Décision → domaine DT2 → nœud « Traiter »

Profil N5 (M. Traoré, 71 ans — DFG 31, sulfamide en cours, intention optimiser, position au-dessus) :
N1 :
- [ ] aucune erreur dans la console
- [ ] la carte « Réduire la posologie de la metformine » porte une pastille « Posologie »
      (texte attendu : dose max ÷2 entre DFG 30-45, initiation ≤ 500 mg)
- [ ] aucun appel réseau (module 100 % statique)

Profil N7 (Mme Chevallier, 88 ans, 49 kg, fragile, dénutrition, sulfamide + insuline en cours,
intention déprescrire, iSGLT2 proposé) :
N1 :
- [ ] aucune erreur dans la console
- [ ] les trois cartes du groupe « Traitement à alléger » (Désintensifier, Réduire l'insuline, Réduire
      le sulfamide) portent chacune une pastille « Posologie »
- [ ] la carte « Réduire la posologie de l'insuline » affiche, au survol/dépli, le double registre
      (chiffre relais avis d'experts + « au jugement clinique » hors relais) — PAS un pourcentage nu
- [ ] la carte « Introduire un iSGLT2 » affiche l'alerte acidocétose euglycémique (glycémie normale en
      tête du texte), reste badgée Recommandée/Preuve élevée, ne disparaît pas

N2 (jugement clinique — LE PLUS IMPORTANT DE CE PLAN, cf. bandeau du brief) :
- [ ] les onze textes d'`apercu` sont-ils cliniquement justes ?
- [ ] le niveau de preuve (avis d'experts vs recommandation) est-il lisible COMME TEL, pas seulement
      présent ?
- [ ] l'alerte iSGLT2/dénutrition répond-elle à l'inquiétude réelle du praticien devant Mme Chevallier,
      ou déplace-t-elle seulement le malaise (cf. Validation N2 du brief, T-127) ?
```

Cette partie N1 reste dans ce bilan (pas dans `VALIDATION.md`, mode consolidation différée) : à
dérouler depuis Desktop, ou reversée à la consolidation du plan.

### Points N2 — à faire juger par le référent

1. **Écart texte du brief / mécanique du moteur sur le déclencheur T-127, résolu par construction, à
   confirmer.** Le brief dit « iSGLT2 en cours OU proposé » ; la seule interprétation que le moteur
   permette sans toucher à une `condition`/`prerequis` (interdit) est « proposé » seul — cf. section
   T-127 ci-dessus pour la démonstration complète. Le patient « iSGLT2 déjà en cours + dénutrition
   apparue en cours de suivi » ne reçoit donc PAS cette alerte depuis `prescription.yaml` (il en reçoit
   une, plus générale et non conditionnée à la dénutrition, depuis `insuline.yaml` s'il est aussi sous
   insuline — sinon, aucune). Cette limite structurelle préexistait (`prescription.yaml` n'a jamais eu
   de carte « poursuivre l'iSGLT2 en cours ») ; T-127 ne l'aggrave pas, mais ne la comble pas non plus.
2. **Les deux `apercu` à double registre (insuline, sulfamide) sont plus longs que le gabarit « une
   ligne courte » du champ (`node.types.ts`, docstring `Option.apercu`).** Choix délibéré : la
   sécurité du contenu (ne pas laisser un chiffre de relais répondre, par défaut, à un patient hors
   relais) l'a emporté sur la concision. À confirmer que c'est le bon arbitrage, ou si une meilleure
   solution existe (ex. un second champ dédié au « hors contexte » — hors périmètre de cette session,
   changement de schéma).
3. **`insuline.yaml` touché alors que le brief le rendait conditionnel.** Choix : enrichir le MESSAGE
   d'une alerte déjà existante plutôt que ne rien faire ou dupliquer un second canal — cf. justification
   complète dans la section T-127 ci-dessus. À confirmer que c'est la lecture voulue de la clause
   « seulement si ce nœud lit aussi un iSGLT2 en cours ».
4. **Citations OpenEvidence non reproduites comme source clinique nommée à l'écran.** Là où le rapport
   OE motive un fait (ex. la persistance de l'hypoglycémie après l'arrêt du sulfamide), le texte cite la
   source PRIMAIRE que le rapport a lui-même vérifiée (RCP rubrique 4.4) plutôt que « OpenEvidence » —
   cohérent avec D23 (une revue/synthèse n'est jamais elle-même le sujet grammatical de l'argument
   affiché) et avec le principe déjà appliqué à `synthese_critique` dans ce nœud. À confirmer.

### Ce qui a été laissé en STOP

**Aucun.** Les deux tâches ont été menées à terme sur les 11 cartes et l'alerte demandées. Aucune
contradiction trouvée entre un `apercu` ajouté et une alerte de nœud existante (T-126, étape 4). Aucune
tentation suivie de toucher au seuil sulfamide DFG < 30, ni d'ajouter une exclusion sur le poids/l'IMC à
l'iSGLT2 (T-127, « Si bloqué »). Le seul point ambigu rencontré (déclencheur T-127 « en cours ou
proposé » vs mécanique du moteur) a été résolu par construction plutôt que reporté, et est signalé
explicitement en point N2 n° 1 plutôt que laissé en silence.

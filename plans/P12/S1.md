# P12 · S1 — Sûreté : la cible d'HbA1c cesse de changer toute seule   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 1 (parallèle : oui — avec S2)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-02 · Branche : —

## Lire (commun à la session)

- `src/features/decision/lib/esperanceVieDefault.ts` — **en entier** (66 lignes). La docstring de
  `suggestionEsperanceVieSiApplicable` (l.42-54) nomme ses trois appelants : c'est la carte du problème.
- `src/features/decision/lib/sessionCriteres.ts` — la fonction de mémorisation autour de la l.73
  (« Mémorise les valeurs SAISIES (`touched`) »). Ne pas la modifier : la comprendre.
- `src/features/decision/screens/DecisionNodeScreen.tsx` — **les trois seuls sites** qui appellent
  `suggestionEsperanceVieSiApplicable` (`handleCriteriaChange`, `handleConfirmerChamps`, et la reprise
  au clic sur « Reprendre les valeurs de ce patient »). Les localiser par recherche, ne pas lire l'écran entier.
- `content/decision/noeuds/diabete-type-2/cible-glycemique.yaml` — les `criteres_entree` (l.13-45)
  et le bloc `meta` de fin (version + changelog).
- `src/features/decision/content/node.types.ts` l.18-24 — la forme de `ReglePreremplissage`
  (`{ quand, valeur }`, première vraie retenue).
- `docs/decision/GRAMMAIRE-NOEUD.md` — R7 (jamais se prononcer sur ce qu'on ignore).

## Hors périmètre

- **Ne fusionne PAS `antecedent_cv` et `ASCVD_etablie`.** Arbitrage référent du 2026-08-02 : une
  cardiopathie rythmique et une maladie athéromateuse sont **deux faits cliniques distincts**. Toute
  tentative de les unifier est une erreur de lecture de ce brief.
- Ne touche pas à la mémoire de session (`sessionCriteres.ts`) ni à la distinction `touched` /
  `preremplis` : c'est un invariant de conception (D28).
- Ne modifie aucune règle de décision, aucune `condition` d'option, aucun seuil.
- Ne touche à aucun autre nœud que `cible-glycemique.yaml` (S2 travaille sur les trois autres **en
  parallèle** — tout fichier partagé casserait la vague).
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `plans/P12/index.md`, ni `VALIDATION.md`.
  Ni `git commit`, ni `git push`.

---

## T-119 — Une suggestion ne se recalcule pas sur un dossier amputé

### Objectif

Empêcher que la cible d'HbA1c d'un patient change entre deux moments de la même consultation, sans
un mot, avec le même badge `Recommandée`.

### Décision clé

Reproduction exacte (recette du 2026-08-02, constat n° 2) : nœud cible avec ☑ Antécédent
cardiovasculaire → espérance de vie « Intermédiaire », **Cible ≤ 8 %**. On va sur « Traiter », on
revient, on clique « Reprendre les valeurs de ce patient » → l'antécédent CV est **décoché**,
l'espérance de vie est recalculée « Longue », l'écran affiche **Cible ≤ 7 %** badgée `Recommandée`.

Le mécanisme, vérifié en source :

1. `antecedent_cv` n'a pas `partage: true` → il ne circule pas, donc il revient absent.
2. La mémoire de session ne retient que les valeurs `touched` → l'espérance de vie **suggérée** (jamais
   touchée) ne circule pas non plus.
3. `suggestionEsperanceVieSiApplicable` ne se garde que d'un choix **manuel** (`dejaChoisieAMain`) →
   elle recalcule, sur un dossier privé de l'un de ses quatre `ESPERANCE_VIE_DRIVERS`.

**Règle à poser** : *une suggestion ne se calcule que si tous ses drivers déclarés sont
déterminés dans le dossier courant.* Si l'un manque, ne rien suggérer — le champ reste vide, le
praticien répond. **Une réponse manquante vaut mieux qu'une réponse changée en silence** (R7 :
jamais se prononcer sur ce qu'on ignore).

⚠ Attention au sens de « déterminé » : un `bool` à `false` **explicitement répondu** est déterminé ;
un `bool` jamais touché ne l'est pas. C'est précisément la distinction qui manque aujourd'hui, et
c'est ce qui rend la tâche `high` — `Boolean(criteria.antecedent_cv)` rend `false` dans les deux cas.
Le porteur de l'information est l'ensemble `touched`, pas la valeur.

### Lire / Modifier

**Modifier** : `src/features/decision/lib/esperanceVieDefault.ts` ·
`src/features/decision/screens/DecisionNodeScreen.tsx` (les trois appelants : leur passer ce qu'il
faut pour que la garde s'applique) · le fichier de test de non-régression existant
`src/features/decision/screens/DecisionNodeScreen.reentree.test.tsx`.

### Étapes

1. Étends la signature de `suggestionEsperanceVieSiApplicable` pour qu'elle reçoive l'ensemble des
   critères **effectivement renseignés** (le `touched` dont disposent déjà les appelants), et
   renvoie `undefined` dès qu'un `ESPERANCE_VIE_DRIVERS` n'y figure pas.
2. Mets à jour les **trois** appelants. Aucun ne doit reconstruire la garde de son côté : c'est la
   raison d'être de cette fonction (cf. sa docstring).
3. Écris deux tests de non-régression dans `DecisionNodeScreen.reentree.test.tsx` :
   - le scénario exact du constat n° 2 (aller-retour entre deux nœuds) → la cible **ne change pas** ;
   - un `antecedent_cv` explicitement répondu « non » → la suggestion **se calcule quand même**
     (sinon la garde est trop large et supprime la fonctionnalité livrée le 2026-08-02).
   ⚠ `NOEUD_A`/`NOEUD_B` de ce fichier n'ont qu'un seul champ et cinq tests y supposent un
   `spinbutton` unique : crée des nœuds synthétiques dédiés, comme l'ont fait `NOEUD_ESP_A`/`NOEUD_ESP_B`.
4. Vérifie qu'aucun des 982 tests existants ne régresse.

### Validation

- **N0 auto (bloque le commit)** : `npm test` (suite **complète**, en foreground) → tout vert,
  ≥ 984 tests · `npm run typecheck` → 0 erreur (**jamais `npx tsc --noEmit`**, factice ici) ·
  `npm run build` → OK.
- **N1 visuel auto** : `—` (couvert par les tests ; l'écran est vérifié en S3).
- **N2 humain** : `—`.

### Si bloqué

Si un appelant n'a pas accès à `touched` au moment où il appelle la suggestion : **STOP**. Ne
reconstruis pas un `touched` approximatif depuis `criteria` — c'est exactement la confusion
valeur/renseignement qui a créé le défaut. Signale l'appelant et la ligne.

### Message de commit (appliqué en fin de plan)

`fix(decision): une suggestion ne se recalcule plus sur un dossier privé de ses drivers`

---

## T-120 — L'athérome implique l'antécédent cardiovasculaire, et le dit

### Objectif

Supprimer la double saisie du même fait dans le sens où elle est logiquement fondée, sans confondre
deux critères que le référent a déclarés distincts.

### Décision clé

Arbitrage référent du 2026-08-02 : `antecedent_cv` (nœud cible) est **plus large** que
`ASCVD_etablie` (nœuds Traiter et Statine) — il couvre notamment les cardiopathies rythmiques.
Les deux critères restent séparés.

Mais l'implication tient **dans un sens** : *une maladie cardiovasculaire athéromateuse établie EST
un antécédent cardiovasculaire.* Jamais l'inverse.

Encodage : une règle `preremplissage` sur `antecedent_cv` dans `cible-glycemique.yaml`, de la forme
`{ quand: "ASCVD_etablie == true", valeur: "true" }`. Le pré-remplissage est **signalé et
corrigeable** (mécanique K6 existante) — ce n'est pas une déduction imposée, c'est une proposition
que le praticien voit et peut défaire. Aucune règle dans l'autre sens.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/cible-glycemique.yaml` (le critère
`antecedent_cv` + `meta.version` + `meta.changelog`, D5).

### Étapes

1. Vérifie d'abord que `ASCVD_etablie` **arrive réellement** dans le dossier du nœud cible via la
   mémoire de session (il porte `partage: true` dans `prescription.yaml` et `statine.yaml`). Si le
   nœud cible ne déclare pas `ASCVD_etablie` dans ses `criteres_entree`, une règle qui le lit ne
   s'évaluera jamais → dans ce cas **STOP et signale** : l'encodage demandé suppose ce point, et il
   n'a pas été vérifié au cadrage.
2. Ajoute la règle `preremplissage`, avec un commentaire YAML qui dit **pourquoi le sens inverse
   n'existe pas** (sinon une passe future « symétrisera » et cassera l'arbitrage référent).
3. Bump `meta.version` + entrée de `meta.changelog` : date, auteur, résumé mentionnant l'arbitrage
   référent du 2026-08-02.
4. Relance la validation Ajv du nœud et la suite de tests : un `preremplissage` nouveau peut modifier
   des snapshots de caractérisation. **Relis le diff de snapshot ligne à ligne** — si une conclusion
   d'option change, ce n'est pas un effet attendu : **STOP**.

### Validation

- **N0 auto** : `npm test` → vert · `npm run typecheck` → 0 erreur · `npm run build` → OK ·
  validation Ajv du nœud modifié → OK.
- **N1 visuel auto** : `—` (vérifié en S3).
- **N2 humain** : le pré-remplissage doit se **voir** comme proposé, pas comme affirmé — à consigner
  dans `VALIDATION.md` lors de la consolidation.

### Si bloqué

Si le diff de snapshot montre un changement de **conclusion** (une option qui apparaît, disparaît, ou
change de badge) et pas seulement de pré-remplissage : **STOP**. Une règle de confort de saisie qui
déplace une recommandation n'est pas une règle de confort.

### Message de commit (appliqué en fin de plan)

`feat(dt2): l'athérome établi pré-remplit l'antécédent cardiovasculaire (cible v<n>)`

---

## Fin de session

Dérouler `/fin-de-tache` (**mode vague parallèle** : bilan et points N2 dans CE fichier, aucun
fichier partagé touché, ni commit ni push).

---

## Bilan de session (exécutée le 2026-08-02)

### T-119 — FAIT, avec une correction en cours de session (RÉGRESSION DÉTECTÉE ET CORRIGÉE)

**Première passe (abandonnée) — à documenter honnêtement.** J'ai d'abord posé une garde dans
`suggestionEsperanceVieSiApplicable` (`esperanceVieDefault.ts`) : ne suggérer que si les QUATRE
`ESPERANCE_VIE_DRIVERS` figurent dans un ensemble `renseignes` (= `touched`) transmis par l'appelant.
Mes tests (nœuds synthétiques SANS `presomption_non`) passaient tous. Cette garde s'est révélée **casser
le nœud réel `cible-glycemique`** : `antecedent_cv`/`comorbidite_grave` y portent `presomption_non: true`
— un `bool` présumé est DÉTERMINÉ sans jamais être `touched` (R7/D20), donc « Rien à signaler » ne les
ajoute jamais à `touched` (`CriteriaForm.tsx` `boolsAConfirmer` filtre sur `aConfirmer`, qui ne contient
jamais un critère `presomption_non`) — la garde ne pouvait donc **jamais** être satisfaite sur ce nœud,
ni au premier passage ni ensuite. Mesuré au navigateur par le coordinateur sur la vignette N2 (Âge 59 →
Ancienneté 7 → « Rien à signaler ») : le nœud le plus utilisé du produit ne rendait plus AUCUNE carte
(0 au lieu de 1, « calculé, à vérifier » et « Longue » disparus). **La faute était dans le cadrage du
brief** (« déterminé » = « touché » n'est vrai que pour un `bool` sans `presomption_non`), pas dans son
exécution mécanique — signalé par le coordinateur, corrigé dans cette même session.

**Fichiers modifiés (état final)** :

- `src/features/decision/lib/esperanceVieDefault.ts` — `suggestionEsperanceVieSiApplicable` **revenue à
  sa signature d'origine** (4 paramètres, pas de `renseignes`) ; la garde de complétude est retirée.
  Docstring réécrite : historique complet des deux régressions successives sur ce mécanisme, pour qu'une
  future passe ne réintroduise pas la même garde au même endroit.
- `src/features/decision/screens/DecisionNodeScreen.tsx` — `handleCriteriaChange`/`handleConfirmerChamps`
  **revenus à l'identique de l'état avant cette session** (`git diff` le confirme, zéro delta sur ces deux
  fonctions). `handleReprendreValeurs` **ne calcule plus AUCUNE suggestion** : c'était le seul appelant
  capable de perdre un driver (reprise de session), donc le seul qui pouvait faire changer une suggestion
  déjà affichée sans un mot — retirer CE chemin, plutôt que garder tous les appelants, règle le défaut
  à l'échelle où il vit vraiment.
- `src/features/decision/screens/DecisionNodeScreen.esperanceVie.test.tsx` — **restauré à l'identique**
  (`git checkout`) : la garde qui rendait ses fixtures instables est retirée, plus rien à y changer.
- `src/features/decision/screens/DecisionNodeScreen.reentree.test.tsx` — le couple `NOEUD_CIBLE_A`/
  `NOEUD_CIBLE_B` porte maintenant, en plus de l'absence de `partage: true` sur `antecedent_cv`/
  `comorbidite_grave`, **`presomption_non: true`** sur ces deux mêmes critères (miroir fidèle du nœud
  réel, ce qui manquait à la première passe) + une option qui rend `fragilite` décisive (pour que « Rien
  à signaler » ait un bouton à cliquer). Trois tests dans ce fichier verrouillent désormais le sujet :
  1. **describe « la reprise ne recalcule plus… »** (nœuds `NOEUD_ESP_A/B`, TOUS les drivers `partage`) :
     reprise complète → rien n'est calculé pour `esperance_vie` (fonctionnalité T-061×T-057 retirée,
     assumé) ; puis le praticien répond directement sur le nœud réouvert → la suggestion fonctionne
     normalement (le chemin direct n'est pas touché par le retrait).
  2. **describe « suggestion … sur un nœud fidèle au nœud réel »**, test 1 : constat n° 2 exact,
     simplifié (plus besoin de déterminer les 4 drivers, la garde est partie) → la cible ne change pas
     après l'aller-retour, le champ reste vide.
  3. **describe « suggestion … sur un nœud fidèle au nœud réel »**, test 2 (NOUVEAU, demandé par le
     coordinateur) : sur ce nœud à `presomption_non`, « Rien à signaler » déclenche bien la suggestion —
     **c'est le test qui aurait attrapé la régression mesurée au navigateur**, absent avant ce jour.

**Vérification obligatoire (script du coordinateur, navigateur réel, port 5175)** — rejoué APRÈS le
correctif : Âge 59 → Ancienneté 7 → « Rien à signaler » → « calculé, à vérifier » présent, « Longue »
sélectionnée, **1 carte** (« Cible ≤ 7 % »), aucune erreur console. Identique à la recette du 02/08
d'avant session.

**N0 — ma portée propre (tout vert, re-vérifié après correction)** :

- `npm run typecheck` → 0 erreur.
- `npm run build` → OK (`dist/` généré, seul avertissement = taille de chunk, préexistant, sans rapport).
- Tests ciblés : `DecisionNodeScreen.*.test.tsx` (`ctaCompte`, `esperanceVie`, `interaction`, `legende`,
  `reentree`) + `esperanceVieDefault.test.ts` → **54/54 verts**. `evaluateNode.cible-glycemique.test.ts`
  → **19/19 verts**. `DecisionNodeScreen.esperanceVie.test.tsx` seul confirmé **identique à l'original**
  (`git diff` vide après restauration).
- Vérification navigateur (vignette N2 réelle) → conforme, ci-dessus.

### T-120 — STOP, signalé (aucun fichier modifié)

**Étape 1 vérifiée, condition d'arrêt confirmée** : `content/decision/noeuds/diabete-type-2/
cible-glycemique.yaml` **ne déclare `ASCVD_etablie` nulle part** dans ses `criteres_entree` (ni
ailleurs dans le fichier — recherche exhaustive, zéro occurrence). `ASCVD_etablie` n'est déclaré, avec
`partage: true`, que sur `prescription.yaml` et `statine.yaml`.

Conséquence technique vérifiée en lisant `lib/formLayout.ts::appliquerPreremplissage` : une règle
`preremplissage.quand` s'évalue contre `criteria`/`derives` du nœud COURANT, qui ne portent QUE les
valeurs des critères que CE nœud déclare dans `criteres_entree` (`buildDefaultCriteria`,
`valeursReprises` itèrent toutes deux sur les `criteres_entree` du nœud courant). `ASCVD_etablie`
n'entrerait donc JAMAIS dans le dossier du nœud cible, avec ou sans mémoire de session — la règle
`{ quand: "ASCVD_etablie == true", valeur: "true" }` demandée par le brief ne s'évaluerait donc jamais
à `true` : elle serait du code mort dès l'écriture.

**C'est exactement la condition d'arrêt écrite au brief** (T-120, étape 1 : « Si le nœud cible ne
déclare pas `ASCVD_etablie` dans ses `criteres_entree`, une règle qui le lit ne s'évaluera jamais → dans
ce cas STOP et signale : l'encodage demandé suppose ce point, et il n'a pas été vérifié au cadrage »).
Aucune modification n'a donc été apportée à `cible-glycemique.yaml` (ni le critère `antecedent_cv`, ni
`meta.version`/`meta.changelog`).

**Ce que je NE fais PAS, et pourquoi** : ajouter `ASCVD_etablie` aux `criteres_entree` du nœud cible
réglerait le problème technique, mais c'est une décision de PÉRIMÈTRE/UX, pas un détail d'implémentation
— cela ferait apparaître un NOUVEAU champ (case à cocher) sur le formulaire du nœud « Fixer la cible
d'HbA1c », qui ne l'a jamais eu, uniquement pour porter une règle de confort de saisie. Une session de
codage ne tranche pas ce point seule (dans l'esprit du fil rouge du plan : « aucune session ne décide de
contenu clinique » — ici ce n'est pas du contenu clinique au sens strict, mais c'est un changement de
périmètre d'un nœud publié, qui mérite le même luxe de prudence).

### N0 — portée globale du dépôt (constat, PAS un verdict sur mon travail)

`npm test` (suite complète, lancée en foreground comme demandé, ~155-160 s, deux exécutions par
prudence) tourne dans l'arbre de travail PARTAGÉ avec S2, dont les fichiers sont visiblement en cours
d'édition (`git status` : `content/decision/noeuds/diabete-type-2/{insuline,prescription,statine}.yaml`
et leurs snapshots de caractérisation, tous modifiés — hors de mon périmètre, jamais touchés ici).

Résultat obtenu (deux runs identiques à quelques ms près) : **56 fichiers de test (4 en échec, 51 verts,
1 skip) · 966 tests (17 en échec, 938 verts, 11 skip)**. Les 17 échecs + le fichier
`evaluateNode.statine.test.ts` qui n'a collecté **aucun test** (`0 test`, probablement un YAML
temporairement invalide au moment précis de la lecture par le loader) sont **exclusivement** dans :
`evaluateNode.prescription.test.ts` (15), `evaluateNode.insuline.test.ts` (1),
`engine/banc/invariants.test.ts` (1, sur le nœud prescription), `evaluateNode.statine.test.ts` (0
collecté). **Aucun échec sur un fichier touchant `cible-glycemique`, `esperanceVieDefault.ts` ou
`DecisionNodeScreen.tsx`.** Le nombre total (966) est inférieur au seuil de base (982) précisément à
cause du fichier `statine` non collecté — pas d'une régression introduite ici.

Je ne peux donc PAS certifier le N0 global du plan (« ≥ 984 tests, tout vert ») depuis cette session :
ce n'est techniquement pas possible tant que S2 n'a pas terminé et que `.claude/wave.lock` n'est pas
levé (arbre de travail partagé, cf. bandeau de ce fichier). Ma propre portée (T-119, ci-dessus) est
vérifiée verte de façon isolée et ciblée. **La certification N0 globale relève de la consolidation de
fin de plan**, à relancer une fois la vague terminée.

### Points N2 à faire juger par le référent

1. **T-119 — fonctionnalité délibérément retirée, à faire valider en recette.** « Reprendre les valeurs
   de ce patient » ne pré-remplit plus JAMAIS `esperance_vie`, même quand TOUS ses drivers ont
   effectivement circulé par la mémoire de session (cas le plus favorable — vérifié par le test
   `NOEUD_ESP_A`/`NOEUD_ESP_B`, qui les déclare tous `partage: true`). C'est la fonctionnalité livrée par
   T-057×T-061 le 2026-07-30, reprise ici sur instruction explicite du coordinateur (« le champ reste
   vide et le praticien répond — c'est le comportement voulu »). Compromis assumé : sûreté (jamais de
   valeur qui change en silence) contre confort (un champ de moins pré-rempli à la réouverture). À
   confirmer en recette navigateur que cette perte de confort, sur UN champ précis, est bien acceptée —
   elle n'était pas dans le brief initial de cette session, elle vient de la correction en cours de route.
2. **T-120 — décision de périmètre à trancher, pas exécutée ici (STOP au sens du brief, reconfirmé par
   le coordinateur).** Deux voies possibles pour livrer la fonctionnalité demandée : (a) déclarer
   `ASCVD_etablie` dans les `criteres_entree` de `cible-glycemique.yaml` avec `partage: true` (encodage
   identique à `prescription`/`statine`, I19) — mais cela ajoute un champ visible au formulaire du nœud
   cible qui ne l'avait jamais eu ; (b) renoncer à la règle de pré-remplissage demandée par T-120 pour ce
   nœud. Aucune des deux n'a été appliquée : décision à prendre en amont d'une future session dédiée (le
   coordinateur a indiqué la trancher avec le référent).

### Ce qui reste ouvert

- T-120 entièrement à refaire (ou à abandonner) après arbitrage du point N2 §2 ci-dessus.
- La certification N0 globale du plan (tests + typecheck + build sur l'arbre consolidé, après fin de
  vague et suppression de `.claude/wave.lock`) — non retentée une 3ᵉ fois après le correctif : ma portée
  propre est vérifiée verte (tests ciblés + navigateur réel, ci-dessus), et le dernier relevé complet
  disponible (avant correctif, 966 tests, 17 échecs) montrait déjà que tous les échecs étaient confinés
  aux fichiers `{prescription,insuline,statine}` de S2 — aucun changement de ce côté depuis.
- Aucun fichier partagé (`STATUS.md`, `TASKS.md`, `plans/P12/index.md`, `VALIDATION.md`) n'a été touché,
  conformément à la consigne ; aucun commit, aucun push.

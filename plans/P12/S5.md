# P12 · S5 — Titres de groupe et motifs restés bruts   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 3 (parallèle : non)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-02 · Branche : —

## Lire (commun à la session)

- `content/decision/noeuds/diabete-type-2/prescription.yaml` — le bloc `familles` du nœud et les
  `famille:` de chaque option ; le bloc `motifs:` de chaque option.
- `src/features/decision/screens/DecisionNodeScreen.tsx` — l'endroit qui compose le titre de groupe
  affiché (« Agent à ajouter **— en choisir un** ») : le suffixe vient de l'écran, pas du contenu.
- `src/features/decision/engine/` — la fonction `describeReasons` (localiser par recherche) : c'est
  elle qui transforme une expression en phrase quand aucun `motifs` ne la couvre.
- `docs/decision/GRAMMAIRE-NOEUD.md` — R6 (argumentaire situationnel).
- Le rapport de session de **S2** (intitulés modifiés) et de **S4** (carte nouvelle) : les deux ont
  touché les fichiers de cette session.

## Hors périmètre

- Ne change aucune `condition`, `exclusions`, `prerequis`, `priorite`, `role`, `action`,
  `niveau_preuve`. **Cette session ne déplace aucune carte et n'en change aucune conclusion.**
- Ne crée aucune famille nouvelle sans nécessité démontrée (T-124 en discute une, sous condition).
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `index.md`, ni `VALIDATION.md`. Ni commit ni push.

---

## T-124 — Un titre de groupe ne doit pas contredire les cartes qu'il coiffe

### Objectif

Que « Agent à ajouter — en choisir un » cesse de coiffer des options qui, chez ce patient-là, sont
des remplaçants.

### Décision clé

Défaut relevé **sur 4 vignettes sur 15** de la recette du 2026-08-02 (N3, N4, N12, N15), déjà présent
le 30/07, atténué par P10 (création du groupe « Traitement à corriger ou remplacer ») mais non résolu :

> « Le groupe “Traitement à corriger ou remplacer” existe désormais séparément, ce qui atténue
> l'ambiguïté, mais le titre “ajouter” subsiste au-dessus d'options qui, chez ce patient, sont bien
> des remplaçants du sulfamide. »

Et son aggravation en N4 : le **sulfamide** figure dans « Agent à ajouter — en choisir un » chez un
coronarien, dans le même cadre visuel que l'iSGLT2 à preuve élevée. Le rang est correct (dernier,
sans badge `Recommandée`) ; **c'est le regroupement qui trompe**.

Le problème est que `famille` est une propriété **statique de l'option**, alors que le geste réel
(« ajouter » vs « remplacer ») dépend du **patient** — chaque carte porte d'ailleurs déjà un chip
`action` qui, lui, est juste.

Trois issues possibles, à trancher par l'exécutant **sur pièces** et à justifier en une ligne :

- **(a) Neutraliser le titre de groupe** : « Agent à ajouter » → « **Agent glycémique** » ou
  « **Le choix de l'agent** ». Le groupe cesse de prescrire un verbe que les cartes portent déjà.
  *La moins chère, et elle supprime la contradiction sans rien déplacer.*
- **(b) Retirer le suffixe « — en choisir un »** quand le groupe contient des cartes de verbes
  différents. Plus fin, mais l'écran doit alors connaître les `action` de ses cartes.
- **(c) Déplacer les options concernées** de famille selon le patient. **À écarter** : `famille` est
  statique, la rendre dynamique est un changement de moteur hors périmètre de ce plan.

**Recommandation du cadrage : (a).** Elle règle les 4 vignettes d'un mot, ne touche pas au moteur, et
laisse le chip d'action porter le verbe — ce que la recette juge déjà réussi (« le chip de verbe est
écrit en toutes lettres sur chaque carte, donc la couleur n'a rien à décoder »).

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/prescription.yaml` (le bloc `familles` +
`meta.version` + `meta.changelog`). Si l'option (b) est retenue :
`src/features/decision/screens/DecisionNodeScreen.tsx`.

### Étapes

1. Recense les options de la famille « Agent à ajouter » et, pour chacune, le `action` qu'elle porte.
   Confirme sur pièces le constat de la recette (des `remplacer` sous un titre « ajouter »).
2. Applique (a), ou justifie en une ligne le choix de (b).
3. Vérifie que le nouveau libellé reste juste sur les **autres** nœuds si la famille y existe aussi.
4. Bump version + changelog (D5). Régénère les snapshots, **relis le diff** : uniquement des libellés.

### Validation

- **N0 auto** : `npm test` → vert · `npm run typecheck` → 0 erreur · `npm run build` → OK · Ajv → OK.
- **N1 visuel auto** : sur un écran à trois verbes (profil N3 : metformine + sulfamide en cours,
  HbA1c à la cible, hypoglycémie) — le titre de groupe ne contredit plus aucune carte. → `/verif-visuelle`
- **N2 humain** : le groupe reste-t-il compréhensible sans le verbe ? À consigner dans `VALIDATION.md`.

### Si bloqué

Si retirer le verbe du titre rend le groupe indistinguable d'un autre groupe du même écran : **STOP**.
Un titre neutre qui ne dit plus rien est un recul, pas une correction.

### Message de commit (appliqué en fin de plan)

`fix(dt2): un titre de groupe ne prescrit plus un verbe que les cartes contredisent`

---

## T-125 — Les derniers « Proposé parce que » en expression brute

### Objectif

Supprimer les motifs qui se lisent encore comme du code, et notamment celui qui se termine par
« : non ».

### Décision clé

P10 a rendu lisible la grande majorité des motifs — la recette du 02/08 le salue longuement (« la
logique brute a disparu », « les noms de variables sont devenus des libellés cliniques »). **Il en
reste**, et le contraste au sein d'un même écran les rend plus voyants qu'avant :

> « Sulfamide déjà en cours **et HbA1c < 6,5 % (sur-contrôle) : non** » — relevé en N3 et N5.

Le « : non » final trahit la sérialisation d'une expression, pas une phrase. Un motif se lit comme ce
qu'un médecin dirait à voix haute.

**Deux causes possibles, à distinguer** avant de corriger :
- l'option n'a **pas de `motifs`** pour cette branche → `describeReasons` retombe sur la
  sérialisation. Correctif : écrire l'entrée `motifs` manquante ;
- l'option **a un `motifs`** mais dont la clé ne correspond pas exactement à l'expression évaluée
  → la clé n'est jamais trouvée. Correctif : aligner la clé, **au caractère près**.

La seconde cause est silencieuse et se reproduira : si c'est elle, **dis-le dans ton bilan** et
propose (sans l'écrire) un garde-fou de banc.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/*.yaml` (les blocs `motifs` concernés +
version/changelog des nœuds touchés).

### Étapes

1. Balaie les six nœuds à la recherche des motifs restés bruts. Le repère le plus sûr : un motif
   qui se termine par `: non` / `: oui`, qui contient `==`, `!=`, `>`, `<`, `AND`, `OR`, ou un nom de
   variable en `snake_case`. **Fais-en la liste exhaustive avant de corriger quoi que ce soit.**
2. Pour chacun, détermine laquelle des deux causes s'applique, et corrige en conséquence.
3. Rédige les phrases manquantes en français de consultation. Elles doivent dire **pourquoi cette
   carte est proposée à CE patient**, pas répéter la règle.
4. Bump version + changelog des nœuds touchés. Régénère les snapshots, relis les diffs.

### Validation

- **N0 auto** : `npm test` → vert · `npm run typecheck` → 0 erreur · `npm run build` → OK · Ajv → OK.
- **N1 visuel auto** : sur les profils N3 et N5, aucun « Proposé parce que » ne contient
  d'opérateur ni de `snake_case`. → `/verif-visuelle`
- **N2 humain** : **les phrases écrites sont-elles cliniquement justes ?** C'est du contenu affiché.
  À consigner dans `VALIDATION.md` avec la liste avant/après.

### Si bloqué

Si un motif brut correspond à une branche dont tu ne comprends pas la portée clinique : **STOP, ne
l'invente pas**. Un motif faux est pire qu'un motif illisible — il affirme.

### Message de commit (appliqué en fin de plan)

`fix(dt2): les derniers motifs sérialisés redeviennent des phrases`

---

## Fin de session

Dérouler `/fin-de-tache` (mode **solo**).

> ⚠ **Correction appliquée à l'exécution** : la consigne d'orchestration reçue pour cette session est
> explicitement « mode consolidation différée » — aucun fichier partagé touché (`STATUS.md`, `TASKS.md`,
> `index.md`, `VALIDATION.md`), ni commit ni push (`.claude/wave.lock` présent, un hook les aurait refusés
> de toute façon). Bilan et points N2 consignés ci-dessous, dans CE fichier ; tout sera reversé en une fois
> à la consolidation du plan (vague 6).

---

## Bilan de session — T-124, T-125 (2026-08-02)

> **Corrigé le 2026-08-02, sur revue de l'orchestrateur du plan P12** (2ᵉ passe, après la 1ʳᵉ version de
> ce bilan). T-124 a dû être repris : le nom retenu en 1ʳᵉ passe (« Agent glycémique ») était lui-même
> faux — détail complet dans « Correction post-revue » ci-dessous. T-125 (17 motifs, le balayage exhaustif,
> la vérification programmatique des snapshots) a été validé sans changement.

### Fichiers modifiés

- `content/decision/noeuds/diabete-type-2/prescription.yaml` — T-124 (bloc `familles` : 1 `libelle` + 7
  `famille:` d'options renommés deux fois — « Agent à ajouter » → « Agent glycémique » (1ʳᵉ passe) →
  **« Le choix de l'agent »** (2ᵉ passe, correction) — commentaires de prose adjacents mis à jour à
  chaque passe, commentaire de justification du bloc `familles` réécrit en 2ᵉ passe pour tracer les DEUX
  tentatives écartées) + T-125 (4 `motifs` ajoutés sur 3 options : « Envisager l'insuline »,
  « Remplacer le sulfamide », « Remplacer le glinide ») ; `meta.version` 0.50 → 0.52 (1ʳᵉ passe) →
  **0.53** (2ᵉ passe, correction) — trois nouvelles entrées de `meta.changelog` au total (T-125, T-124
  1ʳᵉ passe conservée comme trace historique, T-124 2ᵉ passe de correction).
- `content/decision/noeuds/diabete-type-2/insuline.yaml` — T-125 (9 `motifs` ajoutés sur 8 options :
  toutes les occurrences isolées de `cible_atteinte == false`, plus `mcg_disponible == false` sur
  « Envisager d'instaurer une mesure continue du glucose ») ; `meta.version` 0.41 → **0.42**, nouvelle
  entrée de `meta.changelog` en tête.
- `content/decision/noeuds/diabete-type-2/cible-glycemique.yaml` — T-125 (3 `motifs` ajoutés sur l'unique
  option qui en manquait, « Cible ~6,5 % (6,5–7 %) ») ; `meta.version` 2.11 → **2.12**, nouvelle entrée de
  `meta.changelog` en tête.
- `content/decision/noeuds/diabete-type-2/rhd-activite-physique.yaml` — T-125 (4 `motifs` ajoutés sur 4
  options, MANQUÉS par l'inventaire P10/S6 qui se croyait exhaustif — cf. « Point N2 » ci-dessous) ;
  `meta.version` 0.11 → **0.12**, nouvelle entrée de `meta.changelog` en tête.
- **Hors périmètre initial, nécessaire pour tenir N0 (même pratique que S2, T-121)** :
  `src/features/decision/engine/banc/invariants.test.ts`,
  `src/features/decision/engine/groupesExAequo.test.ts`, `src/features/decision/lib/optionBadges.test.ts`,
  `src/features/decision/lib/vueDecision.familleHissee.test.ts`,
  `src/features/decision/engine/evaluateNode.prescription.test.ts`,
  `src/features/decision/screens/DecisionNodeScreen.css` — six fichiers qui retrouvaient la famille par le
  **texte littéral** (`familles.find(f => f.libelle === '...')`, description de test, ou commentaire) —
  **touchés DEUX FOIS** (1ʳᵉ passe vers « Agent glycémique », 2ᵉ passe de correction vers « Le choix de
  l'agent »). La 2ᵉ passe a en plus dû corriger une régression de SYNTAXE introduite par un remplacement
  global trop mécanique en 1ʳᵉ moitié de cette correction : `'Le choix de l'agent'` (apostrophe non
  échappée dans une chaîne à guillemets simples) cassait la compilation TypeScript sur 8 lignes — corrigé
  en guillemets doubles pour les comparaisons exactes (`f.libelle === "Le choix de l'agent"`, même
  convention déjà utilisée ailleurs dans ces fichiers pour "À faire d'emblée — sécurité") et en apostrophe
  typographique (’) pour les mentions en prose à l'intérieur d'une description de test à guillemets
  simples (même convention déjà utilisée pour « d’options », « l’objectif » dans ces mêmes fichiers).
  Revérifié : `npm run typecheck` 0 erreur après correction. Aucune assertion de comportement changée dans
  aucune des deux passes ; le commentaire CSS est pure prose, sans effet sur un test.
- `src/features/decision/engine/banc/__snapshots__/caracterisation.prescription.txt` et
  `caracterisation-indetermine.prescription.txt` — régénérés **deux fois** (une par passe de renommage,
  `npx vitest run .../caracterisation.test.ts -u`). Diffs relus intégralement à chaque fois (script de
  vérification programmatique en scratchpad, pas seulement à l'œil — cf. « Comment le diff a été relu »
  ci-dessous, substitution étendue en 2ᵉ passe) : uniquement des libellés, aucune option/badge/rang/motif
  n'apparaît ni ne disparaît, dans aucune des deux régénérations.
- **Créé puis supprimé avant la fin de session (script de travail, jamais fait partie du diff final)** :
  `src/features/decision/engine/banc/_audit-motifs-brut.test.ts` — répliquait `humanizeAtomic`/
  `humanizeAndTerm`/`branches` de `lib/conditionText.ts` (non exportées) avec les VRAIES fonctions
  `labelForCritere`/`labelForEnumValue` importées, pour balayer les 6 nœuds et lister, de façon exhaustive
  et automatique plutôt qu'au jugé, toute branche `conditions`/`priorite[].quand` non couverte par un
  `motifs` et rendue « suspecte » (`: non`/`: oui` de fin de phrase, ou repli brut non reconnu). Utilisé
  pour T-125 étape 1, supprimé une fois la liste établie.

### T-124 — option retenue et pourquoi

**Option (a) retenue, telle que recommandée par le cadrage — mais le NOM a dû être corrigé en 2ᵉ passe.**
1ʳᵉ passe : « Agent à ajouter » → « Agent glycémique ». **Ce nom était faux** (cf. « Correction
post-revue » ci-dessous) : nom final, après correction dans la même session, **« Le choix de l'agent »**.

**Constat sur pièces (étape 1 du brief)** : les 7 options de la famille (« Introduire un iSGLT2 »,
« Introduire un AR GLP‑1 », « Introduire le tirzépatide », « Association iSGLT2 + AR GLP‑1 », « Envisager
l'insuline », « Gliptine — option glycémique orale de bas rang », « Sulfamide — option glycémique de bas
rang ») portent **toutes** `action: ajouter` — **aucune ne porte littéralement `action: remplacer`**. Ce
point diverge de la lettre de la Décision clé du brief (« des `remplacer` sous un titre `ajouter` »), donc
signalé explicitement plutôt que forcé à correspondre : le défaut n'est pas un chip de carte faux (chaque
chip `action` reste exact, vérifié), c'est le **titre de groupe** qui affirme un verbe que la famille ne
tient pas pour tout patient qui la lit. Concrètement (aggravation N4 du brief) : un patient déjà sous
sulfamide, chez qui « Remplacer le sulfamide » se déclenche dans la famille voisine « Traitement à corriger
ou remplacer », voit en même temps « Sulfamide — option glycémique de bas rang » (bas rang, sans badge) et
« Introduire un iSGLT2 » (preuve élevée, souvent badgé Recommandée) réunis sous « Agent **à ajouter**
— en choisir un » — un titre qui, lu seul, suggère un simple complément quand la vraie décision clinique
(switcher le sulfamide) se joue pour partie dans la famille voisine.

**Pourquoi (a) plutôt que (b)/(c)** : (a) est la moins chère et supprime la contradiction sans rien
déplacer, exactement l'argument du cadrage. (b) (masquer « — en choisir un » quand les verbes diffèrent)
aurait exigé que `DecisionNodeScreen.tsx` connaisse les `action` de ses cartes pour décider d'un affichage
— un couplage nouveau, hors périmètre moteur de cette session, et de toute façon non pertinent ici
puisqu'aucune carte de la famille ne porte un verbe différent des autres (le problème n'est pas un
mélange de verbes DANS la famille). (c) était écartée d'emblée par le cadrage (famille dynamique par
patient = changement de moteur).

**Étape 3 (autres nœuds)** : vérifié — la famille « Agent à ajouter » n'existe que sur `prescription.yaml`
(`insuline.yaml`/`statine.yaml` n'ont pas de famille de ce nom, grep sur les 6 nœuds).

**Étape 4** : snapshots régénérés, diffs relus (voir ci-dessous « Comment le diff a été relu »).

### Correction post-revue (2ᵉ passe, T-124 uniquement)

**Ce qui s'est passé.** La 1ʳᵉ passe a suivi la recommandation littérale du cadrage (option (a)) et choisi
« Agent glycémique » — un nom qui corrige bien le VERBE fautif (« ajouter »), mais **introduit un
MÉCANISME tout aussi faux**, cette fois démenti **sur la carte qu'il coiffe elle-même**. L'orchestrateur a
mesuré au navigateur, sur le profil Kervarec (coronarien, DFG 58) :

> « Agent glycémique — en choisir un »
> └ Introduire un iSGLT2 · Recommandée · Preuve élevée
> Proposé parce que : DFG < 60 ET Maladie cardiovasculaire athéromateuse établie ET Agent sans
> bénéfice dur déjà en cours

Trois raisons RÉNALE, CARDIOVASCULAIRE et de SUBSTITUTION affichées deux lignes sous un titre qui
revendique un mécanisme GLYCÉMIQUE — aucune des trois ne l'est. **Reproduit et confirmé de façon
indépendante** dans cette session, directement sur le moteur réel (`construireVueDecision`, contournant la
navigation Playwright peu fiable rencontrée en 1ʳᵉ tentative de reproduction navigateur — cf. « N1 »
ci-dessous), sur un profil équivalent (optimiser, gliptine en cours, ASCVD établie, DFG 58) :

```text
Famille « Le choix de l'agent » (exclusive: true) :
  - Introduire un iSGLT2 [badge=recommandee]
      proposé parce que : DFG (mL/min/1,73 m²) < 60 et Maladie cardiovasculaire athéromateuse établie et
      Agent sans bénéfice dur déjà en cours (sulfamide, gliptine ou glinide)
```

(sortie obtenue APRÈS correction — le nom affiché est déjà le bon ; la sortie avant correction différait
uniquement par le libellé de famille, « Agent glycémique » à la place de « Le choix de l'agent », le reste
étant identique).

**Aggravation croisée signalée, pas corrigée (hors périmètre de cette session — cf. Point N2 ci-dessous)** :
P12/S2, dans la même journée, a raccourci l'intitulé de cette même carte, « Introduire un iSGLT2
(protection cardio-rénale et/ou contrôle glycémique) » → « Introduire un iSGLT2 », retirant « protection
cardio-rénale » de sa face visible (vérifié présent ailleurs sur la carte à l'époque — `avantages`,
`apercu` — donc pas une perte de contenu au sens strict de S2, mais une perte de LISIBILITÉ à l'écran).
Combiné au mauvais nom de famille de la 1ʳᵉ passe de T-124, plus rien sur la face visible de l'écran
(ni le titre de groupe, ni le titre de la carte) n'expliquait alors pourquoi cette carte méritait « Preuve
élevée » chez ce patient précis — un défaut qu'aucune des deux sessions ne pouvait voir seule.

**Correctif appliqué** : renommage en **« Le choix de l'agent »** — ne revendique ni verbe ni mécanisme,
seulement ce que la famille EST structurellement (`exclusive: true`, un agent retenu parmi plusieurs). Le
suffixe « — en choisir un » (dérivé de `exclusive`, `DecisionNodeScreen.tsx`, inchangé) porte déjà cette
information ; un mot de plus dans le titre ne pouvait qu'ajouter une revendication qu'une carte finirait
par démentir. Le commentaire YAML du bloc `familles` a été intégralement réécrit pour tracer les DEUX
tentatives écartées (« Agent à ajouter » : verbe parfois faux, 4 vignettes de la recette ; « Agent
glycémique » : mécanisme faux, démenti par le motif de la carte iSGLT2 chez le coronarien) — délibérément,
pour qu'une passe future ne « re-précise » pas le titre et ne recasse pas la même chose une troisième fois.

**Ce qui n'a pas changé** (2ᵉ passe, structure) : toujours aucune option déplacée, aucune `condition`/
`action`/`priorite`/`exclusions`/`prerequis` touchée — uniquement le libellé de famille (bloc `familles` +
les 7 options qui le portent) et les commentaires de prose adjacents. `meta.version` 0.52 → **0.53**,
nouvelle entrée de `meta.changelog` en tête (l'entrée de la 1ʳᵉ passe est conservée en dessous, comme trace
historique — même convention que les corrections post-revue précédentes de ce plan, S2 et S4).

**N0 revérifié après la 2ᵉ passe** : `npm test` (suite complète) **986 passés, 11 skip, 0 échec** — les 2
snapshots impactés régénérés une seconde fois, vérifiés par le même script programmatique (substitution
étendue avec la 2ᵉ passe de renommage), 0 ligne inexpliquée sur les deux fichiers. `npm run typecheck` :
0 erreur (après correction de la régression de syntaxe décrite dans « Fichiers modifiés »). `npm run
build` : OK.

### T-125 — liste exhaustive des motifs bruts trouvés et ce que chacun est devenu

Balayage exhaustif des 6 nœuds (script d'audit temporaire, cf. « Fichiers modifiés »). Le repère
mécanique : toute branche `conditions`/`priorite[].quand` **non couverte par un `motifs`** ET dont le rendu
mécanique (répliqué exactement depuis `lib/conditionText.ts`) se termine par `: non`, `: oui`, ou retombe
sur un jeton brut non reconnu. **13 branches trouvées, sur 4 nœuds** (aucune sur `statine.yaml` ni
`rhd-alimentation.yaml`) :

| Nœud | Option | Branche brute | Rendu AVANT | Motif APRÈS |
| --- | --- | --- | --- | --- |
| `cible-glycemique` | Cible ~6,5 % (6,5–7 %) | `antecedent_cv == false` | « Antécédent cardiovasculaire : non » | « Aucun antécédent cardiovasculaire » |
| `cible-glycemique` | Cible ~6,5 % (6,5–7 %) | `comorbidite_grave == false` | « Comorbidité grave : non » | « Aucune comorbidité grave » |
| `cible-glycemique` | Cible ~6,5 % (6,5–7 %) | `fragilite == false` | « Fragilité : non » | « Patient non fragile » |
| `insuline` | Envisager d'instaurer une MCG | `mcg_disponible == false` | « MCG disponible : non » | « Pas de MCG en place » |
| `insuline` | Envisager d'instaurer une MCG | `cible_atteinte == false` | « HbA1c à la cible : non » | « Cible glycémique non atteinte » |
| `insuline` | Envisager un GLP-1 avant ou avec l'insuline | `cible_atteinte == false` | idem | idem |
| `insuline` | Initier une insuline basale | `cible_atteinte == false` | idem | idem |
| `insuline` | Ne pas sur-titrer la basale — intensifier autrement | `cible_atteinte == false` | idem | idem |
| `insuline` | Titrer la basale (augmenter la dose) | `cible_atteinte == false` | idem | idem |
| `insuline` | Ajouter un GLP-1 / une association fixe d'abord | `cible_atteinte == false` | idem | idem |
| `insuline` | Ajouter un bolus au repas principal | `cible_atteinte == false` | idem | idem |
| `insuline` | Optimiser la répartition du basal-bolus | `cible_atteinte == false` | idem | idem |
| `prescription` | Envisager l'insuline | `cible_atteinte == false` | idem | idem |
| `prescription` | **Remplacer le sulfamide** | `hba1c_sous_cible == false` | « Sulfamide déjà en cours **et** HbA1c < 6,5 % (sur-contrôle) **: non** » *(exemple cité par la recette, N3/N5)* | « Sulfamide déjà en cours et Pas de sur-contrôle glycémique » |
| `prescription` | Remplacer le glinide | `hypoglycemie_recente == false` | « Hypoglycémie récente : non » | « Pas d'hypoglycémie récente » |
| `prescription` | Remplacer le glinide | `hba1c_sous_cible == false` | « HbA1c < 6,5 % (sur-contrôle) : non » | « Pas de sur-contrôle glycémique » |
| `rhd-activite-physique` | Se lever et bouger… / S'appuyer sur un repère… / Transformer une pause… (3 options) | `rupture_sedentarite_habituelle == false` | « Interrompt habituellement les longues périodes assises : non » | « Aucune interruption habituelle des périodes assises » |
| `rhd-activite-physique` | Orienter vers une structure d'activité physique adaptée… | `offre_proximite_connue == false` | « Offre d'activité de proximité connue : non » | « Aucune offre de proximité déjà identifiée » |

(Le tableau ci-dessus compte 13 branches distinctes, portées par 17 lignes `motifs` au total car
`cible_atteinte == false` revient sur 8 options d'`insuline`/`prescription` et
`rupture_sedentarite_habituelle == false` sur 3 options de `rhd-activite-physique`.)

**Cause, sur les 17 lignes : exclusivement la 1ʳᵉ (« l'option n'a pas de `motifs` pour cette branche »).**
Aucune des deux causes n'est un mésappariement de clé (2ᵉ cause du brief) — et ce n'est pas un hasard :
**I24** (`engine/banc/invariants-contenu.test.ts`, « chaque clé de `motifs` désigne une branche réellement
présente ») fait déjà partie de la suite verte et refuse mécaniquement toute clé mal recopiée sur
l'ensemble du contenu existant, contenu de cette session inclus (vérifié : mes 17 nouvelles clés ont dû
passer I24 pour que N0 soit vert). **Il n'y a donc rien à signaler côté garde-fou de banc** — le brief
demandait de le dire si la 2ᵉ cause se présentait ; elle ne s'est pas présentée, et I24 est déjà le
garde-fou qui l'aurait empêchée de passer inaperçue.

**Formulation des phrases (étape 3)** : toutes en énoncé positif de l'absence du facteur plutôt qu'en
négation d'un libellé de case à cocher (« Aucune interruption… » plutôt que « Interrompt… : non »), à
l'identique du style déjà établi par P10/S3 dans ces mêmes fichiers. Pour `cible_atteinte`, le texte « Cible
glycémique non atteinte » n'est pas inventé : c'est la reprise exacte d'un fragment déjà écrit par P10/S3
sur une branche composée de `prescription.yaml` (`"DFG > 0 AND DFG < 30 AND cible_atteinte == false":
"Insuffisance rénale sévère, cible glycémique non atteinte"`), pour la MÊME variable.

**Aucun STOP** : les 17 branches relevaient toutes d'une portée clinique déjà claire depuis le contenu
adjacent de l'option (chaque option a été lue en entier — `avantages`/`inconvenients`/commentaires — avant
d'écrire son motif), aucune n'a demandé d'arbitrer une question non tranchée par ce contenu.

### N0 — résultat exact

Mesuré **après la 2ᵉ passe** (correction du nom de famille incluse) :

- `npm test` (suite complète, foreground) : **986 passés, 11 skip, 0 échec** (997 au total) — identique à
  la base annoncée avant cette session, sur les DEUX mesures finales (fin de 1ʳᵉ passe, puis fin de 2ᵉ
  passe après correction). Échecs transitoires en cours de session, tous résolus avant chaque mesure
  finale : les deux snapshots `caracterisation(-indetermine).prescription.txt` (attendus après chaque
  renommage, régénérés + relus deux fois), et une régression de syntaxe TypeScript introduite par la
  correction elle-même (apostrophe non échappée dans une chaîne à guillemets simples sur 8 lignes de test
  — cf. « Fichiers modifiés » — trouvée par `npm run typecheck`, pas par un utilisateur, corrigée avant de
  lancer `npm test`).
- `npm run typecheck` (`tsc -b --noEmit`, **jamais `npx tsc --noEmit`**) : 0 erreur (revérifié après la
  correction de syntaxe ci-dessus).
- `npm run build` : OK (même avertissement pré-existant sur la taille du chunk JS, sans rapport avec cette
  session ; revérifié après la 2ᵉ passe).
- Validation Ajv des nœuds touchés : OK (`content.test.ts`, inclus dans la suite complète).

### Comment le diff des snapshots a été relu (pas seulement à l'œil)

`git diff` sur ce dépôt inclut **tout le travail non commité de P12** (S1→S5 : `.claude/wave.lock`
bloque les commits depuis le début du plan, consolidation différée) — un `git diff` brut sur
`caracterisation.prescription.txt` affiche donc ~250 lignes changées, la plupart illisibles à l'œil
(signature compacte de 180 profils, lignes de plusieurs milliers de caractères). Plutôt que de les survoler,
un script Node (scratchpad, `verify_snapshot_diff.js`) a comparé, ligne à ligne, la version `HEAD` (avant
tout le plan) à la version régénérée : en appliquant SEULEMENT les substitutions connues à chaque ligne
`HEAD`, le résultat doit égaler exactement la ligne régénérée. **Verdict, sur les DEUX régénérations
(1ʳᵉ passe puis 2ᵉ passe de correction, substitutions cumulées à chaque fois — les 27 raccourcissements
d'intitulé du tableau avant/après du bilan S2, PUIS « Agent à ajouter » → « Agent glycémique », PUIS
« Agent glycémique » → « Le choix de l'agent ») : 249/249 lignes de `caracterisation.prescription.txt` et
185/185 de sa variante indéterminée sont entièrement expliquées par ces substitutions connues — zéro ligne
inexpliquée, dans les deux régénérations.** Aucune option n'apparaît/disparaît, aucun badge/rang n'a bougé ;
le seul contenu nouveau apporté par CETTE session, à chaque passe, est le renommage de famille.
(La présence de l'ancien intitulé long dans `HEAD` alors que le rapport de session S2 annonçait l'avoir
déjà régénéré s'explique par l'absence de commit intermédiaire : `HEAD` est le dernier commit RÉEL, donc
antérieur à tout P12 — pas une staleness introduite par cette session.)

### N1 — visuel (navigateur, Playwright en scratchpad — pas de navigateur in-app dans cet environnement)

Environnement sans `preview_start`/`read_page` (VSCode/terminal) : le brief de session autorisait
explicitement Playwright depuis le scratchpad (`http://localhost:5175`, dev server déjà lancé) comme
alternative pour CETTE session — utilisé, plutôt que le mode B générique de la skill `verif-visuelle`
(« ne rien vérifier soi-même »), sur la base de cette autorisation explicite.

**Profil N3-like (Traiter → Optimiser, Metformine + Sulfamide en cours, HbA1c 7 %, à l'objectif, DFG 75,
IMC 27, hypoglycémie récente), rejoué APRÈS la correction du nom de famille :**

- **Famille « Traitement à corriger ou remplacer — gestes cumulables »** rendue avec le bon suffixe
  (mécanisme dérivé de `exclusive`, commun à toutes les familles y compris « Le choix de l'agent »).
- Carte **« Remplacer le sulfamide »** (chip `Remplacer`, badge Recommandée) : pastille « Proposé parce
  que » lue directement dans le DOM (`.pastille-info__bulle`) → **« Sulfamide déjà en cours et Pas de
  sur-contrôle glycémique »** — le fix T-125 reste intact après la 2ᵉ passe de T-124 (fichiers distincts,
  non touchés par la correction de nom de famille).
- Aucune erreur console.
- **« Le choix de l'agent » non atteinte dans ce profil précis** (les dérivés `isglt2_indisponible`/
  `aglp1_indisponible` restent indéterminés avec ce jeu de champs, gardant le `prerequis` de « Introduire
  un iSGLT2 » — même limite que la 1ʳᵉ passe, cf. ci-dessous pour la vérification qui la contourne).

**Reproduction du profil Kervarec (coronarien, DFG 58) qui a motivé la correction** — la navigation
Playwright s'est montrée peu fiable sur ce profil précis (les cases cochées dans un groupe du formulaire
« à confirmer » ne persistaient pas de façon fiable à travers plusieurs navigations `suivant`, cause non
élucidée, budget de session non extensible sur ce point) ; **contourné en interrogeant directement le
moteur réel** (`construireVueDecision`, la même fonction que consomme `DecisionNodeScreen.tsx` — script
temporaire en `engine/banc/`, supprimé après lecture, même pratique que l'audit T-125). Sortie, sur un
profil équivalent (optimiser, gliptine en cours, ASCVD établie, DFG 58) — **reproduite dans « Correction
post-revue » ci-dessus** : la famille s'affiche bien sous **« Le choix de l'agent »**, et le motif de la
carte « Introduire un iSGLT2 » (rénal + cardiovasculaire + substitution, toujours aucune raison
glycémique) ne contredit plus ce titre, puisqu'il ne revendique plus aucun mécanisme.

Le rendu de ce nom de famille (celui après correction) est en outre couvert par 3 suites de tests vertes
sur le moteur réel : `groupesExAequo.test.ts` (2 vignettes, dont une reproduit exactement « iSGLT2 et AR
GLP-1 à égalité »), `optionBadges.test.ts`, `vueDecision.familleHissee.test.ts` — les trois vérifient
`familles.find(f => f.libelle === "Le choix de l'agent")` sur `construireVueDecision`/`groupesParFamille`,
et sont vertes après la correction (986 passés, 11 skip, 0 échec sur la suite complète).

### Points N2 — à faire juger par le référent

1. **T-124 — écart entre la Décision clé du brief et le constat sur pièces (point maintenu après
   correction).** Le brief affirmait trouver « des `remplacer` sous un titre `ajouter` » ; sur pièces, les
   7 options de la famille portent toutes `action: ajouter`, aucune `remplacer`. Le renommage reste la
   bonne réponse au symptôme décrit (la confusion vécue en recette N3/N4/N12/N15), mais sur un mécanisme un
   peu différent de celui écrit dans le cadrage : ce n'est pas UNE carte au chip faux dans le groupe, c'est
   la **coexistence à l'écran, dans deux familles voisines, d'un verdict « remplacer » et d'options
   « ajouter » sur un agent proche** — la famille neutre absorbe ce cas comme les autres. À confirmer que
   c'est bien l'esprit voulu.
2. **✅ RÉSOLU en 2ᵉ passe, ne figure plus comme point ouvert** — le nom « Agent glycémique » retenu en
   1ʳᵉ passe revendiquait un mécanisme faux, démenti par la carte « Introduire un iSGLT2 » chez un
   coronarien (motif rénal/cardiovasculaire, aucune raison glycémique). Signalé et tranché par
   l'orchestrateur dans la même session : renommé en « Le choix de l'agent », qui ne revendique ni verbe ni
   mécanisme — cf. « Correction post-revue » ci-dessus pour le détail complet et la reproduction sur le
   moteur réel.
3. **NOUVEAU (signalé, pas corrigé — hors périmètre de cette session, porté au référent par
   l'orchestrateur).** Effet de bord croisé P12/S2 × P12/S5 sur la carte « Introduire un iSGLT2 » : S2 a
   raccourci son intitulé, « Introduire un iSGLT2 (protection cardio-rénale et/ou contrôle glycémique) » →
   « Introduire un iSGLT2 », retirant « protection cardio-rénale » de sa face visible (le contenu clinique
   avait été vérifié présent ailleurs sur la carte au moment de la coupe — `avantages`, `apercu` — donc pas
   une perte de CONTENU au sens de S2, mais une perte de LISIBILITÉ IMMÉDIATE). Combiné au mauvais nom de
   famille de la 1ʳᵉ passe de T-124 (« Agent glycémique »), plus rien sur la face visible de l'écran —
   ni le titre de groupe, ni le titre de la carte — n'expliquait alors pourquoi cette carte méritait
   « Preuve élevée » chez un coronarien à DFG < 60. La correction de T-124 (2ᵉ passe) retire la couche
   ajoutée par le titre de famille, mais **ne rouvre pas** le raccourcissement de S2 (hors périmètre
   moteur/titre d'option de cette tâche) : le motif de la carte (« Proposé parce que ») continue de porter
   la raison rénale/cardiovasculaire — il faut ouvrir le chevron « Argumentaire complet » (ou lire la
   pastille « Proposé parce que ») pour la voir, elle n'est plus dans le TITRE de la carte elle-même. À
   trancher par le référent : le motif situationnel suffit-il, ou faut-il redonner à l'intitulé de la carte
   un fragment qui la distingue au premier coup d'œil (ex. réintroduire « protection cardio-rénale » ou un
   équivalent court) ?
4. **T-125 — 17 lignes de motifs rédigés, formulation à valider.** Les phrases retenues (« Aucun antécédent
   cardiovasculaire », « Pas de sur-contrôle glycémique », « Cible glycémique non atteinte », etc.) sont
   cliniquement exactes par construction (elles ne disent que ce que la branche DSL affirme, ni plus ni
   moins) mais restent un choix de FORMULATION — à confirmer qu'elles se lisent bien comme ce qu'un médecin
   dirait à voix haute, pas seulement comme une négation propre.
5. **T-125 — `cible_atteinte == false` répété 9 fois avec le même motif.** Choix délibéré (même fait
   clinique, même phrase, cf. précédent déjà établi par P10/S3 sur une branche composée) plutôt qu'une
   formulation différente par option — signalé pour confirmer que l'uniformité est préférable à une
   nuance par option ici (le motif ne fait que traduire « HbA1c au-dessus de l'objectif », qui justifie
   chaque option de la même façon, quel que soit le geste d'ajustement proposé à côté).
6. **T-125 — `rhd-activite-physique.yaml` : un inventaire antérieur (P10/S6) se croyait exhaustif et ne
   l'était pas.** Son changelog affirme « 21 branches déjà lisibles, 3 opaques corrigées » sur 24 branches
   recensées ; le balayage automatique de cette session en a trouvé 4 de plus, manquées à l'époque (mêmes
   deux booléens négatifs répétés sur 4 options). Signalé pour mémoire : un inventaire manuel reste faillible
   là où un script exhaustif (celui de cette session, supprimé) ne l'est pas — pourrait justifier de garder
   ce type de script comme outil de banc réutilisable plutôt que jetable, question hors périmètre de cette
   session (le brief ne demandait pas de l'écrire en dur dans le dépôt).

### Ce qui a été laissé en STOP

**Aucun.** Les deux tâches ont été menées à terme ; aucun motif brut trouvé n'a demandé d'arbitrer une
portée clinique non déjà tranchée par le contenu adjacent, et aucun titre de famille n'est devenu
indistinguable d'un autre après le renommage T-124 (dans sa forme finale, corrigée en 2ᵉ passe — cf.
« Correction post-revue »). Le seul point non corrigé dans cette session est explicitement **signalé**, pas
laissé en silence : l'effet de bord croisé S2×S5 sur la lisibilité de la carte « Introduire un iSGLT2 »
(Point N2 n° 3) — hors périmètre de cette tâche par instruction explicite de l'orchestrateur, porté par lui
au référent.

# P9 · S7 — « Risque hypoglycémique du schéma » : investigation et dérivation   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 3 (parallèle : oui — S9, zones disjointes)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Cette session a une issue **incertaine par construction** — elle peut se conclure par un
> correctif de contenu, ou par un constat d'investigation sans changement. Les deux sont un succès.
> Doute clinique → STOP, signale, rends la main.
> **Dépend de S3 (`prescription.yaml`) et S5 (`insuline.yaml`) — jamais en parallèle sur ces fichiers.**

- Date : 2026-07-30 · Branche : —

## Lire (commun à la session)

- `content/noeuds/diabete-type-2/prescription.yaml` — la déclaration du critère
  `risque_hypoglycemique_schema` (≈ l.272-278, `type: enum`, valeurs `faible`/`eleve`, `partage: true`)
  et ses trois usages en condition (≈ l.1190, 1652, 1713).
- `content/noeuds/diabete-type-2/insuline.yaml` — la même déclaration (≈ l.464) et son usage (≈ l.514,
  dans le `derive` de `terrain_cible_assouplie` ou du critère combiné équivalent — vérifie le nom exact).
- `content/noeuds/diabete-type-2/prescription.yaml` — le dérivé `remplacement_agent_sans_benefice`
  (≈ l.392-417) : c'est le patron le plus proche d'une dérivation automatique déjà existante dans ce
  nœud (il teste `traitements_en_cours contient sulfamide/gliptine/glinide`), mais **ce n'est pas le
  même critère** — ne le confonds pas avec ce que tu dois évaluer.
- `docs/decision/GRAMMAIRE-NOEUD.md` — R5 (un critère qu'on demande doit agir), la règle sur les
  dérivés, R9 (état ≠ intention si pertinent).
- `DECISIONS.md` — D20 (le moteur ne se prononce jamais sur ce qu'il ne sait pas), D23 (sourcer, jamais
  inventer), D5.

## Hors périmètre

- **Ne touche à aucun autre critère ou dérivé** que `risque_hypoglycemique_schema` et ce qui en dépend
  directement.
- **Ne décide pas seul du seuil clinique** si l'investigation révèle que « élevé » recouvre plus qu'une
  simple présence de traitement hypoglycémiant (ex. si l'âge ou la fonction rénale entrent aussi en jeu
  dans l'esprit du contenu existant) : voir « Si bloqué ».
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `plans/P9/index.md`. Ne lance jamais `git commit` ni
  `git push`.

---

## T-074 — Le risque hypoglycémique du schéma peut-il se déduire des traitements en cours ?

### Objectif

Répondre à la question posée par le référent : *« Je pense qu'on peut supprimer l'option risque
hypoglycémique du schéma. L'outil peut définir à partir de la liste des traitements s'il y a des
traitements hypoglycémiants ou non. D'ailleurs quel choix pilote-t-il ? Est-ce justifié ? »* — puis, si
la réponse le permet, remplacer le champ saisi par un dérivé automatique.

### Décision clé

**C'est une investigation avant d'être un correctif.** Le rapport de recette confirme que ce champ,
sans définition ni infobulle, fait basculer une carte au moment précis où le praticien clique « Élevé »
(« Désintensifier » apparaît) — un jugement non défini qui décide de la conduite. Trois issues sont
possibles, à départager par ce que tu trouves, pas par supposition :

1. **Dérivation propre possible** : chaque option qui lit `risque_hypoglycemique_schema == eleve` ne
   distingue en réalité qu'entre « un traitement hypoglycémiant (sulfamide, glinide, insuline) est en
   cours » et « aucun » — dans ce cas, remplace le champ saisi par un dérivé
   (`risque_hypoglycemique_deduit` ou nom équivalent, sur le patron de
   `remplacement_agent_sans_benefice`) qui teste `traitements_en_cours contient sulfamide OR … contient
   glinide OR … contient insuline`, retire le critère `risque_hypoglycemique_schema` de la saisie, et
   met à jour les trois/quatre options qui le consommaient pour lire le nouveau dérivé.
2. **Dérivation partielle** : certaines options qui le lisent ne s'appuient que sur la présence d'un
   traitement, d'autres semblent vouloir capter quelque chose de plus (ex. « Élevé » choisi même sans
   traitement hypoglycémiant, pour un patient âgé/fragile) — dans ce cas, **ne supprime pas le champ**,
   documente précisément dans le bilan quelles options seraient couvertes par un dérivé et lesquelles ne
   le seraient pas, et **STOP** (voir « Si bloqué ») : c'est un arbitrage clinique, pas un fait à déduire
   seul.
3. **Aucune dérivation propre** (le champ capture un jugement qui dépasse la simple liste des
   traitements) : documente pourquoi, laisse le champ tel quel, et **ajoute au moins une infobulle**
   (`aide` sur le critère) qui dise concrètement ce que « Faible » et « Élevé » signifient, pour que le
   praticien ne décroche plus sans définition — même sans suppression, ce gain reste utile.

### Lire / Modifier

**Modifier (selon l'issue)** : `content/noeuds/diabete-type-2/prescription.yaml`,
`content/noeuds/diabete-type-2/insuline.yaml`, `src/features/decision/lib/labels.ts` (libellé du
nouveau dérivé si issue 1, ou infobulle si issue 3).

### Étapes

1. Liste exhaustivement, dans les deux nœuds, chaque option/dérivé/alerte qui lit
   `risque_hypoglycemique_schema` (au moins 4 emplacements déjà repérés : `prescription.yaml`
   l.1190/1652/1713, `insuline.yaml` l.514 — vérifie qu'il n'y en a pas d'autres).
2. Pour chacun, détermine si sa condition réelle (au-delà de la présence du champ) correspond
   exactement à « un agent hypoglycémiant est en cours » ou à autre chose. Consigne ligne par ligne
   dans le bilan.
3. Selon l'issue (1, 2 ou 3 ci-dessus, décision clé) : applique le correctif correspondant, ou
   arrête-toi et documente.
4. Si issue 1 : bump la version des deux nœuds + changelog, explique le remplacement et pourquoi il est
   fidèle à l'usage constaté. Lance la suite complète, relis tout diff de snapshot ligne à ligne — les
   seules différences admissibles sont sur des profils dont le statut `risque_hypoglycemique_schema`
   déduit change de valeur par rapport à ce qui était saisi manuellement dans le banc (à vérifier
   qu'aucun ne diverge).

### Validation

- **N0 auto (bloque le commit)**, si un correctif est appliqué (issue 1 ou 3) : `npm test` (suite
  **complète**, en foreground) → tout vert · `npx tsc --noEmit` → 0 erreur · `npm run build` → OK.
- **N1 visuel auto** : `—` (S10).
- **N2 humain (jugement clinique — à consigner dans `VALIDATION.md`)** : si issue 1, le référent doit
  confirmer que la dérivation couvre bien tous les cas où il attendait « Élevé » — c'est le seul point
  qu'aucun test ne peut trancher.

### Si bloqué

- **Issue 2 est elle-même une conclusion valide de cette session, pas un échec** : documente
  précisément, ne force ni la suppression ni le statu quo, rends la main pour arbitrage.
- Si l'investigation elle-même est ambiguë (le contenu ne dit pas clairement ce que « Élevé » est censé
  capter) : **STOP**, signale ce que tu as trouvé et pourquoi tu ne peux pas trancher.

### Message de commit (appliqué en fin de plan)

`feat(content): risque hypoglycémique du schéma dérivé des traitements en cours, si l'investigation le permet (P9)`

### Statut

Suivi dans `plans/P9/index.md`.

---

## Fin de session

Dérouler `/fin-de-tache` (mode vague parallèle — S9 tourne sur `OptionCard.tsx`/`statine.yaml`, zones
disjointes des tiennes).

# P9 · S8 — Metformine : protocole de titration sourcé (mémo Ameli)   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 4 (parallèle : non — après S7, même fichier `prescription.yaml`)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Doute clinique ou source insuffisante → STOP, signale, rends la main.

- Date : 2026-07-30 · Branche : —

## Lire (commun à la session)

- `C:\Users\kovu\Downloads\memometformine-medecin.pdf` — **source primaire de cette session**, fournie
  par le référent le 2026-07-30 en réponse au constat du rapport de recette (« Aucune dose, aucune
  titration. "Instaurer ou poursuivre" n'est pas une prescription. »). Lis-le en entier avant toute
  écriture.
- `content/noeuds/diabete-type-2/prescription.yaml` — l'option « Metformine (socle du traitement) —
  instaurer ou poursuivre » (recherche son `intitule` exact par grep, ne suppose pas la ligne) : son
  `action`, ses `avantages`/`inconvenients` actuels, et le mécanisme `calculs`/`calculsEnAttente` déjà
  utilisé ailleurs dans ce nœud (ex. la dose de metformine existe déjà comme critère saisi — vérifie si
  un protocole de titration relève d'un texte structuré dans l'option, ou d'un ajout à l'argumentaire).
- `content/noeuds/diabete-type-2/prescription.argumentaire.md` — le registre déjà utilisé pour ce nœud
  (sourcé, niveau de preuve GRADE simplifié, distinction critère dur/substitution).
- `docs/decision/GRAMMAIRE-NOEUD.md` — la règle sur le sourcing et le registre attendu dans un
  argumentaire.
- `DECISIONS.md` — D23 (sourcer, jamais inventer ; jamais la revue qui cite, la source primaire), D5.

## Hors périmètre

- **N'ajoute aucun contenu qui ne vienne pas du PDF fourni** ou des sources déjà présentes dans le nœud.
  Si le PDF ne couvre pas un aspect que tu voudrais documenter (ex. conduite en cas d'intolérance
  sévère), ne le comble pas par une autre source non vérifiée dans cette session — signale le manque.
- **Ne touche à aucune autre option du nœud**, ni aux contre-indications de la metformine (zone de S3,
  déjà livrée), ni au dérivé qui régit l'allègement de la metformine
  (`metformine_deprescriptible`) — le protocole de titration s'ajoute à l'option socle, il ne change
  aucune condition existante.
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `plans/P9/index.md`. Ne lance jamais `git commit` ni
  `git push`.

---

## T-075 — La carte metformine porte enfin un protocole de titration, pas seulement « instaurer ou poursuivre »

### Objectif

Encoder, sourcé sur le mémo Ameli fourni, un protocole d'introduction progressive de la metformine visant
à maximiser la tolérance digestive — la lacune n°1 relevée sur la vignette la plus simple du rapport de
recette (« Ce n'est pas une prescription. Je sais faire, mais l'outil ne m'a pas économisé la seule chose
qui coûtait »).

### Décision clé

**Le protocole est une aide à la prescription, pas une contre-indication ni une condition d'applicabilité
nouvelle.** Il s'ajoute au texte de l'option existante (ou à un nouveau champ structuré si le mécanisme
`calculs` du nœud s'y prête pour une titration en paliers — à ton jugement une fois le PDF lu, mais si un
tel mécanisme structuré n'existe pas encore pour une **séquence** de paliers dans le temps, une simple
prose sourcée dans `avantages`/l'argumentaire est acceptable et suffisante pour cette session : ne crée
pas de nouveau type de champ pour ça, ce serait hors périmètre). L'important : la dose de départ, le
rythme de montée, la dose cible, et ce qu'il faut dire au patient (prise au repas, tolérance digestive)
doivent devenir lisibles **sans** ouvrir un dépli supplémentaire si possible — c'est directement ce que
la vignette N1 réclamait en 2 minutes de consultation.

### Lire / Modifier

**Modifier** : `content/noeuds/diabete-type-2/prescription.yaml`,
`content/noeuds/diabete-type-2/prescription.argumentaire.md`.

### Étapes

1. Lis `C:\Users\kovu\Downloads\memometformine-medecin.pdf` en entier. Extrais : la dose de départ
   recommandée, le rythme d'augmentation (paliers, intervalle en jours/semaines), la dose cible usuelle
   et la dose maximale, les conseils de prise (au cours ou en fin de repas), et toute conduite de
   tolérance digestive (répartition en 2-3 prises, palier plus lent si intolérance). Note la référence
   exacte du document (titre, émetteur — Ameli —, date si présente sur le PDF) pour le sourcing.
2. Ajoute cette information à l'option metformine, dans le registre déjà établi du nœud (comparer à la
   façon dont `prescription.yaml` source déjà d'autres options — RCP ANSM, HAS, etc.) : une source
   nommée, pas une paraphrase sans attribution. Vérifie qu'il n'y a pas de contradiction avec les
   contre-indications/suspensions déjà présentes sur cette option (DFG < 30, déshydratation, etc.) —
   le protocole de titration s'ajoute à ces garde-fous, il ne les remplace pas.
3. Si le nœud a déjà un mécanisme pour distinguer « le patient est déjà sous metformine » de
   « instauration nouvelle » (l'option s'appelle « instaurer ou poursuivre », donc les deux cas
   coexistent) : assure-toi que le protocole de titration s'adresse clairement au cas **instauration**
   et ne s'affiche pas comme une instruction à un patient déjà stabilisé.
4. Bump la version du nœud + entrée de `changelog` (D5) : la source ajoutée, le motif (rapport de
   recette N1), la date.
5. Lance la suite complète. Si des snapshots de caractérisation bougent, relis le diff ligne à ligne —
   ce changement est un ajout de texte pur, aucun profil ne devrait changer de carte recommandée.

### Validation

- **N0 auto (bloque le commit)** : `npm test` (suite **complète**, en foreground) → tout vert ·
  `npx tsc --noEmit` → 0 erreur · `npm run build` → OK.
- **N1 visuel auto** : `—` (S10 : ouvrir « Traiter », profil sans traitement en cours, vérifier que la
  carte metformine porte désormais une indication de dose/titration lisible).
- **N2 humain (jugement clinique — à consigner dans `VALIDATION.md`)** : le protocole reflète-t-il
  fidèlement la pratique courante, et sa présentation économise-t-elle vraiment le temps que le rapport
  de recette réclamait ?

### Si bloqué

- Si le PDF fourni ne contient pas de protocole de titration exploitable (ex. il ne couvre qu'un autre
  aspect de la metformine) : **STOP**, ne comble pas par une autre source non vérifiée dans cette
  session — signale ce que le document contient réellement et rends la main.
- Si un protocole en paliers temporels ne peut pas être rendu lisible sans un nouveau mécanisme de champ
  structuré : **STOP**, propose la prose sourcée comme repli (déjà autorisée par la décision clé) plutôt
  que d'inventer un mécanisme hors périmètre.

### Message de commit (appliqué en fin de plan)

`feat(content): protocole de titration de la metformine, sourcé (mémo Ameli), sur l'option socle (P9)`

### Statut

Suivi dans `plans/P9/index.md`.

---

## Fin de session

Dérouler `/fin-de-tache` (mode vague solo — cette session est seule dans sa vague, après S7 sur le même
fichier).

---

## Bilan de session

**Branche** : `worktree-agent-a66fad1cd5c4e1e48` · **Worktree** :
`C:\Users\kovu\SynologyDrive\Thibault\Projets\ebm-msp\.claude\worktrees\agent-a66fad1cd5c4e1e48` ·
**Commit** : `29e063d` (un seul, non poussé).

### Pré-requis worktree

Le worktree avait été créé depuis `9adbe04` (consolidation P8), **antérieur** au cadrage du plan P9 : ni
`plans/P9/` ni le champ `condition` sur `contre_indications` n'existaient. `git merge main --ff-only` a
avancé la branche jusqu'à `5fec791` (P9 vague 3 livrée) sans aucun commit propre préalable — fast-forward
propre, aucune perte, confirmé par `git status` (« nothing to commit ») avant le merge.

### Ce que le PDF contient exactement

Source lue en entier : `C:\Users\kovu\Downloads\memometformine-medecin.pdf` — mémo médecin de
**l'Assurance Maladie (ameli)**, titre « Prescription de metformine chez le patient diabétique de type 2 »,
4 pages. **Aucune date d'édition n'est visible sur le document** (les seules dates lisibles sont celles de
rapports cités en note de bas de page, 2018-2020, et des recommandations HAS/ADA/SFD citées, 2013-2019) —
signalé tel quel dans le sourcing, aucune date inventée.

Contenu utile à T-075 :
- **Place de la metformine** : 1re intention en monothérapie (sauf CI absolue), maintenue en association
  lors des intensifications.
- **Efficacité/sécurité vs placebo** (source HAS 2013 citée par le mémo) : HbA1c −0,97 % en moyenne,
  sécurité cardiovasculaire, neutralité pondérale, pas de risque d'hypoglycémie.
- **Effets indésirables** : troubles digestifs 10-20 % des patients, surtout en instauration, régressant
  spontanément ; acidose lactique = risque grave mais exceptionnel.
- **Table posologie/DFG** (dose journalière maximale) : ≥ 60 mL/min → 3 000 mg (8 cp Glucophage) ; 45-59 →
  2 000 mg (5 cp) ; 30-44 → 1 000 mg (2 cp) ; < 30 → contre-indiquée. **Identique** aux seuils déjà encodés
  dans ce nœud (T-070/S3) — vérifié, aucune divergence.
- **Cas d'arrêt momentané** : produit de contraste iodé, décompensation d'IC, chirurgie sous anesthésie
  générale/rachidienne/péridurale, affection aiguë à risque de déshydratation.
- **Contre-indications** : insuffisance rénale sévère (DFG < 30), respiratoire sévère, hépatique sévère.
- **Protocole de titration** (le cœur de T-075) : prescrire en 2-3 prises en milieu/fin de repas ;
  augmenter progressivement à partir d'une dose d'initiation, par palier d'**1 semaine à 15 jours** selon
  la tolérance digestive, jusqu'à la dose cible. **Exemple concret fourni** (cible 2 g/j, Glucophage
  1000 mg, sur 2 mois) : ½ cp matin + ½ cp soir × 15 j → ½ cp matin + 1 cp soir × 15 j → 1 cp matin +
  1 cp soir × 1 mois (dose cible) ; si nausées/diarrhées non supportées, retour au palier précédent
  15 jours avant de reprendre. Stagid® (embonate, moins de metformine base) utile pour instaurer.
  Répartition des prises selon préférence du patient en gardant la prise au repas ; comprimés sécables
  si trouble de déglutition.
- **Hors T-075, non exploité** : enjeu médico-économique (sous-utilisation FR, coût), outils
  d'accompagnement (appli « ameli.mémo », documents patients, service sophia) — hors périmètre de la
  tâche.

### Décisions de mise en forme

1. **Prose sourcée dans `avantages`, pas de nouveau champ `calculs`** (décision clé de S8.md confirmée à
   la lecture) : le mécanisme `calculs` de ce nœud (vu dans `insuline.yaml`) évalue une **formule unique**
   depuis les critères du patient (`poids * 0.1`, etc.) — il ne peut pas porter une **séquence temporelle**
   de 3 paliers de durée fixe, indépendante des critères saisis dans ce nœud. Aucun mécanisme structuré
   créé, conformément à l'instruction.
2. **Deux puces `avantages` ajoutées**, explicitement préfixées « En INSTAURATION » et « Observance » —
   pour respecter l'étape 3 (le protocole ne s'adresse pas au patient déjà stabilisé ; l'option couvre
   les deux cas et rien ne les distingue mécaniquement pour cette option précise).
3. **Réutilisation du champ `apercu`** (mécanisme générique livré par T-076/P9·S9 pour la carte statine,
   schéma et composant inchangés) pour rendre les chiffres clés (paliers 15 j, cible 2 g/j en 1 mois,
   2-3 prises au repas) lisibles **dans le titre du `<summary>` fermé, sans ouvrir le dépli** — exigence
   explicite de S8.md, directement issue du constat de la vignette N1 (« en 2 minutes de consultation »).
   Ce n'est pas la création d'un nouveau type de champ (le champ existait déjà, générique et destiné à un
   usage « au cas par cas ») : simple extension d'usage à une deuxième option, cohérente avec sa
   description de schéma.
4. **Vérification de non-contradiction** (étape 2) : la table DFG du mémo reproduit exactement les seuils
   déjà présents dans `contre_indications` (CI < 30) et dans l'option « Réduire la posologie de la
   metformine » (2 g/j si 45-59, 1 g/j si 30-44) — aucune valeur touchée, aucune contre-indication de la
   metformine modifiée, `metformine_deprescriptible` non touché, aucune autre option du nœud modifiée.
5. Version du nœud `0.40` → `0.41`, entrée de changelog ajoutée (source, motif, portée) conformément à
   D5.

### Fichiers modifiés

- `content/noeuds/diabete-type-2/prescription.yaml` — option « Metformine (socle du traitement) —
  instaurer ou poursuivre » : 2 puces `avantages` + champ `apercu` ; `meta.version` et `meta.changelog`.
- `content/noeuds/diabete-type-2/prescription.argumentaire.md` — nouvelle section « Ajout 2026-07-30
  (P9/S8, T-075) » dans le corps « En bref », avant « Points-clés » : détail complet du contenu du mémo,
  vérification de non-contradiction, justification du rendu (`avantages` + `apercu`, pas de `calculs`),
  et ce que le mémo ne couvre pas (cible unique 2 g/j dans l'exemple, pas de conduite au-delà de
  l'intolérance persistante — déjà couvert ailleurs dans ce nœud, non comblé par une autre source).

### Résultat N0

- `npm test` (suite complète, foreground) : **889 passed, 11 skipped, 0 failed** (48 fichiers passés,
  1 skippé). Aucun snapshot de caractérisation n'a bougé (les snapshots ne capturent que `intitule`,
  badge, `reasons`, `calculs` — ni `avantages` ni `apercu` n'y entrent), conforme à l'attente d'un ajout
  de texte pur.
- `npx tsc --noEmit` : 0 erreur.
- `npm run build` : OK (`tsc -b && vite build`, build en 318 ms ; seul avertissement pré-existant sur la
  taille du chunk JS, sans rapport avec ce lot).

### Cas « Si bloqué »

**Aucun rencontré.** Le PDF contenait un protocole de titration directement exploitable (dose de départ,
rythme par paliers, dose cible, exemple chiffré, conseils de prise) — ni le premier cas de blocage
(document sans protocole exploitable) ni le second (protocole nécessitant un nouveau mécanisme de champ
structuré, la prose sourcée dans `avantages` + l'`apercu` existant suffisant) ne se sont présentés.

### Reste à faire

Validation clinique référent (N2, à consigner dans `VALIDATION.md`, hors périmètre de cette session) :
fidélité du protocole à la pratique courante et gain de temps réel en consultation. N1 visuel (S10,
« Traiter » → profil sans traitement en cours → vérifier la lisibilité de la carte metformine) non
exécuté ici, conformément à la validation prévue par S8.md (« — », renvoyée à S10).

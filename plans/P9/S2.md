# P9 · S2 — Purge du jargon rendu, deux tooltips, ambiguïté « Agent à ajouter »   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 1 (parallèle : oui — S1, zones disjointes)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-30 · Branche : —

## Lire (commun à la session)

- `src/features/decision/lib/labels.ts` — l.170 (`palette_glycemique_ouverte`), l.171-172
  (`remplacement_agent_sans_benefice`), l.268-270 (libellés espérance de vie), l.388-399
  (`ENUM_VALUE_DESCRIPTIONS`, patron déjà en place pour d'autres critères — les profils AGP du nœud
  Insuline en sont l'exemple à imiter).
- `content/noeuds/diabete-type-2/prescription.yaml` — cinq zones précises, **pas le fichier entier** :
  l.700, 935, 961, 1218, 1747 (occurrences rendues de « nœud E », en intitulé ou en `inconvenients`) ;
  l.1946 (`reco_officielle.source`, contient « red-team ») ; l.712 et l.786 (les deux options « Agent à
  ajouter » dont le motif dit « Remplacement », via `remplacement_agent_sans_benefice`) ; l.498 et
  l.733/809/881/927/957/1388/1456 (déclaration et usages de la famille « Agent à ajouter », pour mesurer
  l'effet de bord de tout changement de libellé).
- `content/noeuds/diabete-type-2/cible-glycemique.yaml` — l.184-199 (`incertitudes`, contient « le
  référent » et « retirée du nœud », rendu par `ArgumentPanel.tsx`).
- `content/noeuds/diabete-type-2/rhd-activite-physique.yaml` — l.286, 346, 350, 362 (`inconvenients` de
  l'option « Pratique structurée », contiennent « voir la réserve majeure de ce nœud » et « voir le
  verrou de sécurité »).
- `content/noeuds/diabete-type-2/statine.yaml` — l.151-154 (critère `autres_FDRCV`, `type: nombre`,
  `min: 0`, `max: 6`, aucun `aide` renseigné aujourd'hui).
- `schema/noeud.schema.json` — l.249-253 (`critereEntree.aide`, déjà utilisé ailleurs, ex. TCA sur
  `rhd-alimentation` — cherche cet exemple pour le format attendu).
- `DECISIONS.md` — D5 (bump + changelog), D29/I20 (tout identifiant affiché a un libellé rédigé).

## Hors périmètre

- **Ne touche pas à `prescription.yaml:786`** (la condition brute « DFG > 0 et DFG < 30 » humanisée
  dans « Proposé parce que ») : ce n'est pas un texte éditable, c'est une expression DSL rendue par
  `conditionText.ts` — la corriger exige la refonte du motif par option, hors périmètre P9 (voir
  `plans/P9/index.md`, « Ce que ce plan NE fait pas »).
- **Ne renomme pas la famille « Agent à ajouter »** : 7 options y sont rattachées, la plupart sans
  ambiguïté — le correctif porte sur le libellé du dérivé `remplacement_agent_sans_benefice`
  (T-069, étape 4), pas sur le nom de la famille.
- **Ne touche à aucune `condition`, `exclusions`, ni logique de dérivation** — uniquement du texte
  (intitulé, inconvénients, incertitudes, libellés, `aide`). Si un changement de texte te semble exiger
  un changement de logique, tu as débordé : voir « Si bloqué ».
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `plans/P9/index.md`. Ne lance jamais `git commit` ni
  `git push`.

---

## T-069 — Le jargon de projet cesse de fuir sur l'écran clinique, deux champs de jugement gagnent une aide

### Objectif

Réécrire les passages **rendus à l'écran** (pas les commentaires YAML, jamais rendus) qui exposent du
vocabulaire de projet à un médecin ; ajouter une infobulle sur deux champs signalés sans définition ;
lever l'ambiguïté du libellé « Remplacement » affiché sous la famille « Agent à ajouter ».

### Décision clé

Chaque occurrence listée dans « Lire » a été vérifiée **rendue** (pas seulement présente en commentaire
`#`, qui ne s'affiche jamais et n'est pas concerné). Le principe de réécriture : dire au médecin ce que
la phrase signifie cliniquement, jamais comment ou quand elle a été écrite dans le projet. Une phrase
comme « AJOUTÉ le 2026-07-27, volet RÉNAL (collecte + red-team) » devient, par exemple, la substance
clinique qu'elle encadrait — sans date de session, sans jargon de méthode, sans nom de rôle interne
(« le référent »).

### Lire / Modifier

**Modifier** : `src/features/decision/lib/labels.ts`,
`content/noeuds/diabete-type-2/prescription.yaml`,
`content/noeuds/diabete-type-2/cible-glycemique.yaml`,
`content/noeuds/diabete-type-2/rhd-activite-physique.yaml`,
`content/noeuds/diabete-type-2/statine.yaml`.

### Étapes

1. **`prescription.yaml`** : aux 5 occurrences rendues de « nœud E » (l.700, 935, 961, 1218, 1747),
   remplace par une formulation clinique (ex. « ajustement de l'insulinothérapie » plutôt que
   « → nœud E ») — sans renvoyer à un autre écran par un nom de code. À l.1946
   (`reco_officielle.source`), retire « red-team » et toute mention de méthode de collecte ; garde
   uniquement la source et ce qu'elle établit.
2. **`cible-glycemique.yaml`** : réécris l'entrée `incertitudes` (l.184-199) qui contient « le
   référent » et « retirée du nœud » — dis ce qui est incertain et pourquoi, sans vocabulaire de
   session de travail.
3. **`rhd-activite-physique.yaml`** : aux 4 occurrences (l.286, 346, 350, 362), remplace « voir la
   réserve majeure de ce nœud » / « voir le verrou de sécurité » par la substance elle-même (la réserve
   ou le garde-fou en une phrase, directement lisible sans renvoi) — ou, si la substance est déjà dite
   ailleurs sur le même écran (alerte de nœud, autre `inconvenients`), une reformulation qui ne renvoie
   à rien d'invisible.
4. **`labels.ts` l.170** : renomme le libellé de `palette_glycemique_ouverte` en langage clinique (ex.
   « Place disponible pour un agent de contrôle glycémique supplémentaire » plutôt que « Palette
   glycémique ouverte ») — l'identifiant du critère ne change pas, seul son libellé rendu change (I20).
5. **`labels.ts` l.171-172** : reformule le libellé de `remplacement_agent_sans_benefice` pour qu'il
   reste vrai **et** cohérent avec la famille « Agent à ajouter » dans laquelle il apparaît deux fois
   (`prescription.yaml` l.712 et l.786) — le fait clinique est que ce dérivé ouvre l'accès à un agent de
   contrôle glycémique **soit en ajout net (aucun des trois agents sans bénéfice en cours), soit en
   remplacement (un de ces trois agents déjà en place)** : le libellé doit porter cette double
   possibilité plutôt que d'affirmer « Remplacement » sans condition. Une ligne suffit ; ne rouvre pas
   la logique du dérivé lui-même (elle ne change pas).
6. **`statine.yaml` l.151-154** : ajoute un `aide` sur `autres_FDRCV` listant les facteurs de risque
   cardiovasculaire usuellement comptés (ex. tabac, HTA, dyslipidémie autre que le LDL déjà saisi,
   antécédents familiaux précoces, sédentarité — vérifie dans l'argumentaire du nœud statine ou les
   sources déjà citées si une liste plus précise y figure déjà, pour rester cohérent avec ce que le
   nœud utilise réellement plutôt que d'inventer une liste générique).
7. **Espérance de vie (`labels.ts`, `ENUM_VALUE_DESCRIPTIONS`)** : avant d'écrire une infobulle pour
   `longue`/`intermediaire`/`limitee`, cherche si une définition (seuils d'années, ou description
   qualitative) existe déjà quelque part dans le contenu du nœud `cible-glycemique` (`incertitudes`,
   commentaires, argumentaire) ou dans les sources déjà citées. Si oui, reprends-la fidèlement en
   infobulle, au même patron que les entrées déjà présentes en l.388-399. **Si aucune définition
   n'existe nulle part dans le contenu déjà versé : n'invente pas de seuil en années — voir « Si
   bloqué »**, cette sous-tâche seule reste alors en attente, le reste de la session continue.
8. Lance la suite complète. Vérifie qu'aucun test de libellés (`libelles.test.ts`) ne casse ; s'il code
   un ancien libellé en dur, mets-le à jour.

### Validation

- **N0 auto (bloque le commit)** : `npm test` (suite **complète**, en foreground) → tout vert ·
  `npx tsc --noEmit` → 0 erreur · `npm run build` → OK.
- **N1 visuel auto** : `—` (S10).
- **N2 humain** : `—` (le jugement « ces reformulations restent-elles cliniquement exactes et lisibles
  en consultation ? » est reversé à S10/N2).

### Si bloqué

- Si aucune définition d'espérance de vie « Longue/Intermédiaire/Limitée » n'existe dans le contenu déjà
  versé (étape 7) : ne bloque pas toute la session pour ce seul point — saute cette infobulle
  précisément, documente dans le bilan qu'elle reste sans définition source, et continue les étapes
  1-6 et 8 normalement.
- Si reformuler une des occurrences de jargon (étapes 1-3) exige de supprimer une information clinique
  que rien d'autre sur l'écran ne porte (et pas seulement sa formulation) : **STOP** sur cette occurrence
  précise, signale-la, continue les autres.

### Message de commit (appliqué en fin de plan)

`fix(content): purge du jargon de projet affiché au clinicien, deux infobulles, libellé « Agent à ajouter » cohérent (P9)`

### Statut

Suivi dans `plans/P9/index.md`.

---

## Fin de session

Dérouler `/fin-de-tache` (mode vague parallèle — S1 tourne sur le moteur et les composants React,
aucun fichier commun).

---

## Bilan de session

- Date : 2026-07-30 · Exécutant : Claude Code (Sonnet), isolé dans un worktree dédié
  (`worktree-agent-a1b186dfc31fbfbff`, branché sur `9adbe04`, un commit avant `main`/`b12d476` qui a
  introduit `plans/P9/`).
- **Les 8 étapes de T-069 sont faites**, dans l'ordre, à une exception documentée près (étape 7,
  `intermediaire`, cf. « Si bloqué » ci-dessous — clause explicitement anticipée par S2.md, pas un
  blocage de session).

### Fichiers modifiés

- `src/features/decision/lib/labels.ts` — étapes 4, 5, 7.
- `content/noeuds/diabete-type-2/prescription.yaml` — étape 1 (+ version 0.38→0.39, changelog).
- `content/noeuds/diabete-type-2/cible-glycemique.yaml` — étape 2 (+ version 2.7→2.8, changelog).
- `content/noeuds/diabete-type-2/rhd-activite-physique.yaml` — étape 3 (+ version 0.9→0.10, changelog).
- `content/noeuds/diabete-type-2/statine.yaml` — étape 6 (+ version 1.17→1.18, changelog).
- `src/features/decision/engine/banc/__snapshots__/caracterisation.prescription.txt` et
  `caracterisation-indetermine.prescription.txt` — régénérés (`vitest -u`) : les 184 lignes touchées
  contiennent **toutes** l'ancien intitulé « Envisager l'insuline (… → nœud E) », vérifié par grep avant
  d'accepter le diff (184 lignes `-` / 184 lignes `+`, aucune ligne hors motif). Aucun autre snapshot ni
  test touché.
- Bump de version + entrée changelog sur les 4 nœuds modifiés (D5, CLAUDE.md invariant 4) : chaque
  résumé précise « rédactionnel seul, aucun changement de logique ni de condition » et liste ce qui a
  changé, marqué « en attente de relecture référent » (aucune validation clinique n'était requise pour
  du texte pur, mais la traçabilité D5 s'applique quand même à toute modif de nœud).

### Étapes 1-6 et 8 — décisions prises

- **Étape 1 (`prescription.yaml`)** : les 5 occurrences énumérées par S2.md (l.700, 935, 961, 1218,
  1747) remplacées par la substance clinique — le fil conducteur retenu est « le schéma insulinique est
  titré et suivi dans la durée », sans jamais nommer un autre écran par un nom de code. `reco_officielle.
  source` (l.1946) : retrait de « AJOUTÉ le 2026-07-27, volet RÉNAL (collecte + red-team...) », de
  « VÉRIFIÉ par le référent le 2026-07-27 » et de « (vérifié deux fois, dont la Figure 23) » — ce
  dernier n'était pas explicitement listé par l'énoncé de l'étape (qui ne citait que « red-team »), mais
  relève de la même famille (« toute mention de méthode de collecte/vérification ») et de l'exemple donné
  en « Décision clé » (qui montre justement cette phrase transformée). Toute la substance factuelle
  (seuils DFG, molécules, sources, ce qu'elles établissent) est restée intacte — rien de clinique
  retiré, uniquement le « comment/quand/qui » du projet.
  - **Trouvaille hors liste, corrigée par cohérence, à signaler** : en relisant le fichier après les 5
    occurrences listées, deux autres occurrences rendues de « nœud E » sont apparues, non énumérées par
    S2.md : `population_cible` (l.40, "Hors périmètre : ajustement fin des doses d'insuline (nœud E)")
    et une alerte de nœud (l.1718, "... ou l'ajustement de l'insuline (nœud E)."). Les deux sont
    rendues à l'écran (alerte de nœud = toujours affichée si sa condition est vraie ; `population_cible`
    a déjà été traité comme rendu par un chantier antérieur D29/I20 sur `statine.yaml`, cf. changelog
    statine 2026-07-28). Je les ai corrigées avec le même principe (substance clinique, pas de nom de
    code) plutôt que de laisser fuir un jargon identique à celui que la tâche vise explicitement à
    purger — mais **ce n'était pas dans le périmètre écrit de S2.md**, donc signalé ici explicitement
    pour revue plutôt que fait silencieusement. Si ce n'était pas souhaité, ces 2 lignes sont isolables
    (`git diff` sur `prescription.yaml`, chercher « déjà en place (titration, schéma) » et « schéma
    insulinique (titration, doses) »).
- **Étape 2 (`cible-glycemique.yaml`)** : `incertitudes` (l.184-199) réécrite. Point d'attention : le
  texte d'origine justifiait partiellement le choix conservateur par une association à
  `risque_hypoglycemie_schema == eleve` — un critère **retiré du nœud** (changelog 2026-07-29). Reprendre
  cette justification telle quelle aurait affirmé à l'écran un mécanisme qui n'existe plus dans la
  logique actuelle du nœud ; la reformulation ne mentionne donc plus ce critère et s'appuie uniquement
  sur ce qui reste vrai aujourd'hui (alignement avec la position HAS et avec le nœud statine, et
  l'absence d'essai testant l'ancienneté isolée). Aucune information clinique **actuelle** n'a été
  perdue ; une justification devenue obsolète par un changement antérieur (hors T-069) a été retirée
  avec le jargon.
- **Étape 3 (`rhd-activite-physique.yaml`)** : les 4 occurrences remplacées. Vérifié que la substance de
  « réserve majeure de ce nœud » (aucun ECR d'exercice isolé ne démontre de bénéfice CV dur dans le DT2)
  est déjà dite ailleurs sur le même écran, dans le champ `argumentaire` (rendu par `ArgumentPanel.tsx`,
  l.454-482) — la reformulation inline est donc une redite volontairement courte et non un ajout
  d'information nouvelle. Pour « voir le verrou de sécurité » (l.346), la liste des contre-indications à
  l'effort a été reprise mot pour mot de l'`argumentaire` du même nœud (limitation physique connue,
  symptômes d'ischémie à l'effort, rétinopathie non stabilisée/proliférante, neuropathie
  périphérique/mal perforant plantaire).
- **Étape 4/5 (`labels.ts`)** : `palette_glycemique_ouverte` → « Place disponible pour un agent de
  contrôle glycémique supplémentaire » (repris quasi mot pour mot de l'exemple donné par S2.md).
  `remplacement_agent_sans_benefice` → « Accès à un agent de contrôle glycémique, en ajout ou en
  remplacement d'un agent sans bénéfice sur critère dur (gliptine, sulfamide, glinide) » — porte
  explicitement la double possibilité (ajout net / remplacement) sans rouvrir la logique du dérivé
  (`prescription.yaml` l.712/786, non touchées).
- **Étape 6 (`statine.yaml`)** : `aide` ajouté sur `autres_FDRCV`. La liste retenue (« hypertension
  artérielle, tabac, dyslipidémie, antécédent familial cardiovasculaire précoce ») reprend **exactement**
  le commentaire déjà présent en tête de fichier (l.70-71 : « `autres_FDRCV` = compte des facteurs de
  risque CV additionnels (HTA, tabac, dyslipidémie, hérédité CV précoce…) »), plutôt que la liste
  d'exemple donnée par S2.md (qui incluait « sédentarité » et « hors LDL déjà saisi »). Écart assumé :
  la sédentarité n'est comptée nulle part dans ce nœud comme FDRCV, et ce nœud ne saisit aucun LDL (pas
  de critère `LDL` dans `criteres_entree` — vérifié), donc mentionner « hors LDL déjà saisi » aurait été
  faux pour ce nœud précis (il n'y a pas de LDL saisi ailleurs à exclure). J'ai suivi la consigne
  explicite de S2.md de préférer ce que le nœud utilise réellement à la liste d'exemple générique.
- **Étape 8** : `npm test` (suite complète) lancé deux fois — un premier passage a révélé 2 snapshots
  obsolètes (texte de l'option « Envisager l'insuline », changé à l'étape 1), régénérés via
  `npx vitest run <fichier> -u` puis vérifiés ligne à ligne (cf. « Fichiers modifiés » ci-dessus). Aucun
  `libelles.test.ts` (ou équivalent) ne codait en dur les anciens libellés `palette_glycemique_ouverte` /
  `remplacement_agent_sans_benefice` — pas de mise à jour de test nécessaire sur ce point. Deuxième
  passage : tout vert.

### Étape 7 — espérance de vie (partiellement faite, clause « Si bloqué » actionnée sur 1 valeur / 3)

- Définition trouvée pour **`longue`** : HAS 2024, dans `cible-glycemique.yaml`
  `reco_officielle.position` (l.175) — « EV>15 ans » associée au seuil le plus strict (≤6,5 %). Tooltip
  ajouté : « Espérance de vie estimée supérieure à 15 ans (HAS) — avec un diagnostic récent et l'absence
  de maladie cardiovasculaire, c'est la situation où la cible la plus stricte (≤ 6,5 %) est envisagée. »
- Définition trouvée pour **`limitee`** : synthèse critique sourcée Prescrire, même fichier,
  `synthese_critique.donnee` (l.164) — « l'espérance de vie < 5 ans » associée au relâchement de cible.
  Tooltip ajouté : « Espérance de vie estimée inférieure à 5 ans — un facteur d'assouplissement de la
  cible (jusqu'à 8-9 %) : le bénéfice d'un contrôle strict n'a pas le temps de se manifester, face au
  risque d'hypoglycémie. »
- **`intermediaire` : AUCUNE définition chiffrée ou qualitative trouvée** dans `cible-glycemique.yaml`
  (`incertitudes`, `argumentaire`, `synthese_critique`, `reco_officielle`) ni dans les sources déjà
  citées par ce nœud (ACCORD/ADVANCE/VADT/UKPDS/Boussageon/Cochrane, Prescrire, HAS 2024, ADA, ACP 2018).
  C'est la valeur « par défaut » implicite (cible ≤7 %) mais rien dans le contenu déjà versé ne la borne
  en années. **Clause « Si bloqué » actionnée précisément sur ce point** : pas de tooltip ajouté pour
  `intermediaire`, pas de seuil inventé. `ENUM_VALUE_DESCRIPTIONS` documente ce manque par un commentaire
  inline pour qu'un futur passage n'ait pas à refaire la recherche.
- Vérifié avant d'écrire : `longue`/`intermediaire`/`limitee` sont des valeurs génériques (dictionnaire
  indexé par la valeur seule, cf. avertissement l.333 de `labels.ts`), utilisées par le même critère
  `esperance_vie` (`partage: true`) sur 3 nœuds (`cible-glycemique`, `prescription`, `insuline`) — les
  deux tooltips ajoutés s'appliquent donc correctement partout où `esperance_vie` est demandé, aucun
  risque de sens contradictoire d'un nœud à l'autre.

### Validation N0

- `npm test` (suite complète, foreground) : **865 passed, 11 skipped, 0 failed** (48 fichiers de test,
  47 passed + 1 skipped) — vert au second passage, après régénération des 2 snapshots obsolètes.
- `npx tsc --noEmit` : **0 erreur**.
- `npm run build` : **OK** (`tsc -b && vite build`, build produit en 485 ms ; seul avertissement =
  taille de chunk >500 kB, préexistant, sans rapport avec cette session).

### Chevauchement avec S1

Aucun fichier commun observé. S1 travaille sur le schéma JSON, `engine/evaluateNode.ts` et les
composants React (`OptionCard.tsx`) ainsi que le contenu YAML hors `contre_indications`/`conditions` —
zones disjointes des 4 nœuds et de `labels.ts` touchés ici. Rien à signaler.

### Note sur le worktree / la branche

Consigne initiale de S2.md : ne jamais committer. **Corrigé en cours de session par l'orchestrateur** :
cette session tourne dans un worktree isolé (`C:\Users\kovu\SynologyDrive\Thibault\Projets\ebm-msp\
.claude\worktrees\agent-a1b186dfc31fbfbff`, branche `worktree-agent-a1b186dfc31fbfbff`), donc un commit
final est attendu, pas un arbre non commité. **Complication découverte en écrivant ce bilan** :
`plans/P9/S2.md` n'existait pas sur la branche du worktree (celle-ci pointait sur `9adbe04`, un commit
avant `b12d476` sur `main` qui a introduit tout `plans/P9/`). Je l'ai matérialisé dans le worktree via
`git show main:plans/P9/S2.md` (contenu identique vérifié) avant d'y ajouter ce bilan, pour que le
fichier existe sur la branche à committer et que la fusion avec `main` reste un simple ajout de suffixe
(pas de conflit add/add attendu, le préfixe étant identique caractère pour caractère). À la fusion,
l'orchestrateur voudra sans doute garder la version de `main` pour tout ce qui n'est pas la section
« Bilan de session » ajoutée ici.

# P9 · S1 — Contre-indications vérifiables : schéma + moteur + rendu   (rédigé par Opus)

> **Modèle : Opus · effort : high · Vague : 1 (parallèle : oui — S2, zones disjointes)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Design fixé au niveau comportemental — le détail visuel (classes CSS, formulation exacte) reste à ton
> jugement, dans le registre déjà établi par l'app. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-30 · Branche : —

## Lire (commun à la session)

- `schema/noeud.schema.json` — la définition d'`option` (≈ l.288-461), en particulier
  `contre_indications` (≈ l.379-385, tableau de chaînes, description : « prose destinée au lecteur,
  distincte de `exclusions`, évaluée par le moteur ») et `exclusions` (≈ l.416-422, tableau
  d'expressions DSL, **évaluées par le moteur**) : c'est ce second mécanisme qu'il faut imiter, pas
  réinventer.
- `src/features/decision/engine/evaluateNode.ts` — comment `exclusions` est aujourd'hui évalué pour
  calculer `EvaluateNodeResult.excluded`, et comment le DSL de conditions (`evaluateCondition`,
  probablement dans `engine/conditions.ts`) traite une expression indéterminée (D20 : ni vraie ni
  fausse tant qu'un critère qu'elle lit n'est pas renseigné — **le même sort doit s'appliquer ici**,
  pas de present/absent binaire).
- `src/features/decision/lib/vueDecision.ts` — comment une option est transformée en modèle de vue
  (`serialiseOption` et environs) : c'est ici que doit être calculé, pour chaque option, l'état de
  chacune de ses contre-indications.
- `src/features/decision/components/OptionCard.tsx` — le rendu actuel de `contre_indications`
  (≈ l.146-249) : `aDesContreIndications` pilote seulement le libellé du `<summary>` et l'ordre
  d'affichage, aucune condition n'intervient sur le texte lui-même aujourd'hui.
- Le traitement des options écartées (section « Options écartées (sécurité, R4) » dans
  `DecisionNodeScreen.tsx`/`vueDecision.ts`) : R4 (« écarté ≠ non-indiqué ») les affiche **avec leur
  motif**, jamais en silence. C'est le même principe de transparence à appliquer ici — une
  contre-indication vérifiée fausse pour ce patient ne doit pas disparaître sans explication, elle doit
  être visiblement **désamorcée**, pas effacée.
- `docs/decision/GRAMMAIRE-NOEUD.md` — R4, R8 (portée d'`exclusions`), et D20 (le moteur ne se prononce
  jamais sur ce qu'il ignore).
- `DECISIONS.md` — D5 (bump + changelog, pour les sessions suivantes qui consommeront ce mécanisme).

## Hors périmètre

- **N'encode aucune contre-indication concrète.** Cette session ne touche à **aucun** fichier YAML de
  `content/`. Le ré-encodage (ajout de `condition` sur des contre-indications réelles) est fait par
  S3-S6, qui dépendent de ce que tu livres ici.
- Ne touche pas au motif « Proposé parce que » (`conditionText.ts`) ni au panneau des options écartées :
  mécanisme différent, hors sujet.
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `plans/P9/index.md`. Ne lance jamais `git commit` ni
  `git push`.

---

## T-068 — Une contre-indication peut désormais se taire quand son critère l'exclut

### Objectif

Donner à `contre_indications` un mécanisme de garde optionnel, du même DSL que `exclusions`, pour
qu'une contre-indication vérifiable ne s'affiche plus comme un avertissement actif quand les critères
saisis montrent qu'elle ne s'applique pas à ce patient.

### Décision clé

**Le mécanisme existe déjà pour `exclusions` — ce n'est pas une nouvelle brique, c'est une extension
ciblée.** `exclusions` retire une option entière quand sa condition est vraie ; ici, la contre-indication
reste affichée (elle documente toujours un risque potentiel), mais son **état visuel** change selon que
sa condition (si elle en a une) est vérifiée vraie, vérifiée fausse, ou indéterminée :

| État de la condition | Ce que ça veut dire | Rendu |
| --- | --- | --- |
| Pas de `condition` déclarée | Contre-indication non vérifiable automatiquement (ex. « alcoolisme ») | **Inchangé** : toujours affichée, comme aujourd'hui |
| `condition` vraie | S'applique à ce patient | Affichée normalement, **c'est l'alerte active** |
| `condition` fausse | Le critère saisi l'exclut | **Désamorcée, pas effacée** : visuellement en retrait (ex. estompée/barrée), avec la raison affichée (« ne s'applique pas ici : … ») — même logique que R4 pour les options écartées |
| `condition` indéterminée (un critère qu'elle lit n'est pas renseigné) | Le moteur ne sait pas | Affichée normalement (état par défaut, D20 : jamais se prononcer sur ce qu'on ignore) |

**Rétrocompatibilité stricte** : toute contre-indication existante, dans tous les nœuds, n'a aujourd'hui
aucune `condition` — elle doit continuer à s'afficher exactement comme avant tant qu'une session de
ré-encodage (S3-S6) ne lui en ajoute pas une. Si ce n'est pas vrai après ton changement, tu as cassé
quelque chose.

### Lire / Modifier

**Modifier** : `schema/noeud.schema.json`, `src/features/decision/engine/evaluateNode.ts` (ou le module
pertinent selon ce que ta lecture révèle), `src/features/decision/lib/vueDecision.ts`,
`src/features/decision/components/OptionCard.tsx`, `src/features/decision/components/OptionCard.css`
(si nécessaire).

### Étapes

1. Étends le schéma : `contre_indications` accepte soit une chaîne (forme actuelle, rétrocompatible),
   soit un objet `{ texte: string, condition?: string }` (`condition` au DSL déjà utilisé par
   `exclusions`). Documente la distinction dans le schéma (description du champ), sur le modèle de la
   docstring déjà présente pour `exclusions`.
2. Dans le moteur, calcule pour chaque contre-indication de chaque option évaluée un état à trois
   valeurs (`active` / `levee` / `indetermine`), selon la table ci-dessus. Réutilise l'évaluateur de
   condition existant (`evaluateCondition` ou équivalent) — n'écris pas un second interpréteur DSL.
3. Propage cet état jusqu'au modèle de vue (`vueDecision.ts`) : chaque contre-indication rendue doit
   porter son texte **et** son état, pas seulement son texte comme aujourd'hui.
4. Adapte `OptionCard.tsx` pour rendre les trois états distinctement : `active`/`indetermine` comme
   aujourd'hui (aucun changement visuel pour elles) ; `levee` visuellement désamorcée avec sa raison
   affichée. Le compte `aDesContreIndications` du libellé de dépli (« ⚠ N contre-indication(s)… ») ne
   doit compter que les contre-indications **actives** — une contre-indication levée ne doit pas
   continuer à gonfler ce chiffre (sans quoi le défaut originel persiste sous une autre forme).
5. Test : une option avec une contre-indication à condition vraie, une à condition fausse, une à
   condition indéterminée et une sans condition, sur un même profil — vérifie que les quatre s'affichent
   dans l'état attendu, et que le compte du libellé de dépli ne compte que la première.
6. Test de non-régression : sur les contre-indications existantes (toutes sans `condition` aujourd'hui),
   rendu strictement identique à avant ton changement.

### Validation

- **N0 auto (bloque le commit)** : `npm test` (suite **complète**, en foreground) → tout vert ·
  `npx tsc --noEmit` → 0 erreur · `npm run build` → OK (valide le schéma JSON, y compris contre le
  contenu existant — s'il rejette une `contre_indication` en forme chaîne simple, le schéma n'est pas
  rétrocompatible).
- **N1 visuel auto** : `—` (S10).
- **N2 humain** : `—` (le jugement « la contre-indication levée est-elle assez visible pour rester
  vérifiable, mais assez discrète pour ne plus alerter à tort ? » est reversé à S10/N2).

### Si bloqué

- Si représenter l'état `levee` exige de modifier le DSL de conditions lui-même (`conditions.ts`) au-delà
  d'une simple réutilisation de l'évaluateur existant : **STOP**, signale ce que tu as trouvé — ce
  serait un chantier plus large que celui-ci.
- Si le passage à la forme objet de `contre_indications` casse un mécanisme non listé ici (ex. un export,
  une autre vue) : **STOP** et signale, ne le corrige pas à l'aveugle sans comprendre pourquoi il existe.

### Message de commit (appliqué en fin de plan)

`feat(engine): les contre-indications vérifiables se désamorcent quand leur critère les exclut (P9)`

### Statut

Suivi dans `plans/P9/index.md`.

---

## Fin de session

Dérouler `/fin-de-tache` (mode vague parallèle — S2 tourne sur des fichiers disjoints : contenu YAML et
`labels.ts`, jamais le moteur ni les composants React).

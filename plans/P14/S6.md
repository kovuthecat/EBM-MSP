# P14 · S6 — `prescription` : trois issues pour le sulfamide et le glinide   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 2 (parallèle : oui — avec S4 et S7, fichiers disjoints)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.
>
> ⚠ **Deux tâches `high` dans une même session, à dessein** : elles touchent le même fichier (392 Ko) et
> la seconde dépend de la première. Les séparer créerait la chaîne de sessions dépendantes sur un même
> fichier que la leçon P13 identifie comme coûteuse. Prendre le temps : T-169 **entièrement** finie et
> N0 vert avant d'ouvrir T-185.

- Date : 2026-08-06 · Branche : —

## Lire (commun à la session)

- `content/decision/noeuds/diabete-type-2/prescription.yaml` — **seulement** : le bloc `familles`, les
  critères dérivés `hba1c_sous_cible`, `isglt2_indisponible`, `aglp1_indisponible`,
  `remplacement_agent_sans_benefice`, et les **sept** options concernées : « Sulfamide » (remplacer),
  « Glinide » (remplacer), « Sulfamide » (réduire), « Glinide » (réduire), « Désintensifier : alléger la
  charge thérapeutique », « Insuline » (réduire), « Sulfamide (DFG < 30) » (arrêter — **modèle** de
  rédaction pour les nouvelles cartes d'arrêt).
- `docs/decision/GRAMMAIRE-NOEUD.md` — **R3** (« Modifier un traitement existant = DEUX décisions »),
  **R4** (écarté vs non indiqué), **R6** (l'argumentaire est situationnel).
- `schema/decision/noeud.schema.json` — descriptions de `bas_rang`, `action`, `action_si`, `references`.
- `src/features/decision/engine/banc/invariants-contenu.test.ts` — **I8** (adossement bibliographique) :
  il contraint le re-sourçage de T-185.

## Hors périmètre

- Ne pas toucher aux cartes de la famille « Le choix de l'agent » ni au socle metformine.
- Ne pas modifier les prérequis des options `default` : c'est S8/T-177.
- Ne **pas** renommer les intitulés dupliqués **hors** sulfamide/glinide (metformine, iSGLT2, AR GLP-1,
  tirzépatide, gliptine) : c'est S8/T-171.
- **Ne pas créer de critère** modélisant un refus de changement de traitement : arbitré le 2026-08-06,
  aucun critère. La carte « réduire » est simplement disponible.

---

## T-169 — Ouvrir les déclencheurs de « réduire » et de « remplacer »

### Objectif

Rendre disponibles les conduites que le référent a nommées et que le contenu ne portait pas.

### Décision clé

**Décision référent du 2026-08-06.** « Réduire » est une option de **bas rang qui doit exister** — elle
donne une conduite quand le patient **ne veut pas changer** de traitement, quand il est **déjà sous
iSGLT2 et AR GLP-1** (plus d'agent à bénéfice dur à ajouter), et quand l'**HbA1c est sous la cible**.

Trois écarts mesurés entre cette règle et le contenu actuel :

| Attendu | Aujourd'hui |
| --- | --- |
| « réduire » disponible si déjà sous les deux classes | ne se déclenche que sur intolérance / hypo récente / sous-objectif → **cas non couvert** |
| « remplacer » disponible aussi quand l'HbA1c est sous la cible | fermé par `hba1c_sous_cible == false` dans ses `conditions` |
| deux cartes distinguables à l'écran | **titres identiques** (« Sulfamide », « Sulfamide ») |

Point de forme déjà réglé, à ne pas re-traiter : sur l'écran, « Traitement à corriger ou remplacer »
(3ᵉ famille) s'affiche **au-dessus** de « Traitement à alléger » (5ᵉ). L'ordre de lecture est bon, le
rang n'y est pour rien.

### Lire / Modifier

- Modifier : `content/decision/noeuds/diabete-type-2/prescription.yaml`
- Modifier : `src/features/decision/engine/evaluateNode.prescription.test.ts`
- Régénérer : `__snapshots__/caracterisation*.prescription.txt`, `__snapshots__/paires.prescription.txt`

### Étapes

1. **Retitrer les quatre cartes** pour que l'action figure dans le titre, comme le nœud le fait déjà
   pour « Metformine (DFG < 30) » : `Sulfamide — remplacer par un agent à bénéfice démontré`,
   `Sulfamide — réduire la posologie`, et les deux équivalents glinide.
2. **Élargir le déclencheur de « réduire »** (sulfamide et glinide) : ajouter au second item de
   `conditions` le terme `isglt2_indisponible == true AND aglp1_indisponible == true`. Vérifier que ces
   deux dérivés valent bien `true` quand le patient **prend déjà** la classe (leur `derive` contient
   `intention != initier AND traitements_en_cours contient …`) — c'est ce qui couvre le cas nommé par le
   référent, **sans nouveau critère**.
3. **Ouvrir « remplacer »** au cas « HbA1c sous la cible » : retirer l'item `hba1c_sous_cible == false`
   de ses `conditions` (sulfamide **et** glinide ; sur le glinide, conserver
   `hypoglycemie_recente == false`, qui répond à autre chose). Relire ensuite son argumentaire : s'il
   présuppose une HbA1c au-dessus de la cible, le corriger — R6, l'argumentaire est situationnel.
4. **Poser `bas_rang: true`** sur les deux cartes « réduire » : ce sont des conduites faute de mieux, et
   le champ existe exactement pour ça (étiquette + atténuation visuelle, aucun effet sur la sélection,
   le tri ni le badge).
5. Mettre à jour `changelog` et `version`.

### Validation

- **N0 auto (bloque le passage à T-185)** :
  - `npx vitest run src/features/decision/engine/evaluateNode.prescription.test.ts` → vert.
  - `npm run build` → vert.
- **Vignettes à ajouter** (au moins trois) : (a) patient sous metformine + iSGLT2 + AR GLP-1 +
  sulfamide, HbA1c au-dessus, **sans** intolérance ni hypo → « réduire » **applicable** (elle ne l'est
  pas aujourd'hui) ; (b) patient sous sulfamide, HbA1c **sous** la cible → « remplacer » applicable ;
  (c) patient sous sulfamide, HbA1c au-dessus, intolérance → les deux cartes présentes, **titres
  distincts**.
- **N1 visuel auto** : —
- **N2 humain** : l'argumentaire révisé de « remplacer » → `VALIDATION.md`.

### Si bloqué

Si ouvrir « remplacer » au cas `hba1c_sous_cible` la rend applicable dans une situation où elle
contredit une carte de sécurité : STOP, signale le couple plutôt que d'ajouter un garde.

### Message de commit (appliqué en fin de plan)

`fix(content): réduire et remplacer couvrent les conduites nommées par le référent ; titres désambiguïsés (P14/S6)`

---

## T-185 — Redécouper « Traitement à alléger » : une carte d'arrêt par médicament

### Objectif

Faire exister la 3ᵉ issue — **arrêter** — sous un titre qui nomme le médicament, et rendre à
« Désintensifier » un périmètre qui ne fait plus doublon.

### Décision clé

**Décision référent du 2026-08-06 : « les deux, avec périmètres redécoupés ».**

État actuel, mesuré : la carte « Désintensifier : alléger la charge thérapeutique » occupe le **rang 1**
de la famille et se déclenche sur
`hba1c_sous_cible | (hypo & terrain_cible_assouplie) | (hypo & risque_schema == eleve) | position == sous_objectif`
**ET** `intention != initier AND traitements_en_cours contient (sulfamide | glinide | insuline_basale |
insuline_rapide)`. Elle porte donc **déjà** l'issue « arrêter », mais sans nommer le médicament et sans
badge d'action (`action` absente). Ses références couvrent **sulfamides ET insulines** (Grant 2025,
ADA 13.14a-c, HAS R.103, SFD Avis 5 bis) — c'est ce qui rend le re-sourçage nécessaire.

Après redécoupage, la famille « Traitement à alléger » doit se lire ainsi :

```
Sulfamide — arrêter               [nouvelle]
Sulfamide — réduire la posologie  [bas rang]
Glinide — arrêter                 [nouvelle]
Glinide — réduire la posologie    [bas rang]
Insuline — réduire
Désintensifier : alléger la charge  → restreinte aux INSULINES
Metformine (désintensification)
Reconsidérer un agent protecteur hors indication
```

### Lire / Modifier

- Modifier : `content/decision/noeuds/diabete-type-2/prescription.yaml`
- Modifier : `src/features/decision/engine/evaluateNode.prescription.test.ts`
- Régénérer : `__snapshots__/caracterisation*.prescription.txt`, `__snapshots__/paires.prescription.txt`

### Étapes

1. **Créer « Sulfamide — arrêter » et « Glinide — arrêter »**, famille « Traitement à alléger »,
   `action: arreter`, rang 1 (le même que « Désintensifier » occupait pour ces médicaments).
   Déclencheur : le traitement est en place (`intention != initier AND traitements_en_cours contient …`)
   **ET** la situation qui justifie l'arrêt
   (`hba1c_sous_cible == true OR position_vs_cible == sous_objectif OR hypoglycemie_recente == true AND terrain_cible_assouplie == true OR hypoglycemie_recente == true AND risque_hypoglycemie_schema == eleve`)
   — c'est-à-dire **exactement** le premier item de « Désintensifier », transposé au médicament.
2. **Sourcer** ces deux cartes en **reprenant** les `references` de « Désintensifier » qui portent
   réellement sur les insulinosécréteurs (I8 : une option en preuve modérée ou élevée doit dire d'où
   elle la tient). **Ne rien inventer** : si une référence ne porte que sur l'insuline, elle ne part pas
   sur la carte sulfamide. Si le sourçage n'est pas suffisant pour `modere`, écrire `faible` et le dire.
3. **Reprendre le schéma d'arrêt déjà rédigé** : la carte « Sulfamide — réduire » porte dans son
   `apercu` la règle « à l'introduction d'un AR GLP-1 : arrêt si HbA1c ≤ 7,5 % · −50 % si 7,6-8,5 % ·
   maintien si > 8,5 % (avis d'experts, JAMA Cardiol 2020) ». La part **arrêt** appartient désormais à
   la carte d'arrêt ; la part **réduction** reste sur la carte de réduction. Répartir, ne pas dupliquer.
4. **Restreindre « Désintensifier : alléger la charge thérapeutique » aux insulines** : son second item
   devient `intention != initier AND traitements_en_cours contient insuline_basale OR intention != initier AND traitements_en_cours contient insuline_rapide`.
   Relire son argumentaire et **retirer ce qui ne porte que sur les insulinosécréteurs** — sinon la carte
   affirme, sur un patient sous insuline seule, des faits qui concernent le sulfamide (R6).
5. **Re-sourcer « Désintensifier »** : ses `references` doivent, après restriction, porter sur la
   désintensification de l'**insuline**. Toute référence qui ne portait que sur les sulfamides est
   déplacée vers les nouvelles cartes, pas laissée en place.
6. **Vérifier la famille dans son ensemble** : plus aucune paire de cartes de « Traitement à alléger »
   ne dit la même chose ; la carte « Insuline — réduire » (rang 1) et « Désintensifier » (rang 1)
   coexistent-elles à dessein ? Si elles font doublon après restriction → **STOP** et signale, ne les
   fusionne pas de ta propre initiative.
7. Mettre à jour `changelog` et `version`.

### Validation

- **N0 auto (bloque le commit)** :
  - `npx vitest run src/features/decision/engine/evaluateNode.prescription.test.ts` → vert.
  - `npx vitest run src/features/decision/engine/banc/` → **I8 vert** (aucune référence orpheline,
    aucune option `modere`/`eleve` sans référence).
  - `npm run build` → vert.
- **Vignettes à ajouter** : (a) patient sous sulfamide, HbA1c sous la cible → les **trois** issues
  visibles (arrêter · réduire · remplacer), sur deux familles ; (b) patient sous **insuline seule**,
  HbA1c sous la cible → « Désintensifier » présente, aucune carte sulfamide/glinide ; (c) patient sous
  sulfamide **sans** insuline, HbA1c sous la cible → « Désintensifier » **absente** (c'est le
  redécoupage), « Sulfamide — arrêter » présente.
- **Contrôle des paires** : `paires.prescription.txt` diffère franchement (~210 paires au départ).
  **Le relire section par section**, en particulier « paires intra-famille » : c'est le seul moyen de
  voir un doublon réintroduit.
- **N2 humain** : les argumentaires des deux cartes d'arrêt, et l'argumentaire restreint de
  « Désintensifier » → `VALIDATION.md`.

### Si bloqué

- Si la bibliographie du nœud ne porte **pas** de donnée sur l'arrêt d'un insulinosécréteur : **STOP**.
  Ne pas inventer de chiffre ni de NNT (invariant CLAUDE.md 6) ; une carte d'arrêt en preuve `faible`
  qui le dit est acceptable, une carte qui emprunte la preuve de l'insuline ne l'est pas.
- Si restreindre « Désintensifier » la rend inatteignable (aucun profil du banc ne la déclenche plus) :
  STOP — c'est que son périmètre restant est vide et qu'il faut la supprimer, ce qui dépasse le mandat.

### Message de commit (appliqué en fin de plan)

`fix(content): une carte d'arrêt par insulinosécréteur ; « Désintensifier » restreinte aux insulines (P14/S6)`

## Fin de session

Dérouler `/fin-de-tache` (mode parallèle).

---

## Bilan de session (exécuté 2026-08-06, mode vague parallèle)

**Les deux tâches sont finies, dans l'ordre prescrit : T-169 entièrement close et N0 vert avant
ouverture de T-185.** Aucun fichier partagé touché (`STATUS.md`, `TASKS.md`, `plans/P14/index.md`,
`VALIDATION.md` non modifiés) ; rien commité ni poussé (`.claude/wave.lock` toujours présent).

### Fichiers modifiés

- `content/decision/noeuds/diabete-type-2/prescription.yaml` — contenu clinique (T-169 puis T-185),
  version `0.65` → `0.67`, deux entrées de changelog.
- `src/features/decision/engine/evaluateNode.prescription.test.ts` — vignettes existantes corrigées
  (7 assertions dont l'attendu s'inverse : D1, D1b, V-hba1c-derive, R3-3, R3-4-fragile, P-43, R6-4) +
  6 vignettes nouvelles (T169-a/b/c, T185-a/b/c).
- `src/features/decision/engine/banc/invariants.test.ts` — prédicats `estSwitchSulfamide`/
  `estReduireSulfamide` repointés sur les nouveaux intitulés (T-169) ; prédicat `estArretSulfamide`
  ajouté à `VERDICTS_SULFAMIDE` (T-185, sans quoi l'invariant 5 perdait sa couverture sur les profils
  où seule « Sulfamide — arrêter » porte le verdict).
- `src/features/decision/lib/optionBadges.test.ts`, `src/features/decision/engine/groupesExAequo.test.ts`
  — même correctif mécanique (égalité stricte sur l'ancien intitulé nu → `startsWith` sur le nouveau).
- `src/features/decision/engine/banc/paires.test.ts` — conséquence directe et attendue de la
  désambiguïsation T-169 : le test T-161 (`it.fails`, rouge à dessein depuis P14/S1) est devenu VERT
  en réalité (0 violation sur tout le banc, tous nœuds), donc `it.fails` échouait lui-même
  (« Expect test to fail ») ; converti en `it` normal, docstring et commentaire mis à jour pour tracer
  l'historique sans effacer le contexte. Les 5 autres intitulés dupliqués du nœud (Metformine/iSGLT2/
  AR GLP‑1/Tirzépatide/Gliptine — hors périmètre, S8/T-171) restent surveillés en continu par ce même
  test, désormais actif au lieu de dispensé jusqu'à T-171.
- `src/features/decision/engine/banc/couverture.test.ts` — une entrée ajoutée à
  `OPTIONS_REPLI_STRUCTURELLES_INATTEIGNABLES` pour `prescription :: Mesures hygiéno‑diététiques
  seules — réévaluer` (repli déjà dans le contenu, jamais touché par T-169/T-185). Diagnostic dédié
  (détaillé dans l'entrée elle-même) : les 36 profils du banc de 1840 avec
  `intention == initier AND cible_atteinte == true` sont tous bloqués par des cartes ÉTRANGÈRES au
  sulfamide/glinide (Gliptine, Insuline d'initiation, iSGLT2…) — pas par une option touchée cette
  session. C'est un trou de couverture de l'échantillon stratifié, même famille que les deux entrées
  RHD déjà présentes (P14/S7/T-170), signalé au référent plutôt que traité (agrandir le banc dépasse
  le mandat de contenu de cette session).
- `src/features/decision/engine/banc/__snapshots__/caracterisation.prescription.txt`,
  `caracterisation-indetermine.prescription.txt`, `paires.prescription.txt` — régénérés deux fois
  (après T-169, relus, puis après T-185, relus à nouveau).

### N0 — T-169 (isolément, avant ouverture de T-185)

- `npx vitest run src/features/decision/engine/evaluateNode.prescription.test.ts` → **76/76 vert**.
- `npm run build` → **vert**.

### N0 — T-185 (état final de la session)

- `npx vitest run src/features/decision/engine/evaluateNode.prescription.test.ts` → **79/79 vert**
  (76 + 3 vignettes T185-a/b/c ; les 3 vignettes T169-a/b/c comptent dans les 76 puisqu'écrites avant).
- `npx vitest run src/features/decision/engine/banc/` (**I8** et le reste) → vert sur tout ce qui
  concerne `prescription`. La commande plein-dossier prend ~20 min (couverture exhaustive `statine`/
  `rhd-activite-physique`, hors périmètre) ; validé à la place par les 16 fichiers du dossier passés un
  par un — en entier pour `invariants-contenu.test.ts` (I8a/I8b/I8c inclus), `invariants.test.ts`,
  `paires.test.ts`, `caracterisation.test.ts`, et filtrés `-t prescription` pour les 12 autres. **Seuls
  échecs résiduels : 9 tests sur `rhd-activite-physique`/`rhd-alimentation`/`insuline` (P14/S7/T-170,
  session parallèle en cours sur le même dépôt) — aucun sur `prescription`.**
- `npm run build` → **vert**.

### Les 3+3 vignettes ajoutées

**T-169** (`evaluateNode.prescription.test.ts`, describe « T-169 (P14/S6) ») :

- **T169-a** — sulfamide, déjà sous iSGLT2 ET AR GLP‑1, sans intolérance ni hypo → `reduire·Sulfamide`
  applicable (cas nommé par le référent, non couvert avant T-169). Vert.
- **T169-b** — glinide, HbA1c sous la cible, pas d'hypo récente → `remplacer·Glinide` applicable
  (symétrique glinide de R3-3). Vert.
- **T169-c** — sulfamide, HbA1c au-dessus, intolérance → `remplacer·Sulfamide` ET `reduire·Sulfamide`
  coexistent, intitulés vérifiés DISTINCTS (aucun ne vaut le nu « Sulfamide »). Vert.

**T-185** (`evaluateNode.prescription.test.ts`, describe « T-185 (P14/S6) ») :

- **T185-a** — sulfamide, HbA1c sous la cible (`HbA1c_actuelle: 6.0` + `position_vs_cible:
  sous_objectif`) → les **trois** issues visibles (`arreter·Sulfamide`, `reduire·Sulfamide`,
  `remplacer·Sulfamide`), familles vérifiées = `{Traitement à alléger, Traitement à corriger ou
  remplacer}` (2 familles). Vert.
- **T185-b** — insuline SEULE, HbA1c sous la cible → `Désintensifier` présente, aucun intitulé
  contenant « Sulfamide » ni « Glinide » parmi les cartes applicables. Vert.
- **T185-c** — sulfamide SANS insuline, HbA1c sous la cible → `Désintensifier` ABSENTE (c'est le
  redécoupage), `arreter·Sulfamide` présente. Vert.

### État de `paires.prescription.txt` après régénération

180 profils évalués, **179 paires distinctes** (146 avant T-169/T-185 dans la version figée par T-160 ;
diff attendu, relu carte par carte comme demandé — cf. section « Contrôle des paires » ci-dessous).
Section « Paires intra-famille » relue intégralement (65 lignes) : aucun doublon de contenu identifié.
Les coexistences relevées sont toutes attendues et documentées :

- **Sulfamide — arrêter ⟷ Sulfamide — réduire la posologie** (32×) et **Glinide — arrêter ⟷ Glinide —
  réduire la posologie** (53×) : deux gestes différents (arrêter/réduire) sur le même agent, cumulables
  à dessein (c'est exactement ce que T185-a vérifie).
- **Désintensifier ⟷ Sulfamide/Glinide — arrêter/réduire** (15×/12×/5×/11×) : patient sous insuline ET
  sulfamide/glinide simultanément — chaque agent a sa propre carte, pas un doublon.
- **Glinide — arrêter ⟷ Sulfamide — arrêter** (26×) : patient sous les deux insulinosécréteurs, un
  verdict nommé par agent — pas un doublon.
- **Optimiser l'agent mal toléré ⟷ Sulfamide/Glinide — arrêter/réduire** : chevauchement PRÉ-EXISTANT
  (déjà vrai avant T-185 avec « remplacer »/« réduire ») entre la carte générique d'intolérance et les
  cartes nommées par agent — non introduit ni aggravé par cette session.

### Vérification demandée à l'étape 6 de T-185 (« Insuline — réduire » vs « Désintensifier »)

**Pas de doublon, pas de STOP.** Les deux cartes coexistent largement (24× dans le banc figé) mais ne
disent PAS la même chose : « Insuline — réduire » est un geste de **posologie seule** (baisser la
dose) ; « Désintensifier » combine **deux leviers** (baisser la dose ET relâcher la cible glycémique
fixée pour ce patient) — distinction déjà explicite dans le texte de « Désintensifier » avant T-185
(« Le geste combine deux leviers, pas un seul »). Aucune des `conditions` insuline des deux cartes n'a
été modifiée par le redécoupage : la coexistence est pré-existante, pas créée par cette session.

### Sourçage retenu pour les deux nouvelles cartes d'arrêt

- **« Sulfamide — arrêter »** : `niveau_preuve: faible`, référence `grant`. Grant 2025 (PMID 40549370)
  vérifié en source primaire (WebFetch, résumé PubMed) : ECR randomisé, 450 patients ≥ 75 ans, HbA1c
  ≤ 8 %, dont l'intervention de déprescription porte explicitement sur **l'insuline ET/OU le
  sulfamide** — la citation est donc légitime pour cette carte. Limite déclarée dans le texte : Grant
  ne publie pas de résultat isolé pour le seul arrêt du sulfamide (mélange insuline+SU), et son
  critère (hypoglycémie sévère) n'était pas significativement différent (4,7 % vs 6,5 %, NS).
- **« Glinide — arrêter »** : `niveau_preuve: faible`, **aucune référence citée**. Vérifié : Grant 2025
  ne porte QUE sur l'insuline et le sulfamide (jamais le glinide) ; HYPOAGE (vérifié en source primaire,
  DOI 10.2337/dc24-1058, titre exact « … in Older Patients With Type 2 Diabetes ON INSULIN THERAPY »)
  est une cohorte EXCLUSIVEMENT sous insuline ≥ 6 mois — aucune donnée sulfamide/glinide non plus.
  Conformément à l'instruction (« ne jamais faire porter à une carte sulfamide/glinide une preuve qui
  ne concerne que l'insuline/un autre agent »), aucune des deux n'est citée sur cette carte : l'absence
  de donnée est déclarée en toutes lettres dans `effet_attendu`/`inconvenients`, jamais comblée par
  extrapolation chiffrée. Pas de cas de blocage (STOP) : la bibliographie du nœud porte bien des
  données sur l'arrêt d'un insulinosécréteur (le sulfamide, via Grant 2025) — ce n'est que le glinide
  spécifiquement qui n'a pas d'essai dédié, ce que `niveau_preuve: faible` sans référence dit
  honnêtement (I8c ne l'exige pas en `faible`).
- **Correctif de sourçage sur « Désintensifier » restreinte** : `hypoage` y reste cité, et s'y trouve
  désormais CORRECTEMENT scopé — avant T-185, cette référence (population exclusivement sous insuline)
  justifiait aussi l'allègement du sulfamide/glinide, un léger dépassement de son périmètre réel que la
  restriction corrige au passage (pas seulement une réorganisation).

### Blocages rencontrés

**Aucun STOP** (ni sur T-169 « couple contredisant une carte de sécurité », ni sur T-185 « bibliographie
sans donnée sur l'arrêt d'un insulinosécréteur », ni sur « Insuline — réduire »/« Désintensifier »
doublon). Deux effets de bord mécaniques, hors du périmètre « Modifier » de S6.md mais nécessaires au
N0, ont été corrigés en cours de session (détaillés ci-dessus, fichiers listés) : le repointage de
prédicats à intitulé exact dans 3 fichiers de test (renommage T-169), et la bascule `it.fails` → `it`
de T-161 dans `paires.test.ts` (désambiguïsation T-169 ayant fait passer l'invariant réellement à
zéro violation, sur tous les nœuds). Une dette PRÉ-EXISTANTE et non causée par cette session
(`couverture.test.ts`, repli « Mesures hygiéno‑diététiques seules ») a été documentée plutôt que
corrigée — signalée ci-dessus pour le référent.

### N2 — points humains à faire remonter à `VALIDATION.md` en fin de plan

- **Argumentaire révisé de « Sulfamide/Glinide — remplacer par un agent à bénéfice démontré »** (T-169) :
  ouverture au cas HbA1c sous la cible, retrait du motif devenu orphelin — à relire pour confirmer que
  la coexistence à trois cartes (arrêter/réduire/remplacer) reste lisible à l'écran pour le praticien.
- **Argumentaires des deux nouvelles cartes d'arrêt** (« Sulfamide — arrêter », « Glinide — arrêter »,
  T-185) : niveaux de preuve `faible` assumés, sourçage Grant 2025 pour le sulfamide, absence de
  donnée déclarée pour le glinide — à valider cliniquement, en particulier la lecture du niveau de
  preuve « faible » malgré un ECR existant (mélange insuline+SU, non isolé).
- **Argumentaire restreint de « Désintensifier : alléger la charge thérapeutique »** (T-185) : relu et
  récrit au singulier « insuline » (avantages, `contre_indications`) — à confirmer que rien de
  spécifique au sulfamide/glinide n'y subsiste par inadvertance.
- **Repli « Mesures hygiéno‑diététiques seules — réévaluer » inatteignable dans le banc figé**
  (`couverture.test.ts`) : dette documentée, pas corrigée (hors mandat de contenu) — à trancher par le
  référent (agrandir le banc ? accepter comme les deux entrées RHD analogues ?).

### Prochaine action

Rien côté S6 : les deux tâches sont closes, N0 vert, diff non commité laissé dans l'arbre de travail
pour la consolidation de fin de plan (commit tâche par tâche prévu par `/fin-de-tache` une fois toutes
les sessions P14 terminées).

### Correction apportée par l'orchestrateur sur `couverture.test.ts` (hors périmètre de S6)

L'entrée `prescription :: Mesures hygiéno‑diététiques seules — réévaluer` ajoutée à
`OPTIONS_REPLI_STRUCTURELLES_INATTEIGNABLES` **a été retirée**. Le diagnostic technique de S6 est
correct (les 36 profils concernés sont bien bloqués par des cartes étrangères, rien à voir avec
T-169/T-185) mais la catégorisation ne convenait pas : cette table (posée par P14/S7) est réservée aux
replis **structurellement et durablement** inatteignables par construction (le domaine RHD est déjà
couvert à 100 % par les cartes réelles, pour toujours). Ce repli-ci est différent : `plans/P14/S8.md`
T-177 (prochaine vague) le cible **nommément** pour le corriger — élargir son `prerequis` de
`["intention == initier", "cible_atteinte == true"]` à `["intention == initier"]` — précisément parce
que le trou est réel (R10), pas accepté. La Validation de T-177 dit explicitement que
`caracterisation.prescription.txt` doit ensuite montrer « l'apparition du repli sur des profils qui
n'avaient que le socle » — la preuve que ce n'est pas un filet permanent. L'exempter aurait masqué le
signal que T-177 doit lever, et une entrée « acceptée pour toujours » aurait été fausse dès la session
suivante. Repli remis en rouge nu (état identique à avant P14, dette déjà tracée dans `STATUS.md`) :
`npx vitest run couverture.test.ts -t "prescription"` → 1 échec, exactement ce test, message identique
à celui d'avant cette correction. `npm run typecheck` → 0 erreur. **À l'attention de S8** : ce test doit
repasser vert par la correction de contenu de T-177, pas par une nouvelle exemption.

# P14 · S13 — Consolidation   (rédigé par Opus)

> **Modèle : Haiku · effort : low · Vague : 7 (parallèle : non)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les étapes ci-dessous, dans l'ordre. Aucune modification de code ni de contenu.
> Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-06 · Branche : —

## Lire (commun à la session)

- `plans/P14/index.md` — le tableau des sessions et l'ordonnancement.
- Chaque `plans/P14/S<k>.md` — **la ligne « Message de commit » seule** de chaque tâche.
- `STATUS.md`, `TASKS.md`, `VALIDATION.md` — pour savoir où écrire, pas pour les relire en entier.

## Hors périmètre

- **Ne corriger aucun défaut résiduel.** Si un test est rouge, cette session s'arrête et signale.
- Ne pas régénérer de snapshot.

---

## Étapes

1. **Contrôle d'ensemble avant tout commit** :
   - `npm run build` → vert (inclut le typecheck via `tsc -b`) ;
   - `npm test` → vert. **Vérifier explicitement** qu'aucun `it.fails` du plan n'est resté en place :
     T-161 (levé en S8), T-162 (S11), T-163 (S8), T-164 (S8) et **T-189** (levé par S18 **et** S19 —
     il ne passe au vert que lorsque les deux sont livrées). Un `it.fails` survivant = un défaut non
     levé → STOP.
   - `npm run typecheck` → 0 erreur (**jamais** `npx tsc --noEmit`, cf. `CLAUDE.md`).
2. **Commits, fichier par fichier et non tâche par tâche** — c'est la leçon de P13 (sessions dépendantes
   sans commit intermédiaire ⇒ granularité par fichier). Ordre proposé :
   1. `engine/banc/` (les 7 invariants + leurs snapshots) ;
   2. `docs/decision/validation/` (table des conditions, matrice des faits de sécurité) ;
   3. socle P2 (`schema/decision/criteres-communs.schema.json`, `content/criteres-communs/`,
      `content/loadNodes.ts`, `content/node.types.ts`) ;
   4. `content/…/insuline.yaml` (+ ses tests et snapshots) ;
   5. `content/…/prescription.yaml` (+ ses tests et snapshots) ;
   6. `content/…/rhd-*.yaml` (+ leurs tests et snapshots) ;
   7. `content/…/cible-glycemique.yaml` · `schema/decision/noeud.schema.json` ;
   8. socle D50 (`lib/`, `screens/`) ;
   9. `DECISIONS.md` + `docs/commun/decisions/` (D50, D52-D58 — D51 est une décision distincte, hors P14,
      déjà au registre le même jour) ;
   10. `docs/decision/GRAMMAIRE-NOEUD.md` + `CONSTRUIRE-UN-MODULE.md` ;
   11. fichiers de contexte (`STATUS.md`, `TASKS.md`, `VALIDATION.md`, `PROJECT_MAP.md`).
   Reprendre les messages de commit rédigés dans chaque `S<k>.md`.
3. **Statuts** : cocher chaque ligne du tableau de `plans/P14/index.md`. **Le statut ne vit qu'ici.**
4. **`TASKS.md`** : une ligne par tâche, T-160 → T-192 (T-174 abandonnée), au format
   `- T-1xx — <titre> · → plans/P14/S<k>.md`.
5. **`STATUS.md`** : remplacer l'état courant par un résumé de P14 — les neuf défauts traités, le
   chantier P2 (vocabulaire de sécurité du domaine) livré, les sept invariants ajoutés,
   D50, D52-D58, et la leçon consignée (R13, R14, R15, R5/R8/R10 enrichies, P1 et P6 amendés).
   Mentionner ce qui reste **ouvert** : l'arbitrage `antecedent_cv` (scinder le critère ou non, instruit
   par S14 mais non tranché), le doublon éventuel entre les cartes « arrêter » et « Désintensifier » si
   S6 l'a signalé, et tout fait classé « à arbitrer » par S14 et non traité.
   **Vérifier que la ligne de dette `terrain_fragile` a bien été corrigée par S14/T-187** — c'est
   précisément le type de ligne périmée que ce plan existe pour ne plus laisser passer.
6. **Plafonds de lignes** : si un fichier de contexte dépasse, dérouler `/purge-contexte` **avant** le
   dernier commit, pas après.
7. **Push** sur la branche courante.

## Validation

- **N0 auto (bloque le push)** : `npm run build` et `npm test` verts, `git status` propre après le
  dernier commit.
- **N1 visuel auto** : — (S5, S10 et S11 l'ont porté)
- **N2 humain** : `VALIDATION.md` contient bien les entrées attendues de S3, S4, S5, S6, S7, S8, S9,
  S11 et S12. Une session sans entrée alors que son bloc « N2 humain » en demandait une = un oubli →
  le signaler, ne pas l'inventer.

## Si bloqué

Tout test rouge, tout `it.fails` survivant, tout snapshot non relu : **STOP**, ne rien committer, rends
la main avec la liste précise.

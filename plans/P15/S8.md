# P15 · S8 — Câblage moteur du `quand` : classification, G2, `signatureVue`, golden master   (rédigé par Opus)

> **Modèle : Sonnet · effort : xhigh · Vague : 2b (parallèle : non — dépend de S2, cf. correctif ci-dessous)**
> **Environnement : Desktop (navigateur in-app requis)**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-11 · Branche : `decision/panneau-posologie` · **Dépend de S1 ET S2**

> **CORRECTIF DU 2026-08-11, avant exécution.** Cette session a été écrite comme parallélisable avec S2
> (même vague), avec `OptionCard.tsx` en périmètre « Modifier » des deux côtés — un conflit d'édition
> concurrente sur le même fichier, repéré à l'orchestration. S8 démarre maintenant **après S2**, se
> cale sur ses classes (`option-card__posologie-geste` / `--posologie-modalite`) plutôt que de les
> redécouvrir, et son périmètre gagne `screens/DecisionNodeScreen.tsx` — absent de la version
> précédente, alors que c'est le fichier qui câble tout champ de `OptionVue` vers la carte (pattern déjà
> en place : `optionVue.contreIndications` → prop `contreIndications` de `OptionCard`,
> `DecisionNodeScreen.tsx` l. 1414). Sans ce câblage, un champ filtré côté `vueDecision.ts` n'atteindrait
> jamais l'écran.

## Lire (commun à la session)

- `docs/decision/validation/posologie-sourcage-2026-08-11.md` **§5 point 2** — l'analyse du piège, à
  lire en entier avant d'écrire une ligne.
- `docs/decision/GRAMMAIRE-NOEUD.md` — R6, encadré « Même prérequis d'architecture ».
- `src/features/decision/lib/vueDecision.ts` — `serialiseOption` (~l. 562-569), le champ
  `OptionVue.contreIndications` (déclaration l. 178, calcul l. 506) **à prendre pour modèle exact** du
  nouveau champ de posologie filtrée, et les docstrings de tête (l. 19-60) qui expliquent **pourquoi**
  chaque dimension variable entre dans `signatureVue`.
- `src/features/decision/screens/DecisionNodeScreen.tsx` l. 1405-1425 — le bloc de props passées à
  `OptionCard` (`contreIndications={optionVue.contreIndications}` l. 1414,
  `bibliographie={node.sources.references_primaires}` l. 1420) : **le câblage à reproduire** pour le
  nouveau champ.
- `src/features/decision/components/OptionCard.tsx` **tel que livré par S2** — la prop
  `contreIndications?` et son repli (`contreIndicationsParDefaut`, quand la prop est absente) sont le
  patron exact à suivre pour la nouvelle prop de posologie filtrée : optionnelle, avec repli sur
  `option.posologie_detail` traité comme aujourd'hui (toujours affiché) pour tout appelant qui ne la
  transmet pas encore (bancs de test compris).
- `src/features/decision/engine/expressionsNoeud.ts` — `CHAMPS_DU_SCHEMA`, `fragmentsDuNoeud`.
- `src/features/decision/engine/evaluateNode.ts` — `evaluerContreIndications` : **le précédent exact**
  d'une prose conditionnée par le même DSL, côté moteur.

## Hors périmètre

- **Aucun fichier de `content/`.** Ce lot livre le MÉCANISME ; aucun nœud ne pose de `quand` ici.
  Le premier contenu conditionnel est S9.
- Ne pas toucher au rendu de la note de source (S2) ni à I8b (S3).

---

## T-202 — Rendre `posologie_detail` conditionnable au patient, sans casser l'estompage des critères

### Objectif

`itemPosologie` accepte un `quand` (même DSL qu'`exclusions`). Un item dont le `quand` est faux n'est
pas affiché. Et — c'est là tout l'enjeu — un critère qui ne pilote **que** la posologie doit être vu
comme agissant.

### Décision clé

**LE PIÈGE, ET IL EST DOCUMENTÉ COMME RÉCURRENT — CINQUIÈME OCCURRENCE.** `signatureVue`
(`vueDecision.ts`) sérialise ce que l'écran rend ; `engine/relevance.ts` s'en sert pour décider qu'un
critère « agit ». Si `posologie_detail` devient variable **sans entrer dans `signatureVue`**, alors un
critère qui ne change que la posologie affichée sera jugé **sans effet** et **estompé à tort dans le
formulaire** — le praticien ne le renseignera pas, et la posologie restera fausse. Le projet a déjà payé
ce piège sur les alertes d'option, les calculs en attente et les contre-indications conditionnelles.

Deuxième conséquence, du même ordre : `quand` porte des **expressions DSL**, donc il se classe
`decision` dans `CHAMPS_DU_SCHEMA` (et non `inerte` comme `texte`/`sources`), et l'invariant **G2**
exige alors qu'il soit réellement **récolté par `fragmentsDuNoeud`**. Ses littéraux entrent dans les
domaines de tirage du banc → **le golden master de caractérisation bouge**. C'est attendu ; ce qui ne
l'est pas serait qu'il bouge ailleurs que sur cette dimension.

### Lire / Modifier

Modifier : `schema/decision/noeud.schema.json` (`quand` sur `itemPosologie`) ·
`src/features/decision/content/node.types.ts` · `src/features/decision/engine/expressionsNoeud.ts` ·
`src/features/decision/lib/vueDecision.ts` (nouveau champ `OptionVue`, ex. `posologieDetail`, même
statut que `contreIndications`) · `src/features/decision/components/OptionCard.tsx` (nouvelle prop
optionnelle qui remplace `option.posologie_detail` QUAND fournie, repli identique au patron
`contreIndications` sinon — **ne recevoir que des items déjà filtrés**, jamais évaluer un `quand` dans
le composant) · `src/features/decision/screens/DecisionNodeScreen.tsx` (câbler la nouvelle prop depuis
`optionVue`, même bloc que les props `contreIndications`/`bibliographie` existantes) · les tests de banc
concernés · `OptionCard.test.tsx` (cas de repli sans la nouvelle prop, NON couvert avant cette session)
· `src/features/decision/engine/banc/__snapshots__/` (régénération contrôlée).

### Étapes

1. Lire `evaluerContreIndications` **en entier** : tu reproduis sa frontière — le moteur évalue, le
   modèle de vue porte le résultat, **la carte n'évalue rien** et ne connaît pas les critères du patient.
2. Schéma + types : `quand?: string` sur `itemPosologie`.
3. `CHAMPS_DU_SCHEMA` : `quand` en `decision`. Constater G1 vert, puis **G2** : récolter dans
   `fragmentsDuNoeud`. Si G2 reste rouge, tu n'as pas récolté au bon endroit — ne le contourne pas.
4. Filtrage dans le modèle de vue (`vueDecision.ts`) : nouveau champ d'`OptionVue`, calculé au même
   endroit que `contreIndications` (~l. 506), jamais dans le composant.
5. `OptionCard.tsx` : nouvelle prop optionnelle consommant ce champ filtré, avec repli EXACT sur le
   comportement actuel (`option.posologie_detail` en entier, non filtré) pour tout appelant qui ne la
   transmet pas — même contrat que `contreIndications?`. `DecisionNodeScreen.tsx` : câbler la prop.
6. **`signatureVue`** : faire entrer la liste des items retenus dans `serialiseOption`. Vérifier par un
   test dédié qu'un critère ne pilotant QUE la posologie est bien vu comme agissant par
   `engine/relevance.ts` — c'est **l'assertion la plus importante de cette session**.
7. Régénérer le golden master. **Relire le diff** : toute variation qui ne s'explique pas par
   l'ajout de cette dimension est un bug, pas un bruit de snapshot.

### Validation

- **N0 auto (bloque le commit)** : `npm run typecheck` · `npm test` (G1 **et** G2 verts, golden master
  relu et justifié) · `npm run build` → verts. Mesurer machine libre.
- **N1 visuel auto** : aucun contenu ne pose de `quand` — vérifier la **non-régression** du panneau et
  **de l'estompage des critères** dans le formulaire sur `insuline` → `/verif-visuelle`.
- **N2 humain** : —

### Si bloqué

- Le golden master bouge sur autre chose que cette dimension → **STOP**, ne régénère pas en aveugle.
- Tu es tenté de filtrer les items dans `OptionCard.tsx` → **STOP** : c'est la frontière que ce projet
  défend partout.

### Message de commit (appliqué en fin de plan)

`Décision (moteur) : la posologie peut dépendre du patient, et ses critères ne sont plus estompés`

## Fin de session

Dérouler `/fin-de-tache` (mode parallèle).

# P15 · S6 — Migration du sourçage : `prescription`   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 3 (parallèle : oui, avec S5 et S7)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-11 · Branche : `decision/panneau-posologie`

## Lire (commun à la session)

- `docs/decision/validation/posologie-sourcage-2026-08-11.md` **§1.2** (les 20 citations de
  `prescription`, dont 2 seulement mappaient avant l'arbitrage) et **§4.1**.
- `docs/decision/validation/citations-non-verifiables-2026-08-11.md` **et sa section « Arbitrage »**
  (produite par S4, tranchée par Thibault). **Sans cette section, cette session ne démarre pas.**
- `content/decision/noeuds/diabete-type-2/prescription.yaml`.

## Hors périmètre

- Aucun autre nœud. Aucun `quand`. Aucun code, schéma ni test de banc.
- **Ne pas trancher soi-même le sort d'une citation orpheline** : S4 a instruit, Thibault a tranché,
  tu appliques.

---

## T-200 — Sortir les 20 citations de `prescription`, en appliquant l'arbitrage des orphelines

### Objectif

Même migration que S5, sur le nœud le plus chargé — et c'est ici que l'arbitrage de doctrine paie :
avant lui, 2 citations sur 20 étaient mappables ; le second registre débloque l'essentiel du reste.

### Décision clé

**Ce nœud porte 8 des citations orphelines** (ACC 2020, JAMA Cardiol 2020 ×4, AAFP 2026, ameli, base de
données publique des médicaments). Leur sort a été tranché en S4 : chacune est soit versée en
`references_primaires`, soit versée en `reco_officielle.references` avec un `id`, soit retirée de
l'écran. **Applique la décision consignée, ne la rejoue pas.**

Beaucoup d'`apercu` de ce nœud ne sont pas de la posologie mais un motif de sécurité (« arrêt immédiat,
sans décroissance : contre-indication formelle dès DFG < 30 (KDIGO 2022) »). **Ne pas les réécrire ni
les déplacer de canal** : c'est un défaut réel, mais il n'appartient pas à P15 — le signaler dans le
rapport de session et le remonter au backlog.

### Lire / Modifier

Modifier : `content/decision/noeuds/diabete-type-2/prescription.yaml` uniquement.

### Étapes

1. Créer/compléter les entrées de bibliographie décidées en S4 (avec leur `id` pour le registre
   `reco_officielle`).
2. Traiter le §1.2 ligne par ligne, comme en S5 : conversion en `{ texte, sources }`, retrait de
   l'incise, contrôle de la phrase.
3. **Les 13 options qui n'ont qu'un `apercu`** : ce champ est aujourd'hui tout leur contenu de
   posologie et il est rendu au registre principal. **Ne pas les convertir en `posologie_detail` dans
   cette session** — c'est un déplacement de contenu, pas une migration de sourçage. Le signaler.
4. Bump de version + changelog (D5), en nommant les citations laissées en incise s'il en reste.

### Validation

- **N0 auto (bloque le commit)** : `npm test` → vert, I8b étendu et `content.test.ts` compris ·
  `npm run build` → vert.
- **N1 visuel auto** : panneau posologie sur au moins 4 options du nœud, dont « Envisager l'insuline »
  et une option `apercu` seul → `/verif-visuelle`.
- **N2 humain** : **relecture D5** — conduite identique mot pour mot ; les sources nouvellement versées
  en bibliographie sont correctes. `VALIDATION.md`.

### Si bloqué

- La section « Arbitrage » de S4 est absente ou incomplète → **STOP**, la gate n'est pas franchie.
- Une phrase devient bancale sans son incise → **STOP** plutôt que de la réécrire.

### Message de commit (appliqué en fin de plan)

`prescription v0.75 : sources de posologie en note, bibliographie complétée`

## Fin de session

Dérouler `/fin-de-tache` (mode parallèle).

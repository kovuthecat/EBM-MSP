# P16 · S10 — Mesure finale à trois configurations   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 7 (parallèle : oui)**
> **Environnement : indifférent** (`WORKFLOW.md` §6, N1)
> **Régime : fixé**
> **Latitude : moyens libres ; objectif, contrats publics et zone du plan fixes**
> **Correctif localisé : interdit — corriger une skill ou un agent pendant sa propre mesure la
> mesurerait sur les cas mêmes qui ont inspiré la correction ; la remédiation passe par un cycle
> distinct, puis une nouvelle mesure** (le harnais, dans la zone de la session, reste corrigeable)
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Tu t'arrêtes sur un choix que tu n'as pas reçu, pas parce que
> quelque chose a cassé : `EXECUTANT.md`.

- Date : 2026-09-24 · Branche : —

## Lire (commun à la session — point de départ, pas une clôture)
`.claude/workflow/EXECUTANT.md` (toujours en premier), puis :
- `plans/P16/index.md` § Objectif d'ensemble (règle de verdict, risques, arbitrages : configuration 2
  à invite recomposée, corpus plat → non-régression, mesure complète, plafond de coût).
- `docs/commun/epreuves-recherche/harnais/*.mjs` et leurs tests (créés par S4).
- `docs/commun/epreuves-recherche/mesures/*-reference/rapport.md` et `resultats.json` (S4) — la
  configuration 1 se **réutilise**, elle ne se remesure pas.
- `.claude/skills/recherche-preuve-triangulee/SKILL.md`, `.claude/skills/verif-source-veille/SKILL.md`
  (S8) — forme des blocs `<!-- lancement:… -->` ; `.claude/agents/*-preuve.md` (S8).

## Hors périmètre
Le corpus (`cas/`, `README.md`) porte des résultats attendus **validés par le référent** : un
résultat attendu qui paraît faux relève de son jugement (critère d'arrêt n°4). Il se signale dans le
bilan, avec l'exécution qui le montre. Skills et agents : voir le bandeau (correctif localisé
interdit, avec sa raison).
**Aucune requête OpenEvidence.** Vague verrouillée (S11 en parallèle) : ne pas committer, ne pas
toucher `STATUS.md`, `TASKS.md`, `VALIDATION.md`, `index.md`, ni `docs/commun/epreuves-recherche/cas-module/`
(écrit par S11) ; ne rien laisser tourner après la réponse finale.

---

## T18 — Configurations 2 et 3, déroulé à blanc des circuits

### Objectif
Étendre le harnais aux configurations 2 (nouvelles skills, agents généraux à invite recomposée) et 3
(nouvelles skills, agents dédiés), et à un second corpus (`--corpus`) ; relancer le canari ; dérouler
chaque circuit une fois de bout en bout en configuration 3.

### Pourquoi
L'écart 1→2 mesure le contenu (socle, scripts, circuits) ; l'écart 2→3 mesure ce que §14.2 promet des
agents dédiés — des consignes stables contre une invite refaite à chaque lancement (arbitrage du
2026-09-24). Sans une configuration 2 fabriquée mécaniquement à partir des mêmes circuits, les deux
écarts se mélangent. Le déroulé à blanc est la porte « circuits déroulables de bout en bout » de
§14.4.

### Décision clé
- **Configuration 3** = export du commit courant tel quel.
- **Configuration 2** = même export, puis, par le harnais (testé) :
  1. retirer `.claude/agents/{extracteur,contradicteur,reconciliateur}-preuve.md` ;
  2. dans chaque bloc `<!-- lancement:<agent> --> … <!-- /lancement -->` des circuits, remplacer
     la ligne **Lancer** par : « **Lancer** un agent `general-purpose` (outil Agent). Dans cette
     configuration l'agent dédié n'existe pas : compose toi-même son invite à partir du rôle, des
     entrées et des références ci-dessous. »
  Nombre de blocs transformés = nombre de blocs trouvés, sinon erreur.
- Configurations 2 et 3 : Bash autorisé **seulement** pour
  `node .claude/skills/recherche-source-primaire/scripts/*` ; mêmes refus qu'en configuration 1 ;
  `--model sonnet` et `CLAUDE_CODE_SUBAGENT_MODEL=sonnet` (même modèle partout).
- **Second corpus** : `lancer.mjs … --corpus <dossier>` (défaut `cas/`), pour les épreuves de la
  skill module que S11 écrit sous `cas-module/`. Même format ; la rubrique Signatures peut y valoir
  `- (aucune)` ; liste blanche lue dans le `README.md` de ce dossier.
- **Canari à refaire** en configuration 3 : les dix gestes de S4, plus
  `node .claude/skills/recherche-source-primaire/scripts/identite.mjs --help`, **autorisé**, et
  `node -e "1"`, **refusé**.
- **Déroulé à blanc** : un cas du corpus par circuit (Décision, veille §7, veille §7bis), en
  configuration 3.
  - Attendu : la transcription montre le lancement de chaque agent dédié par son nom.
  - Attendu : les livrables existent, et `verifier-registre.mjs` passe sur le registre produit
    (circuit Décision).
  - Pas d'étape OE réelle : le circuit reçoit le retour OE archivé du cas.

### Lire / Modifier
- Modifier : `docs/commun/epreuves-recherche/harnais/{cas,export,lancer}.mjs` et les tests
  `docs/commun/epreuves-recherche/harnais/{cas,export}.test.mjs` (créés par S4, vague 3).
- Créer : `docs/commun/epreuves-recherche/mesures/<date>-deroule/` (transcriptions résumées :
  agents lancés, livrables, verdict du registre).

### Étapes
1. Configuration 2, configuration 3 et `--corpus` dans `export.mjs` et `lancer.mjs` ; tests (config 2
   sans agents `*-preuve`, blocs transformés et comptés, config 3 intacte, `(aucune)` accepté hors
   `cas/`).
2. `lancer.mjs canari --config 3` au premier plan → vert, sinon arrêt (voir Si bloqué).
3. Déroulé à blanc des trois circuits (lot + `attendre` au premier plan), résumé dans
   `mesures/<date>-deroule/`.

### Validation
- **N0 auto (bloque le commit)** : `node .claude/workflow/bin/n0.mjs` → exit 0 ; ciblé :
  `node .claude/workflow/bin/n0.mjs --cible docs/commun/epreuves-recherche/harnais/export.test.mjs`.
- **Tests** : `export.test.mjs` (configurations 2 et 3), `cas.test.mjs` (`--corpus`, `(aucune)`).
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : un déroulé à blanc jugé sur le livrable seul. Si la configuration 3 lance
  `general-purpose` parce que l'agent dédié n'a pas été trouvé, le livrable existe quand même, et la
  mesure compare deux configurations identiques. Le verdict se lit dans la transcription, qui doit
  montrer `subagent_type` égal à `extracteur-preuve`, `contradicteur-preuve` et
  `reconciliateur-preuve`.

### Si bloqué
- Canari rouge en configuration 3 (Bash ou sous-agent qui échappe aux refus) : `FAIL` nature
  `prémisse`, écrite falsifiable. Ne pas mesurer.
- Un circuit qui ne lance pas ses agents par leur nom : c'est une régression de S8, rapportée en
  `FAIL` nature `exécution`, avec la transcription à l'appui.

### Message de commit (appliqué par la session elle-même)
`feat(epreuves): configurations 2 et 3, déroulé à blanc des circuits`
Dernière ligne du commit, obligatoire : `Plan: P16/S10/T18`

---

## T19 — Mesure finale et attribution du gain

### Objectif
Mesurer le corpus en configurations 2 et 3 (2 exécutions par cas, 3e si 1/2), appliquer la porte de
non-régression contre la configuration 1 de S4, attribuer le gain par couche, écrire le rapport final.

### Pourquoi
C'est la réponse au principal revers de l'option C : tout changer d'un coup rend un gain ou une
régression inattribuable (§14.4). La porte « aucune régression » est ce qui autorise à considérer la
refonte comme adoptable.

### Décision clé
- **Plafond de dépense** : estimation = coût moyen par exécution de S4 × exécutions prévues ici. Dès
  que la dépense cumulée dépasse **le double** de l'estimation → arrêt sur question (voir Si bloqué).
- **Porte** : aucun cas réussi en configuration 1 et échoué en configuration 3 (règle de verdict de
  l'index). Une régression → `FAIL`, avec le cas, les exécutions et les `grading.json`.
- **Attribution** : écarts 1→2 (contenu) et 2→3 (agents dédiés), cas par cas et au total. Si S4 a
  conclu « corpus plat » : écrire « gain non mesurable sur ce corpus », la porte de non-régression
  reste la seule conclusion (arbitrage du 2026-09-24).
- Le livrable se lit sans la conversation : `mesures/<date>-finale/rapport.md` contient :
  - la table des verdicts des configurations 1, 2 et 3 ;
  - les régressions (ou « aucune ») ;
  - l'attribution par couche ;
  - les exécutions invalidées et leur motif ;
  - le coût et la durée par configuration ;
  - les erreurs résiduelles, par mode d'échec.

### Lire / Modifier
- Créer : `docs/commun/epreuves-recherche/mesures/<date>-finale/` (enregistrements, `resultats.json`,
  `rapport.md`).

### Étapes
1. Calculer et écrire l'estimation de coût avant de lancer.
2. `mesurer --config 2` puis `--config 3` sur le commit courant ; `attendre` au premier plan jusqu'à
   « lot terminé » ; suivre la dépense cumulée.
3. Un correcteur par cas et par configuration, au premier plan (`grading.json`).
4. 3e exécution pour les cas à 1/2 ; exécutions invalidées rejouées une fois.
5. Verdicts, porte de non-régression, attribution, rapport.

### Validation
- **N0 auto (bloque le commit)** : `node .claude/workflow/bin/n0.mjs` → exit 0.
- **Tests** : — (mesure : verdicts et régressions calculés par `verdicts.mjs`, testé en S4).
- **N1 visuel auto** : —
- **N2 humain** : le référent lit le rapport final. Ce jugement n'est pas bloquant et se consigne
  dans le bilan, reversé dans `VALIDATION.md` en fin de plan. Pour chaque régression ou erreur
  résiduelle, le rapport dit ce qu'elle change pour l'usage réel des circuits.
- **Anti-raccourci** : une configuration 1 remesurée ici plutôt que reprise de S4. Le web a bougé
  entre-temps, par exemple un article devenu libre d'accès : un « gain » ou une « régression »
  apparaît alors, qui ne doit rien aux skills. La table cite pour la configuration 1 les exécutions
  de S4, avec leur date.

### Si bloqué
- Dépense cumulée au-delà du double de l'estimation : finir les exécutions en cours, écrire un
  rapport partiel. S'arrêter ensuite sur une **question** à l'utilisateur, avec deux options :
  poursuivre avec un nouveau plafond, ou s'en tenir au rapport partiel (budget, arbitrage du
  2026-09-24).
- Régression constatée : ce n'est pas un blocage, c'est le verdict de la porte. `FAIL` avec le
  rapport complet, pour la remédiation.

### Message de commit (appliqué par la session elle-même)
`docs(epreuves): mesure finale à trois configurations`
Dernière ligne du commit, obligatoire : `Plan: P16/S10/T19`

---

## Écarts au plan

- **Amendement : circuit Veille exclu, T18 et T19** (arbitrage utilisateur reçu en chat par
  l'orchestrateur, relayé en cours de session, 2026-09-24) : « on SAUTE les épreuves du circuit
  Veille. Il veut surtout tester les skills du circuit Décision. » Conséquence T18 : seul le circuit
  Décision (`recherche-preuve-triangulee`) est déroulé à blanc — les déroulés Veille §7 et §7bis,
  prévus par la Décision clé de T18, ne sont pas joués. Conséquence T19 : seuls les cas du corpus qui
  exercent `recherche-preuve-triangulee` sont mesurés en configurations 2 et 3 — **E02, E03, E04, E08,
  E09, E12** ; **exclus : E01** (`recherche-source-primaire`, le socle seul, pas le circuit
  triangulé), **E05, E06, E07, E10, E11** (`verif-source-veille`, circuit Veille). Mesure commitée
  (T18 : canari + déroulé Décision ; en cours pour T19), objectif intact (mesurer le circuit Décision
  reste possible et fait), remède dans la zone du plan (portée du corpus mesuré, pas le harnais ni les
  skills), aucun critère d'arrêt touché : amendement, pas un arrêt.
- **Amendement : mesure finale (T19) arrêtée avant son terme** (arbitrage utilisateur de coût, reçu en
  chat par l'orchestrateur, relayé en cours de session, 2026-09-24 — « ARRÊT demandé par l'utilisateur
  (arbitrage de coût) : cesse la mesure MAINTENANT »). Sur les 6 cas Décision : E02 et E03 sont
  pleinement mesurés et corrigés (2/2 chacun, configs 2 et 3) ; E04 reste à 1/2 dans les deux
  configurations (3ᵉ exécution non jouée) ; **E08, E09, E12 sont mesurés (24 exécutions, coût et
  livrables réels acquis) mais non corrigés** — l'arrêt est arrivé avant leur passe de correction, pas
  avant leur mesure (aucune exécution `claude -p` n'a été interrompue en cours : le dernier lot avait
  déjà atteint « lot terminé »). **E08 est la lacune la plus importante** : seul autre cas réussi en
  configuration 1 (avec E02) dont la régression en configuration 3 reste indéterminée, faute de
  correction. Dépense réelle 56,62 $, sous le plafond de 71,27 $ (le double de l'estimation) : l'arrêt
  n'est pas un dépassement de plafond, c'est un arbitrage distinct. Mesure commitée (les 24 exécutions
  et leurs livrables sont écrits dans `mesures/2026-09-24-finale/`), objectif intact (mesurer le
  circuit Décision, sur un rapport qui se marque lui-même comme partiel plutôt que de conclure au-delà
  des données), remède dans la zone, aucun critère d'arrêt touché : amendement, pas un `FAIL`. Rapport
  partiel : `docs/commun/epreuves-recherche/mesures/2026-09-24-finale/rapport.md` — marque
  explicitement, cas par cas, ce qui est corrigé, ce qui est mesuré-non-corrigé, et ce qui n'est pas
  mesuré du tout ; ne conclut aucun verdict au-delà des données acquises.
- **Note d'exécution** : sur instruction du coordinateur reçue avec l'arrêt de coût, ce `S10.md` n'est
  **pas commité** dans cette session (l'exception habituelle sous `.claude/wave.lock` ne porte que sur
  le **push** ; ici l'instruction portait explicitement sur le commit). Les fichiers de T18 sont
  commités (`0e8a130`) ; ceux de T19 (rapport partiel, `estimation.md`, `resultats.json`,
  24 enregistrements/livrables sous `mesures/2026-09-24-finale/`) restent **non commités**, à la
  décision de l'orchestrateur ou d'une session de reprise.

## Bilan de session

**Correctif localisé (T18, en tête de bilan — `WORKFLOW.md` §9a, harnais dans la zone de la session,
« Correctif localisé : interdit » du bandeau ne visant que les skills/agents) :** le canari de
configuration 3, étendu par T18 (gestes 11-12), est d'abord sorti rouge — Bash cru scopé à
`identite.mjs`/`verifier-registre.mjs` par un motif `Bash(...)` dans `--allowedTools`, ne l'est pas.
Confirmé par quatre sondes isolées, hors export (`--permission-mode` `dontAsk`, `auto`, `manual`,
`--permission-prompts none`) : dans cette version du CLI, un motif `Bash(…)` dans `--allowedTools` ne
restreint rien tant qu'aucun motif nommant `Bash` ne figure dans `--disallowedTools` — dès qu'un tel
motif y figure (bare `Bash` ou `Bash(*)`), l'outil disparaît ENTIÈREMENT, aucune restriction fine
n'étant atteignable (Bash est binaire : tout ou rien). Remède (`export.mjs` § `construireReglages`,
~15 lignes hors tests) : Bash reste refusé dans les trois configurations, y compris quand les scripts
existent au commit exporté — `identite.mjs`/`verifier-registre.mjs` restent hors de portée d'un
circuit mesuré ici, **uniformément** dans les trois configurations (aucun biais différentiel : la
configuration 1 les avait déjà hors de portée, pour une autre raison — les scripts n'existaient pas
encore à son commit). Canari rejoué : vert. Tests et N0 rejoués verts après le correctif. Commit
`0e8a130` (T18).

**Canari (configuration 3, douze gestes)** — vert :

| # | Geste | Attendu | Constaté |
| --- | --- | --- | --- |
| 1-10 | (dix gestes de S4 : Read/Grep/Glob du dépôt, Bash `echo`, PowerShell, WebFetch github, Read Interface-OE, sous-agent, Read CLAUDE.md export, WebFetch pubmed) | comme S4 | identique à S4 |
| 11 | Bash `identite.mjs --help` (scripts présents) | refusé | refusé (outil absent — correctif ci-dessus) |
| 12 | Bash `node -e` hors liste blanche | refusé | refusé (outil absent) |

**Déroulé à blanc (T18)** — circuit Décision seul (Veille exclu, amendement ci-dessus). Invite
hors-corpus (aucun cas du corpus ne couvre un circuit entier), sujet apparenté à E02, retour OE
archivé réutilisé, aucune requête OE réelle. Agents lancés, lus dans la transcription (jamais dans la
réponse du modèle) : `extracteur-preuve`, `contradicteur-preuve`, `reconciliateur-preuve` — les trois,
par leur `subagent_type` exact. Tous les livrables du circuit produits (cadrage, agent-A, registre,
journal, OE, agent-B, consolidation, synthèse). `verifier-registre.mjs`, lancé par la session
orchestrant le déroulé (hors de l'export, Bash n'y étant pas disponible pour le circuit lui-même — le
circuit l'a correctement constaté et livré son dossier comme « état partiel ») : **0 erreur, code 0**.
Coût 5,77 $, durée ≈ 29 min. Porte « circuits déroulables de bout en bout » (§14.4) : **passée** pour
le circuit Décision. Détail : `mesures/2026-09-24-deroule/rapport.md`.

**Estimation et dépense réelle (T19)** — estimation avant lancement : 1,4849 $/exécution (S4,
configuration 1) × 24 exécutions prévues (6 cas Décision × 2 configurations × 2 exécutions)
≈ 35,64 $ ; plafond (le double) ≈ 71,27 $. Dépense réelle : **56,62 $** (24 exécutions retenues,
configs 2+3), sous le plafond. 7 exécutions techniquement échouées (429, limite de session API,
coût nul), rejouées avec succès après une sonde de reprise. Détail : `mesures/2026-09-24-finale/rapport.md`.

**Verdicts des trois configurations (sous-ensemble Décision, 6 cas — Veille non mesuré)** :

| Cas | Config 1 | Config 2 | Config 3 |
| --- | --- | --- | --- |
| E02 | réussi | réussi | réussi |
| E03 | échoué | échoué | échoué |
| E04 | échoué | incomplet (1/2, 3ᵉ non jouée) | incomplet (1/2, 3ᵉ non jouée) |
| E08 | réussi | mesuré, non corrigé | mesuré, non corrigé |
| E09 | échoué | mesuré, non corrigé | mesuré, non corrigé |
| E12 | échoué | mesuré, non corrigé | mesuré, non corrigé |

**Régressions** : aucune constatée sur les données corrigées (E02 réussi partout). **Une régression
reste indéterminée** : E08 (réussi en configuration 1), non corrigé en configuration 3 — priorité
d'une reprise.

**Attribution 1→2 et 2→3** : non conclusible sur ce rapport partiel — seuls E02 et E03 sont pleinement
corrigés dans les deux configurations, et les deux ont le même verdict dans les trois configurations
(aucune variation à attribuer). Pas une conclusion « gain nul » : une absence de données suffisantes,
écrite comme telle dans le rapport.

**Chemin du rapport** : `docs/commun/epreuves-recherche/mesures/2026-09-24-finale/rapport.md` (et
`resultats.json`, `estimation.md` à côté). Déroulé à blanc : `mesures/2026-09-24-deroule/rapport.md`.

**Item N2** : le référent lit le rapport final (partiel) et juge si l'arrêt de coût et l'exclusion
Veille appellent une reprise immédiate ou une décision différée — non bloquant, à reverser dans
`VALIDATION.md` en fin de plan (bandeau T19).

**N0** : vert (`typecheck` → PASS, `build` → PASS, `tests` → PASS) — rejoué après le correctif
localisé et à la fin de la session, aucun code n'a changé depuis (T19 n'a écrit que des données sous
`mesures/`).

## Fin de session
Dérouler `/fin-de-tache` (mode selon « parallèle : oui/non » du bandeau). **Note** : par instruction du
coordinateur (arrêt de coût), ce fichier et les données de T19 restent non commités à l'issue de cette
session — à traiter en priorité par `/fin-de-tache` ou une reprise.

Bloquant : 0
Couverture : complète

## Bloquants
- aucun

## Backlog
- `docs/commun/epreuves-recherche/harnais/signatures.test.mjs` (hors zone S6, non modifiable ici) —
  `fichiersSuivisArbreDeTravail` résout la liste blanche via `git ls-files`, donc en vague verrouillée
  (rien commité) un fichier neuf comme `acces-identite.md`/`openevidence.md`/`lecons.md` est invisible
  à la porte N0 tant qu'il n'est pas commité : le « PASS » de N0 sur cette session ne couvre pas
  réellement l'anti-fuite de signatures sur ses propres fichiers neufs, seulement sur les fichiers déjà
  suivis. La session l'a repéré elle-même (bilan, « Greps de contrôle ») et a fait un contrôle manuel
  hors N0 (tracked+untracked) — j'ai rejoué ce contrôle indépendamment (`lireTousLesCas` + parcours du
  répertoire de la liste blanche, tracked et untracked, hors PDF) : 42 signatures × 32 fichiers texte
  résolus → 0 fuite. Donc pas un bloquant pour S6, mais la porte mérite d'être corrigée (lire
  l'arborescence plutôt que `git ls-files`, ou au moins avertir) pour que S7-S11 n'aient pas le même
  angle mort en vague verrouillée.
- `.claude/skills/recherche-source-primaire/references/registre-affirmations.md` § Format, l.72-73
  (signalé aussi par le bilan de session, hors zone S6 : section intouchable, contrat S5) — raconte un
  fait correctif d'incident (une valeur de NNT et sa correction) dans le socle, contraire à l'esprit
  « aucun fait correctif dans le socle » que S6 applique partout ailleurs (`lecons.md` notamment). À
  reprendre par une session qui a la section Format dans sa zone.

## Vérifications faites
- Objectif/Validation de T9, T10, T11 (`plans/P16/S6.md`) confrontés au diff : contenu de
  `acces-identite.md`, section « Marche à suivre » de `registre-affirmations.md`, `openevidence.md`,
  `lecons.md`, `SKILL.md` réécrit — chaque point de la « Décision clé » de chaque tâche retrouvé dans
  le fichier livré.
- Anti-raccourci T9 : `grep -rniE "PMID sur|Minerva|Exercer|Prescrire" references/` → aucune ligne ;
  `grep -c "00-global.md" acces-identite.md` → 7 (≥ 2 exigé). Élargi : aucune donnée factuelle propre
  à `00-global.md` l.99-130 (statistiques PMID, noms d'essais, seuil SFD 0,5 U/kg, nom du PDF à 0
  caractère) recopiée dans le socle — grep dédié, aucune occurrence.
- Anti-raccourci T10 : `grep -n "agent" openevidence.md` → la phrase qui réserve l'appel CLI à
  l'orchestrateur (l.21-25), conforme.
- Anti-raccourci T11 : `git show HEAD:.../SKILL.md` comparé à la table « ancien SKILL.md → nouveau
  domicile » du bilan — plusieurs entrées vérifiées au hasard (l.8 rôle/P5, l.32-38 PubMed,
  l.119-120 localisation) : chaque section retirée a bien un domicile réel, contenu retrouvé. `grep -n
  "P5" SKILL.md` → aucune ligne ; SKILL.md fait 68 lignes (< 80, conforme à la cible).
- Amendement « Écarts au plan » (`précision demandée` au lieu de `clarification requise`, signature
  E12) : vérifié falsifiable — `docs/commun/epreuves-recherche/cas/E12-oe-incomplet-fictif/cas.md`
  § Signatures porte bien la signature littérale `` `clarification` `` ; `openevidence.md` emploie
  `précision demandée` de façon cohérente (§4 titre, § statut, § fiche de retour) et ne contient nulle
  part la sous-chaîne `clarification` (`grep -rni clarification` sur tout le socle → 0 résultat).
  L'amendement sert l'objectif (statut distinct, détecté par le circuit, relance encadrée) sans
  toucher de critère d'arrêt — cohérent avec la table WORKFLOW.md §9a.
- Correctif localisé `scripts/identite.test.mjs` : lit désormais les signatures via `lireTousLesCas`
  (`harnais/cas.mjs`) au lieu de les recopier en clair ; vérifié que `parseSignatures` rend bien des
  objets `{valeur, note}` — l'accès `s.valeur` du correctif est correct. Cause mesurée plausible et
  remède dans la zone déclarée (le fichier est cité comme correctif localisé en tête du bilan).
- N0 rejoué indépendamment : `node .claude/workflow/bin/n0.mjs` → `typecheck PASS`, `build PASS`,
  `tests PASS` (269 s), exit 0.
- Périmètre : ni `STATUS.md`, `TASKS.md`, `VALIDATION.md`, `index.md` touchés ; `contradiction.md` et
  `consolidation.md` (zone S7) non créés par S6, conforme à « Hors périmètre ».
- `docs/decision/00-global.md` : tous les renvois cités par les nouvelles pages (§ Pipeline d'un nœud,
  § Règles de sourcing, § Sources locales, § Garde-fous de vérification, et les plages de lignes
  l.20-30, l.99-135, l.103-111, l.123-129) correspondent à un contenu réel à cet endroit — pas de
  renvoi mort.

# P16 · S9 — Mode d'emploi OE, renvois, tri-boite-mail   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 6 (parallèle : non)**
> **Environnement : indifférent** (`WORKFLOW.md` §6, N1)
> **Régime : fixé**
> **Latitude : moyens libres ; objectif, contrats publics et zone du plan fixes**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Tu t'arrêtes sur un choix que tu n'as pas reçu, pas parce que
> quelque chose a cassé : `EXECUTANT.md`.

- Date : 2026-09-24 · Branche : —

## Lire (commun à la session — point de départ, pas une clôture)
`.claude/workflow/EXECUTANT.md` (toujours en premier), puis :
- `docs/commun/OUTIL-INTERFACE-OE.md` (89 l.) ; `C:/Users/Kovu/Projets/Interface-OE/docs/utiliser-le-cli.md`
  l.1-60 (options réelles du CLI, dont `--modele`, et ses codes de sortie).
- `docs/commun/2026-09-16-propositions-skills-recherche.md` §13.1 (l.395-402), N1 (l.516-520), N3
  (l.528-533).
- Les deux circuits réécrits (S8) et `.claude/skills/recherche-source-primaire/SKILL.md` (S6) — ce
  que les renvois doivent décrire.
- Les lignes qui citent les skills : `CLAUDE.md` l.44-49 ; `docs/decision/00-global.md` l.20-30 ;
  `docs/decision/CONSTRUIRE-UN-MODULE.md` l.218-222 ; `docs/veille/SOP_veille.md` l.72-76 et l.221-225 ;
  `docs/veille/TRI_BOITE_MAIL.md` l.7-10 ; `docs/commun/OUTIL-INTERFACE-OE.md` l.82-89.
- `.claude/skills/tri-boite-mail/SKILL.md` (72 l.).
- `PROJECT_MAP.md` — rubriques des skills, agents et docs.

## Hors périmètre
Les **archives** ne se réécrivent pas : `docs/veille/JOURNAL_BOITE_MAIL.md` l.100 (journal daté),
`docs/decision/validation/**` (dont `OE-titration-mcg-2026-08-11.md`), `plans/P15/**` — ils citent les
skills telles qu'elles étaient, c'est leur rôle. Le dépôt Interface-OE ne se modifie pas (son propre
`utiliser-le-cli.md` porte aussi un ancien chemin : le signaler dans le bilan, pas le corriger). Aucune
signature du corpus dans les fichiers de la liste blanche (`00-global.md`, `SOP_veille.md`,
`OUTIL-INTERFACE-OE.md`, `CLAUDE.md` : `signatures.test.mjs`, N0). `CLAUDE.md` garde « 4 skills » : la
cinquième est ajoutée par S11.

---

## T16 — Mode d'emploi OE (N1)

### Objectif
Mettre `docs/commun/OUTIL-INTERFACE-OE.md` à jour :
- le bon chemin du CLI, écrit une seule fois ;
- `--modele` et la ligne `Modèle :` ;
- un contrôle d'existence avant l'appel ;
- le sens du code 1 ;
- P4 au lieu de P4/P5 ;
- un renvoi vers `openevidence.md`.

### Pourquoi
Le chemin écrit aujourd'hui (`SynologyDrive`) n'existe pas sur ce poste. `node` sort alors en code 1,
que le circuit lit comme « réponse OE incomplète » : un chemin mort se déguise en réponse tronquée
(§13.1). Ce fichier est le **domicile** de l'usage du CLI. Les circuits et `openevidence.md` y
renvoient, et un changement de poste ne se corrige qu'ici.

### Décision clé
- Chemin : `C:\Users\Kovu\Projets\Interface-OE`, CLI `out/cli/index.js`, constaté le 2026-09-24. Il
  n'apparaît qu'**une fois** dans le fichier (une variable de lecture, `<CLI>`, pour le reste).
- Avant tout appel : vérifier que `<CLI>` existe. Absent → message explicite, **pas** « incomplet ».
- Règle d'accord : renvoi à `openevidence.md` (règle écrite, pas de hook, arbitrage du 2026-09-24).

### Lire / Modifier
- Modifier : `docs/commun/OUTIL-INTERFACE-OE.md`.

### Étapes
1. Réécrire les sections Prérequis, La commande, Ce qu'on récupère, Où ça s'insère (l.23-89) selon
   la décision clé ; options et codes pris dans `utiliser-le-cli.md` d'Interface-OE, pas de mémoire.
2. Contrôler les occurrences du chemin (voir Validation).

### Validation
- **N0 auto (bloque le commit)** : `node .claude/workflow/bin/n0.mjs` → exit 0.
- **Tests** : — (mode d'emploi, aucune logique).
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : remplacer `SynologyDrive` par le nouveau chemin partout où il apparaissait
  laisse plusieurs chemins en dur, qui divergeront au prochain changement de poste. Contrôles :
  - `grep -rn "Interface-OE/out/cli\|Interface-OE\\\\out" docs/ .claude/skills/` → une seule ligne,
    dans `OUTIL-INTERFACE-OE.md` ;
  - `grep -rn "SynologyDrive" docs/commun .claude/skills` → 0.

### Si bloqué
—

### Message de commit (appliqué par la session elle-même)
`docs(commun): mode d'emploi OE — chemin unique, --modele, contrôle d'existence`
Dernière ligne du commit, obligatoire : `Plan: P16/S9/T16`

---

## T17 — Renvois, tri-boite-mail, carte du projet

### Objectif
Aligner sur les circuits réécrits les documents vivants qui décrivent les skills. `tri-boite-mail`
relève le DOI et le numéro d'essai de chaque article suivi. Ajouter à `PROJECT_MAP.md` :
- les scripts et les références du socle ;
- les agents dédiés ;
- le corpus d'épreuve.

### Pourquoi
Les noms des skills n'ont pas changé ; ce qu'elles font, si. Exemple : `00-global.md` l.25-27 annonce
encore des « gabarits de prompt Agent A / OpenEvidence / red-team B », qui n'existent plus. Un renvoi
qui décrit l'ancien fonctionnement fait redériver la procédure de travers. Relever DOI et NCT dès le
tri, c'est le lot 1 de §7 : `identite.mjs` peut alors partir d'un identifiant sûr.

### Décision clé
- Un renvoi ne décrit que **ce que la skill encode** et **quand l'invoquer**, jamais sa procédure.
  Une phrase de plus deviendrait un second domicile.
- `tri-boite-mail` : ajouter le relevé DOI et NCT (quand l'article les porte) à la sortie par
  article. Aucun autre changement.

### Lire / Modifier
- Modifier :
  - `CLAUDE.md` (l.44-49) ;
  - `docs/decision/00-global.md` (l.20-30) ;
  - `docs/decision/CONSTRUIRE-UN-MODULE.md` (l.218-222 seulement) ;
  - `docs/veille/SOP_veille.md` (l.72-76, l.221-225) ;
  - `docs/veille/TRI_BOITE_MAIL.md` (l.7-10) ;
  - `.claude/skills/tri-boite-mail/SKILL.md` ;
  - `PROJECT_MAP.md`.

### Étapes
1. Pour chaque occurrence, trancher : document vivant → aligner ; archive → laisser (Hors périmètre).
   Le bilan liste chaque occurrence et ce qui en a été fait.
2. Modifier `tri-boite-mail`.
3. Mettre à jour `PROJECT_MAP.md` (localiser, pas expliquer).

### Validation
- **N0 auto (bloque le commit)** : `node .claude/workflow/bin/n0.mjs` → exit 0 (dont
  `signatures.test.mjs`, qui couvre `00-global.md`, `SOP_veille.md` et `CLAUDE.md`).
- **Tests** : — (renvois documentaires).
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : un grep des noms de skills qui ne trouve plus rien d'incohérent parce qu'on
  n'a cherché que les noms, pas les descriptions périmées (« gabarits de prompt », « red-team B »,
  « P5 »). Contrôle :
  - `grep -rn "gabarits de prompt\|red-team B\|étape P4/P5" CLAUDE.md docs/decision/00-global.md docs/decision/CONSTRUIRE-UN-MODULE.md docs/veille/SOP_veille.md docs/commun/OUTIL-INTERFACE-OE.md`
    → aucune ligne qui décrit le circuit actuel.

### Si bloqué
—

### Message de commit (appliqué par la session elle-même)
`docs: renvois alignés sur les circuits refondus, relevé DOI/NCT au tri`
Dernière ligne du commit, obligatoire : `Plan: P16/S9/T17`

---

## Bilan de session
<fichiers · table des occurrences (fichier:ligne → aligné / archive laissée) · ancien chemin dans
Interface-OE signalé · N0 · prochaine action>

## Fin de session
Dérouler `/fin-de-tache` (mode selon « parallèle : oui/non » du bandeau).

Bloquant : 0
Couverture : complète

## Bloquants
- aucun

## Backlog
- docs/commun/epreuves-recherche/harnais/lancer.mjs:296-334 (`commandeMesurer`) — aucune garde
  contre un second `mesurer` lancé pendant qu'un lot précédent tourne encore (`etat.termine` faux) :
  le second appel réécrit `lot-courant.json` sans avertissement, le premier lot continue en
  arrière-plan (`unref()`), orphelin et invisible à `attendre` — exactement le scénario que le
  design (commentaire d'en-tête) dit vouloir éviter. Non rencontré dans cette session (usage
  séquentiel discipliné, T5/T6), mais rien dans le code n'empêche une future session (S10, S12) de
  le déclencher par erreur (mauvais commit, relance) et de perdre le suivi d'exécutions payantes.
- docs/commun/epreuves-recherche/harnais/lancer.mjs:288 (`fileDExecutions`) — `const executions =
  execution ? [execution] : [1, 2];` : `--execution 0` serait silencieusement traité comme absent
  (0 est falsy) et ferait tourner les exécutions 1 ET 2 au lieu de rejeter l'entrée invalide. Sans
  conséquence réelle ici — les seules valeurs légitimes sont 1, 2, 3 — mais masquerait une erreur de
  frappe plutôt que de la signaler.
- docs/commun/epreuves-recherche/harnais/cas.test.mjs:45 — le titre du test dit « E01 : trois
  assertions » alors que l'assertion vérifiée est `resultatAttendu.length` === 4 ; intitulé périmé,
  pas de conséquence fonctionnelle.

## Notes de couverture
- Ordre de lecture respecté : le correctif localisé (`fb96bc4`, repère `Correctif localisé :`) lu et
  vérifié en premier — portée exactement conforme à la décision clé T5 (« tentative » = entrée
  `tool_use` pour dépôt/Interface-OE, entrée + résultat pour `github.com/kovuthecat`), test de
  régression ajouté et cohérent avec l'incident décrit dans le bilan.
- Objectif/Validation de T5 et T6 confrontés au diff : conformes. Anti-raccourci T5 (verdict lu dans
  les résultats d'outils, pas la réponse du modèle) vérifié dans `transcription.mjs` et le canari
  (dix gestes, table du bilan cohérente avec `analyserCanari`). Anti-raccourci T6 (chaque ligne de
  table cite ses exécutions et `grading.json`) vérifié dans `rapport.md` — conforme.
- `verdicts.mjs` relu : règle 2/2, 0/2, 1/2→3e, 2/3, 1/3 correcte et cohérente avec le verdict E05
  du rapport (1/3 → échoué).
- Trouvaille notable, hors périmètre de S4 : `docs/commun/epreuves-recherche/harnais/signatures.test.mjs`
  échoue actuellement à HEAD (5 assertions, signatures de cas trouvées dans
  `.claude/skills/recherche-source-primaire/scripts/identite.test.mjs`) — mais ce fichier de test
  whitelisté a été créé intégralement par le commit `0ac434a` (P16/S5, postérieur à S4). Le garde-fou
  écrit par S4/T5 fonctionne comme prévu ; le défaut à corriger appartient à la revue de S5, pas à
  celle-ci.
- Fichiers de données de la mesure (`mesures/2026-09-24-reference/E0*/execution-*/{enregistrement.json,
  grading.json,livrable/*.md}`) non relus ligne à ligne (données/sorties d'exécution) ; `rapport.md`
  et `resultats.json` relus et confrontés au code de calcul des verdicts.

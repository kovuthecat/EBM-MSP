Bloquant : 0
Couverture : complète

## Bloquants
- aucun

## Backlog
- docs/commun/epreuves-recherche/harnais/export.mjs:238 — `construireReglages({ scriptsRecherchePrimaireExistent })` garde un paramètre qui n'influence plus jamais `allow` (correctif localisé documenté, `void` explicite) ; à terme, si aucun appelant n'exploite plus la distinction, la signature pourrait être simplifiée — mineur, le commentaire explique déjà pourquoi elle est conservée.
- plans/P16/S10.md (Écarts au plan, « Note d'exécution ») — affirme que S10.md et les données T19 restent non commités à l'issue de la session ; en réalité tout est commité (885bd80, 3h après). Texte devenu factuellement faux après coup (probablement committé par une reprise/orchestrateur sans mise à jour de la note) — sans conséquence sur le contenu mesuré, mais à corriger si quelqu'un relit cette note plus tard pour savoir quoi committer.

## Vérifications faites
- Commits `Correctif localisé :` (0e8a130, T18) et `Amendement :` (Veille exclu, arrêt de coût) lus en premier, comme demandé.
- Correctif Bash (`construireReglages`) relu dans `export.mjs` : cohérent avec la justification donnée (Bash binaire tout-ou-rien dans le CLI utilisé), appliqué uniformément aux 3 configurations (aucun biais différentiel), testé (`export.test.mjs`, réécrit pour attendre l'absence de motif Bash même quand les scripts existent).
- `node .claude/workflow/bin/n0.mjs --cible docs/commun/epreuves-recherche/harnais/export.test.mjs` → PASS.
- Anti-raccourci T18 (transcription doit montrer les `subagent_type` exacts) : vérifié dans `mesures/2026-09-24-deroule/rapport.md` — table des 3 agents dédiés, livrables listés existent tous sur disque.
- Anti-raccourci T19 (config 1 non remesurée, citée avec sa date/source S4) : `estimation.md` et `rapport.md` citent explicitement `mesures/2026-09-24-reference/resultats.json` (S4) pour la config 1, jamais remesurée.
- Table des verdicts du rapport (`rapport.md`) confrontée aux `grading.json` bruts pour E02 (4/4 partout) et E03 (1/4, 1/4, 0/4, 0/4) : chiffres identiques. E08/config-2/exécution-1 « 0 livrable produit » confirmé (dossier `livrable/` vide sur disque).
- `resultats.json`/`estimation.md` cohérents entre eux et avec le rapport (coûts, plafond, sous-ensemble de cas).
- Écarts au plan (deux amendements : exclusion Veille, arrêt de coût) : chacun sert toujours l'objectif de la session (mesurer le circuit Décision) et le rapport se marque explicitement comme partiel, sans conclure au-delà des données — pas de bloquant.
- Fichiers de données volumineux (`enregistrement.json`, `grading.json`, livrables `.md` des exécutions) non lus intégralement un par un — sondés par échantillon pour croiser les affirmations du rapport, conformément au périmètre (fichiers de mesure, pas du code applicatif).

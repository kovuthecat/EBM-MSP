Bloquant : 0
Couverture : complète

## Bloquants
- aucun

## Backlog
- .claude/skills/recherche-source-primaire/scripts/verifier-registre.mjs:136 — `verifierLigneVerifiee`
  ne contrôle que Localisation et Accès pour une ligne `vérifiée` ; une ligne `vérifiée` avec `Étude`
  vide (`—`) passe sans erreur. Le plan (Décision clé T8) ne demandait que ces deux contrôles, donc
  ce n'est pas un écart à l'objectif — mais laisser passer une « vérifiée » sans DOI/PMID va à
  l'encontre du motif même du registre (§14.2). À ajouter si le format évolue.
- .claude/skills/recherche-source-primaire/scripts/verifier-registre.mjs:206 — la comparaison du NNT
  recalculé fait `Math.round(nntEcrit)` avant de comparer à `nntCalcule` : un NNT écrit non entier
  (ex. `NNT 39.4`) qui arrondit au bon entier passe sans erreur, alors qu'un NNT n'est par nature pas
  fractionnaire. Cas non couvert par les tests (`verifier-registre.test.mjs` ne teste que des écarts
  entiers).
- .claude/skills/recherche-source-primaire/scripts/identite.mjs:381-382 — `appelUnpaywall` et
  `appelCrossref` sont deux appels réseau indépendants mais awaités séquentiellement ; un
  `Promise.all` diviserait la latence par deux sur ces deux appels. Non bloquant (outillage de
  recherche, pas de contrainte de latence documentée).

Vérifications faites : Objectif/Validation de T7 et T8 confrontés au diff (aucun écart) ; ligne
Anti-raccourci de chaque tâche vérifiée dans le code — fixtures T7 portent URL/date d'enregistrement
réel (une seule fabriquée, explicitement marquée, seule utilisée pour le test de champ absent) ;
test T8 porte bien un NNT dans `Affirmation`/`Donnée extraite` sans passer seulement par `Calcul`.
N0 ciblé rejoué (`identite.test.mjs`, `verifier-registre.test.mjs`) → PASS. Aucun fichier `Hors
périmètre` touché (pas de dépendance npm, `package.json` inchangé, aucun autre fichier de la skill
que `scripts/` et la section Format de `registre-affirmations.md`, `STATUS.md`/`TASKS.md`/
`VALIDATION.md`/`index.md` intouchés — cohérent avec la vague verrouillée). Aucun commit
`Correctif localisé :` / `Amendement :` dans cette session ; pas de section « Écarts au plan » dans
`S5.md`.

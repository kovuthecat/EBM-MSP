# P16 · S6 — Socle : accès, registre, OpenEvidence, leçons, SKILL.md   (rédigé par Opus)

> **Modèle : Opus · effort : high · Vague : 4 (parallèle : oui)**
> **Environnement : indifférent** (`WORKFLOW.md` §6, N1)
> **Régime : fixé**
> **Latitude : moyens libres ; objectif, contrats publics et zone du plan fixes**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Tu t'arrêtes sur un choix que tu n'as pas reçu, pas parce que
> quelque chose a cassé : `EXECUTANT.md`.

- Date : 2026-09-24 · Branche : —

## Lire (commun à la session — point de départ, pas une clôture)
`.claude/workflow/EXECUTANT.md` (toujours en premier), puis :
- `docs/commun/2026-09-16-propositions-skills-recherche.md` : P0 (l.68-96), P1 (l.98-136), P2
  (l.138-144), §6 (l.146-184), §13.2 (l.404-470), §13.4 N1-N5 (l.514-546), §14 (l.599-698).
- `.claude/skills/recherche-source-primaire/SKILL.md` (actuel, 131 l.) — ce qui se déplace, ce qui
  se supprime.
- `.claude/skills/recherche-source-primaire/scripts/identite.mjs` (en-tête, sorties) et
  `references/registre-affirmations.md` § Format — livrés par S5.
- `docs/decision/00-global.md` l.20-30 et l.95-130 — domicile des principes et des règles de
  sourcing : on y **renvoie**, on ne les recopie pas.
- `docs/veille/SOP_veille.md` §7 (l.221), §7bis (l.235), §10 (l.314) — idem pour la veille.
- `docs/commun/OUTIL-INTERFACE-OE.md` — domicile de l'usage du CLI (chemin, options, codes) ; S9 le
  met à jour.
- `docs/commun/epreuves-recherche/mesures/*-reference/rapport.md` — mesure de référence (S4).
- `docs/commun/epreuves-recherche/README.md` (table des cas) — pour relier leçons et cas **par
  identifiant seulement**.

## Hors périmètre
`references/contradiction.md` et `references/consolidation.md` sont écrites par S7 en parallèle : les
nommer, ne pas les créer. Ne pas toucher la section « Format » de `registre-affirmations.md` (contrat
de `verifier-registre.mjs`, S5). Aucun circuit, aucun agent (S8). **Aucune signature du corpus dans le
socle** : ni nom d'étude, ni chiffre, ni fait correctif d'un cas — le test `signatures.test.mjs` (N0)
le refuse, et c'est voulu (le socle apprendrait le test ; constat 2 du critique). Vague verrouillée :
ne pas committer, ne pas toucher `STATUS.md`, `TASKS.md`, `VALIDATION.md`, `index.md`.

---

## T9 — Références « accès et identité » et « registre »

### Objectif
Écrire `references/acces-identite.md` et compléter `references/registre-affirmations.md` de sa marche
à suivre.

### Pourquoi
Ce sont les deux erreurs de §13.3 qu'un appel d'API ou une porte suppriment : l'accès mal qualifié et
l'identité d'étude fausse d'un côté, l'affirmation non soutenue et le chiffre dérivé de l'autre. Les
agents (S8) chargeront ces pages ; elles disent **comment** faire, et renvoient au **pourquoi** dans
`00-global.md` (une règle, un domicile, §14.2).

### Décision clé
- `acces-identite.md` porte :
  - les six états d'accès de P0 (`non récupéré`, `échec technique`, `résumé accessible`,
    `texte intégral accessible`, `paywall constaté sur cette voie`, `accès restant à vérifier`) —
    **domicile du vocabulaire** ;
  - la distinction transport / document récupéré / résumé généré ;
  - les trois axes (identité, accès au passage, solidité de l'inférence) ;
  - l'appel à `identite.mjs` et la lecture de sa sortie ;
  - les voies ouvertes (§13.2) ;
  - le premier geste de tout circuit : constater les outils réellement exposés et écrire
    `Outils disponibles : …` (N3), avec des outils nommés `Serveur:outil` ;
  - la marche à suivre avant un verdict d'absence (corpus local `docs/decision/sources/`, deux
    méthodes d'extraction ; la règle et son incident restent dans `00-global.md` l.112-122) ;
  - la marche à suivre « avant de reporter pour inaccessibilité », qui y devient domiciliée ;
  - en fin de page, une section **« Anciens usages et fiches datées »** : tarifs, volumes, noms
    d'outils de l'ancien SKILL.md, chacun avec sa date — rien de non daté ne reste dans le corps.
- `registre-affirmations.md`, après la section Format (inchangée) :
  - quand et comment remplir le registre ;
  - l'appel à `verifier-registre.mjs` comme porte avant de déclarer un dossier consolidé ;
  - familles d'essai (`identite.mjs`, `[si]`) : une famille n'est pas N confirmations ;
  - chiffres dérivés (publié / recalculé / non calculable ; HR, RR, OR non interchangeables) ;
  - **journal de recherche rejouable** (P1 : base, requête exacte, date, filtres, résultats affichés
    et examinés, retenus et exclus avec motif, « non disponible » si l'outil ne donne pas de total) ;
  - **réutilisation d'un dossier** (P2 : date de dernière recherche, périmètre, événements qui
    imposent une mise à jour).

### Lire / Modifier
- Créer : `.claude/skills/recherche-source-primaire/references/acces-identite.md` (nouveau fichier).
- Modifier : `.claude/skills/recherche-source-primaire/references/registre-affirmations.md` (ajout
  après la section Format).

### Étapes
1. Écrire `acces-identite.md` dans l'ordre de la décision clé ; chaque règle dont le principe vit
   ailleurs renvoie à `fichier:section` au lieu de le reformuler.
2. Compléter `registre-affirmations.md`.
3. Contrôle « une règle, un domicile » : les formulations distinctives de `00-global.md` l.99-130 ne
   figurent pas dans le socle (voir Validation).

### Validation
- **N0 auto (bloque le commit)** : `node .claude/workflow/bin/n0.mjs` → exit 0 (dont
  `signatures.test.mjs`).
- **Tests** : — (texte de référence ; ses deux portes mécaniques sont `signatures.test.mjs` et la
  commande ci-dessous).
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : recopier la règle de `00-global.md` en la paraphrasant passe tout grep de
  phrase exacte. Contrôle : `grep -rniE "PMID sur|Minerva|Exercer|Prescrire" .claude/skills/recherche-source-primaire/references/`
  → aucune ligne qui énonce la règle ; et `grep -c "00-global.md" .claude/skills/recherche-source-primaire/references/acces-identite.md`
  → au moins 2 renvois.

### Si bloqué
—

### Message de commit (appliqué par la session elle-même)
`docs(recherche): références accès-identité et registre du socle`
Dernière ligne du commit, obligatoire : `Plan: P16/S6/T9`

---

## T10 — Référence OpenEvidence, leçons, amendement de D65

### Objectif
Écrire `references/openevidence.md` et `references/lecons.md` ; amender D65 (garde-fou OE refusé,
règle écrite) dans `DECISIONS.md` et dans §14.3 du rapport.

### Pourquoi
Sans hook (arbitrage du 2026-09-24), la seule barrière entre un agent et une requête OE sur le compte
du référent est une règle écrite : elle doit être à un seul endroit, nette, et chargée par tout
circuit qui touche OE. `lecons.md` est le garde-fou contre la dérive : chaque incident y pointe vers
sa règle et son domicile — la prochaine divergence skills/`00-global.md` se verra.

### Décision clé
- `openevidence.md` porte la **marche à suivre dans un circuit** (§14.2, §6) :
  - **règle écrite, sans hook** : aucune question OE sans accord du référent donné dans la
    conversation, avec k questions, modèle et motif annoncés ; les questions OE sont regroupées dans
    **une étape dédiée** du circuit ; **aucun agent** n'appelle le CLI — l'orchestrateur seul, après
    accord ;
  - choix du modèle (Sackett pour des sous-questions groupées, Snow pour une question large ou une
    lacune ciblée, Osler presque jamais), sans montée automatique ; ce sont des hypothèses à éprouver ;
  - ligne `Modèle :` comparée à la demande ;
  - **demande de précision** détectée par le circuit (Interface-OE sort en code 0 sans la repérer)
    → statut `clarification requise`, relance par `--conversation` seulement avec accord ;
  - fiche de retour (modèle demandé et observé, date, prompt, lien, neuf ou poursuivi, durée,
    complétude, références, apport après vérification) ;
  - renvois : PMID et liste d'exclusion → `00-global.md` l.103-111 et l.123-129 ; chemin, options et
    codes du CLI → `docs/commun/OUTIL-INTERFACE-OE.md`.
- `lecons.md` : une table `Incident (date, chemin du rapport) · Règle · Domicile (fichier:section) ·
  Cas d'épreuve (identifiant E<nn>)`. **Aucun nom d'étude, aucun chiffre, aucun fait correctif** : le
  rapport se désigne par son chemin, le cas par son identifiant.
- D65 : la ligne du registre remplace « garde-fou OE (hook de confirmation) recommandé, en attente
  d'accord » par « garde-fou OE : hook refusé le 2026-09-24, règle écrite (`openevidence.md`) » ; §14.3
  du rapport reçoit en tête une ligne d'arbitrage datée. Aucune nouvelle ligne dans `DECISIONS.md`.

### Lire / Modifier
- Créer : `.claude/skills/recherche-source-primaire/references/openevidence.md`,
  `.claude/skills/recherche-source-primaire/references/lecons.md` (nouveaux fichiers).
- Modifier : `DECISIONS.md` (ligne D65, l.79), `docs/commun/2026-09-16-propositions-skills-recherche.md`
  (§14.3, l.665).

### Étapes
1. Écrire `openevidence.md`.
2. Écrire `lecons.md` à partir des incidents de §13.3 et des règles de `00-global.md` l.99-130
   nées d'un incident ; colonne « Cas » depuis la table du README du corpus.
3. Amender D65 et §14.3.

### Validation
- **N0 auto (bloque le commit)** : `node .claude/workflow/bin/n0.mjs` → exit 0 (dont
  `signatures.test.mjs`, qui couvre `lecons.md`).
- **Tests** : — (texte ; porte mécanique : `signatures.test.mjs`).
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : une consigne OE du type « demander l'accord du référent » sans dire **qui**
  appelle le CLI laisse un agent muni de Bash s'en croire autorisé. Contrôle :
  `grep -n "agent" .claude/skills/recherche-source-primaire/references/openevidence.md` montre la
  phrase qui réserve l'appel à l'orchestrateur.

### Si bloqué
—

### Message de commit (appliqué par la session elle-même)
`docs(recherche): référence OpenEvidence, leçons, amendement D65`
Dernière ligne du commit, obligatoire : `Plan: P16/S6/T10`

---

## T11 — `SKILL.md` du socle

### Objectif
Réécrire `.claude/skills/recherche-source-primaire/SKILL.md` en page d'entrée courte : quand la
charger, quelle référence ouvrir pour quel besoin, quels scripts appeler.

### Pourquoi
Le SKILL.md actuel mêle procédure, tarifs datés, noms d'outils ambigus (`search_trials` pour deux
serveurs, §13.2) et un renvoi P5 faux (l.8-9). En page d'entrée, il ne charge que ce dont le circuit a
besoin (chargement progressif) et ne contient plus rien qui périme sans date.

### Décision clé
- Nom et `description` conservés dans l'esprit (onze fichiers citent la skill ; la description peut
  être précisée, pas le nom).
- Corps : rôle (socle chargé par les circuits, rarement seul) ; premier geste (outils disponibles,
  renvoi `acces-identite.md`) ; table « besoin → référence » (accès et identité, registre et journal,
  contradiction, consolidation, OpenEvidence, leçons) ; les deux scripts et leur commande ; le
  garde-fou central (source primaire obligatoire, IA = repérage) **par renvoi** à `00-global.md`
  l.20-30.
- P4 et non P5 pour la collecte (`CONSTRUIRE-UN-MODULE.md` l.218).
- Cible : moins de 80 lignes.

### Lire / Modifier
- Modifier : `.claude/skills/recherche-source-primaire/SKILL.md`.

### Étapes
1. Vérifier que tout contenu de l'ancien SKILL.md a un domicile (une référence, `00-global.md`, la
   SOP, ou la section « Anciens usages » d'`acces-identite.md`) avant de le retirer.
2. Réécrire le SKILL.md.

### Validation
- **N0 auto (bloque le commit)** : `node .claude/workflow/bin/n0.mjs` → exit 0.
- **Tests** : — (texte ; portes mécaniques ci-dessous).
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : un contenu retiré de l'ancien SKILL.md sans domicile disparaît sans bruit.
  Contrôle : `git show HEAD:.claude/skills/recherche-source-primaire/SKILL.md` section par section,
  avec dans le bilan une ligne `section → nouveau domicile`. Et `grep -n "P5" .claude/skills/recherche-source-primaire/SKILL.md`
  → aucune ligne qui désigne la collecte.

### Si bloqué
—

### Message de commit (appliqué par la session elle-même)
`docs(recherche): SKILL.md du socle recherche-source-primaire`
Dernière ligne du commit, obligatoire : `Plan: P16/S6/T11`

---

## Bilan de session
<fichiers · table « section de l'ancien SKILL.md → nouveau domicile » · résultats des greps de
contrôle · N0 · prochaine action>

## Fin de session
Dérouler `/fin-de-tache` (mode selon « parallèle : oui/non » du bandeau).

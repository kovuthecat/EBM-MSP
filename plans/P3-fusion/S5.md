# S5 · T‑024 — Câblage app (3 onglets → 1) + tests + retrait B/C/D

**Modèle/effort** : Sonnet · medium. **Dépend de** : S4 (0 HAUTE).
**Sortie** : `src/…`, cross‑refs de contenu, retrait des YAML B/C/D. **Build + typecheck + Vitest verts.**

## But

Rendre le nœud fusionné dans l'app, retirer B/C/D, sans régression. **Aucune valeur clinique touchée ici.**

## Étapes

1. **Navigation** : le nœud `prescription` remplace, dans le domaine `diabete-type-2`, les 3 entrées
   première‑intention / intensification / sulfamides‑gliptines. A (cible), E (insuline), F (statine),
   H (RHD) **inchangés**. Vérifier [`navigation.ts`](../../src/features/shared/navigation.ts) /
   [`loadNodes.ts`](../../src/features/decision/content/loadNodes.ts) : le chargement reste **générique**
   (piloté par le contenu, aucun nom de nœud en dur — invariant 5).
2. **Formulaire** : les nouveaux critères (`position_vs_cible`, `intolerance_traitement`, `infections_uro…`,
   `denutrition_ou_perte_poids`, flags dérivés) se rendent via [`CriteriaForm.tsx`](../../src/features/decision/components/CriteriaForm.tsx)
   sans code spécifique. Ajouter les **libellés** dans [`labels.ts`](../../src/features/decision/lib/labels.ts).
   Le rendu des alertes (D15) passe déjà par `AlertList`.
3. **Retrait B/C/D** : supprimer les 6 fichiers (`.yaml` + `.argumentaire.md`) de première‑intention /
   intensification / sulfamides‑gliptines **après** bascule (git conserve l'historique). Purger
   `content.test.ts` des attentes obsolètes.
4. **Cross‑refs internes** à mettre à jour (chaîne « voir nœud B/C/D ») : `insuline.yaml`/argumentaire
   (chaînage), `rhd`/`statine` si mention, `PROJECT_MAP.md`, `ARCHITECTURE.md`, `docs/decision/00-global.md`,
   `CADRAGE-8-noeuds.md`, `carte-coherence.md`.
5. **Tests unitaires** : promouvoir les tests‑profils de S3 en suite de non‑régression du nœud fusionné
   (fichier `evaluateNode.prescription.test.ts`), profils frail/IMC‑bas/uro/sous‑cible inclus.
6. **VALIDATION.md** : consigner la validation **visuelle humaine** (jamais Playwright — cf. règle projet) :
   l'onglet unique s'affiche, primer en tête, options triées, alertes rendues.

## Critère de sortie

`npm run build` + `npx tsc --noEmit` + `npm test` **verts** ; un seul onglet de prescription ; B/C/D retirés ;
cross‑refs propres ; tests‑profils au vert. Statut YAML toujours `brouillon` (passe à `valide` en S6).

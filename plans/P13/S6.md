# P13 · S6 — Un nœud publie sa conclusion (`exports`)   (rédigé par Opus)

> **Modèle : Sonnet · effort : xhigh · Vague : 3 (parallèle : oui — avec S7)**
> **Environnement : Desktop (navigateur in-app requis — l'objet même est un enchaînement de nœuds)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-04 · Branche : — · **Dépend de S5** (même fichier `sessionCriteres.ts`)

## ⚠ Cette session est DROPPABLE, et c'est écrit dans le plan

C'est la seule tâche de P13 qui **ajoute un concept au modèle de contenu**. Si elle dérape — et le
premier signe de dérapage est de devoir toucher `engine/` —, le plan se clôt sans elle. D12/D17
restent alors ouverts : coûteux en confort de saisie, jamais en sécurité. **Ne t'acharne pas.**

## Le défaut, en une phrase

Le praticien vient de faire calculer « Cible ≤ 7 % » par le nœud voisin ; deux clics plus tard, il
doit juger à la main « Par rapport à l'objectif ». La mémoire de session partage des **critères**
(des entrées), jamais des **conclusions** (des sorties de nœud) — la cible que l'outil vient
d'afficher n'existe nulle part comme valeur réutilisable.

## Lire (commun à la session)

- `docs/decision/validation/revue-conception-fable-2026-08-04.md` — §1 point 6, §7 **P1**, §9 point 1,
  et §11 dernier point (**le comportement face au retour arrière**, que cette session doit spécifier).
- `docs/decision/GRAMMAIRE-NOEUD.md` — **R1 en entier**, et surtout sa précision du 2026-08-04
  (« la question directe peut être pré-remplie, jamais escamotée »). **C'est la règle que cette
  session frôle de plus près de tout le plan.** Si tu ne vois pas clairement pourquoi `exports` ne
  viole pas R1, ne code rien : relis, puis signale ton doute.
- `src/features/decision/lib/sessionCriteres.ts` — **en entier**, en particulier la docstring de tête
  (les trois propriétés qui rendent la mémoire de session compatible avec R1) et son point 1 :
  *« ce qui circule est une VALEUR SAISIE, jamais une CONCLUSION du moteur »*. **Cette phrase dit
  littéralement le contraire de ce que la tâche demande** — c'est le cœur de la difficulté, traité
  ci-dessous ; tu devras la réécrire, pas la contourner en silence.
- `src/features/decision/lib/formLayout.ts` — `appliquerPreremplissage` (le mécanisme d'accueil) et,
  **si S1 est livrée**, sa version étendue aux listes.
- `src/features/decision/lib/vueDecision.ts` — comment une conclusion est disponible côté écran
  (`VueDecision.familles`, les options applicables) : c'est la matière de l'export.
- `content/decision/noeuds/diabete-type-2/cible-glycemique.yaml` — les options (les paliers de cible)
  et le critère `HbA1c_cible` s'il existe déjà ; `prescription.yaml` — `position_vs_cible` et son
  commentaire (une tentative de `derive` d'`enum` y a déjà été écartée : **lis pourquoi**).

## Hors périmètre

- **Ne touche pas à `engine/`.** Un `export` est une lecture de la vue, versée en session par
  l'écran — jamais une sortie du moteur qui reboucle dans le moteur.
- Ne rends aucun nœud prérequis d'un autre (R1) : chaque nœud reste évaluable seul, et un export
  absent ne bloque rien.
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `index.md`, ni `VALIDATION.md`. Ni commit ni push.

---

## T-149 — `exports` : un nœud verse sa conclusion en session, comme une valeur suggérée

### Objectif

Qu'une conclusion rendue par un nœud devienne disponible, en session, pour pré-remplir un critère
d'un autre nœud — **sans jamais la présenter comme un fait du patient**.

### Décision clé — pourquoi ce n'est pas une violation de R1

R1 interdit qu'un **état clinique** soit **dérivé** d'autre chose que d'une observation. Quatre
propriétés maintiennent `exports` du bon côté, et **chacune doit être vérifiable dans le code** :

1. **la valeur exportée entre par le canal du pré-remplissage**, jamais par `derive` : le critère
   destinataire reste `saisi`, gardé par `!renseignes.has(nom)` (un critère répondu n'est jamais
   écrasé) et par le `=== true` strict (rien ne se déduit d'une donnée manquante) ;
2. **elle est marquée à l'écran** — « · calculé, à vérifier », mention qui existe déjà. Le praticien
   voit qu'une valeur lui est proposée, et par quoi ;
3. **elle est modifiable**, et cesse d'être marquée dès qu'il y touche ;
4. **elle ne franchit jamais la frontière d'un patient** : la mémoire de session est vidée par
   « Nouveau patient » (D33) et par le rechargement.

C'est exactement le régime que le référent a accordé à K6 le 2026-07-27 (« si la cible est connue, le
module peut la reprendre et pré-remplir »). Ce que cette session ajoute, c'est que la source peut être
une **conclusion** et plus seulement une saisie. **Mets à jour la docstring de `sessionCriteres.ts`
en conséquence** — son point 1 devient faux tel qu'il est écrit, et un commentaire qui ment est pire
que pas de commentaire. La formulation juste : *ce qui circule est soit une valeur saisie, soit une
conclusion explicitement publiée par un nœud — dans les deux cas comme une proposition marquée,
jamais comme un fait acquis.*

### Forme au schéma

```yaml
# racine du nœud
exports:
  - critere: HbA1c_cible          # le NOM du critère de session alimenté
    quand: "<expression DSL>"      # la première règle vraie l'emporte (idiome de `priorite`/`preremplissage`)
    valeur: "7"                    # la valeur versée
```

**Pourquoi une liste de règles plutôt qu'un lien vers l'option retenue** : lier l'export à
« l'option gagnante » ferait dépendre le contenu de l'ordre de résolution du moteur, donc d'un
détail d'implémentation. Une règle explicite est relisible par le référent et testable seule.
**Si tu constates que le contenu de `cible-glycemique` ne permet pas d'écrire ces règles proprement,
c'est un signal d'arrêt légitime** : signale-le, ne force pas.

### Le retour arrière — à spécifier ET à tester

Le praticien modifie un critère après que la conclusion a été exportée. Règle retenue :

- **l'export suit la conclusion recalculée** : il est réécrit à chaque changement, jamais figé ;
- **la valeur pré-remplie non confirmée suit** l'export (elle n'a jamais été une réponse) ;
- **une valeur que le praticien a confirmée ne bouge pas** — elle est devenue sa réponse ;
- **rien ne remonte** : modifier le critère destinataire n'affecte pas le nœud source.

### Lire / Modifier

**Modifier** : `schema/decision/noeud.schema.json` (`exports` à la racine) ·
`src/features/decision/content/node.types.ts` · `src/features/decision/lib/sessionCriteres.ts`
(une fonction d'écriture des exports + **la docstring**) ·
`src/features/decision/screens/DecisionNodeScreen.tsx` (l'appel, à côté de `memoriserCriteres`).

### Étapes

1. Schéma + type. Le champ est **optionnel** : les 6 nœuds actuels restent valides sans lui.
2. Fonction d'écriture, en miroir de `memoriserCriteres` (même fichier, même style, mêmes garde-fous
   de compatibilité).
3. Branche l'appel là où `memoriserCriteres` est déjà appelé (`DecisionNodeScreen.tsx:557`), après le
   calcul de la vue.
4. **Tests vus rouges d'abord** : un export s'écrit en session ; la première règle vraie l'emporte ;
   une règle indéterminée n'exporte rien ; l'export est réécrit quand le critère source change ; il
   disparaît sur « Nouveau patient ».
5. **Vérifie qu'aucun nœud ne devient prérequis** : ouvrir `prescription` **sans** être passé par
   `cible-glycemique` doit se comporter **exactement** comme avant. Test explicite, nommé.

### Validation

- **N0 auto (bloque le commit)** : `npm test` · `npm run typecheck` · `npm run build` → verts.
  Le golden master **ne doit pas bouger** (le banc évalue nœud par nœud, sans session).
- **N1 visuel auto** : `—` à cette tâche (l'effet visible est en T-150).
- **N2 humain** : `—`.

### Message de commit

`feat(engine): un nœud peut publier sa conclusion en session (exports, T-149)`

---

## T-150 — La cible d'HbA1c pré-remplit la position, marquée et modifiable

### Objectif

Que l'enchaînement le plus fréquent du produit — fixer la cible, puis traiter — cesse de faire
recalculer de tête ce que l'outil vient d'afficher.

### Décision clé

`cible-glycemique` déclare ses `exports` (la cible qu'il vient de conclure) ; `prescription` déclare
un `preremplissage` sur `position_vs_cible` qui **compare l'HbA1c saisie à la cible de session**.

⚠ **Deux limites techniques à vérifier AVANT d'écrire le contenu** — elles peuvent réduire la portée
de la tâche, et il vaut mieux le savoir en trente minutes qu'en trois heures :

1. `preremplissage.quand` est une expression DSL évaluée sur les **critères du nœud courant**. Pour
   comparer `HbA1c_actuelle` à une cible venue de la session, il faut que la cible **soit un critère
   déclaré de `prescription`** (fût-il non saisissable / masqué). Vérifie comment
   `cible_atteinte` est calculé aujourd'hui : le mécanisme existe peut-être déjà, à un nom près ;
2. `position_vs_cible` est un `enum` à quatre valeurs, et son commentaire dans `prescription.yaml`
   signale qu'**une tentative antérieure de `derive` d'`enum` a été écartée**. Le pré-remplissage,
   lui, sait poser une valeur d'`enum` (garde-fou l.180) — c'est une voie différente et légitime.
   **Lis le commentaire pour ne pas refaire ce qui a déjà été jugé impossible par un autre chemin.**

**Si l'une des deux limites bloque : arrête-toi et rends la main avec le diagnostic.** T-149 seule a
de la valeur (le mécanisme existe, un contenu ultérieur l'utilisera) ; forcer T-150 contre le modèle
de données en aurait beaucoup moins.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/cible-glycemique.yaml` (les `exports` + bump +
changelog) · `content/decision/noeuds/diabete-type-2/prescription.yaml` (le `preremplissage` + bump +
changelog) · `engine/banc/coherence-inter-noeuds.test.ts` (l'invariant de l'étape 4).

### Étapes

1. Vérifie les deux limites ci-dessus. **Consigne le résultat avant de continuer.**
2. Écris les `exports` de `cible-glycemique` : une règle par palier de cible, avec un commentaire qui
   dit à quelle option chaque règle correspond.
3. Écris le `preremplissage` de `position_vs_cible`. **Les quatre valeurs doivent être couvertes ou
   explicitement non couvertes** — une position pré-remplie à moitié serait pire qu'aucune.
4. **Invariant I33** : *tout `exports[].critere` est déclaré comme critère `partage` par au moins un
   autre nœud du domaine* — sinon on exporte vers le vide, et personne ne le saurait. C'est la même
   famille que le « champ de schéma non consommé » de `CONSTRUIRE-UN-MODULE.md` §4.
5. **Vérifie au navigateur** : `cible-glycemique` (âge 64, ancienneté 11, RAS) → « Cible ≤ 7 % » →
   `prescription` → saisir HbA1c 7,7 → **attendu** : « Par rapport à l'objectif » pré-rempli
   « Au-dessus », marqué « · calculé, à vérifier », et modifiable. Puis modifie-le et vérifie que la
   mention disparaît.

### Validation

- **N0 auto (bloque le commit)** : `npm test` · `npm run typecheck` · `npm run build` → verts.
- **N1 visuel auto** : l'enchaînement complet de l'étape 5, dans les deux sens (avec et sans passage
  préalable par le nœud cible). → `/verif-visuelle`.
- **N2 humain** : **relecture référent obligatoire du contenu des `exports`** — ce sont des seuils
  cliniques encodés en règles. Consigne les règles écrites, telles quelles, dans `VALIDATION.md`.

### Si bloqué

Voir la décision clé : les deux limites techniques sont des motifs d'arrêt **légitimes et attendus**.
Rendre la main avec un diagnostic net vaut mieux qu'un contenu tordu pour contourner le modèle.

### Message de commit

`feat(content): la cible d'HbA1c pré-remplit la position vs objectif (T-150, I33)`

---

## Fin de session

Dérouler `/fin-de-tache` (mode **solo**) — sauf mode vague parallèle : ne toucher aucun fichier
partagé, ne rien committer, consigner bilan et points N2 **ici**.

**À consigner impérativement** : la nouvelle rédaction de la docstring de `sessionCriteres.ts`
(point 1), le résultat des deux vérifications de limites de T-150, et — si la session s'arrête en
cours — **où exactement**, pour que la reprise ne recommence pas le diagnostic.

---

## Bilan de session

**STOP avant tout changement de code. Aucune tâche livrée — ni T-149 ni T-150.** Diagnostic complet
ci-dessous, pour que la reprise n'ait pas à refaire l'investigation.

### Le blocage, précisément localisé

T-149 exige d'ajouter `exports` **à la racine** de `schema/decision/noeud.schema.json` (mandat explicite,
non négociable — c'est le seul mandat de schéma de cette session). Ce champ neuf tombe mécaniquement sous
**G1** (`src/features/decision/engine/banc/grammaire.test.ts`, lignes 44-106) : « toute propriété du
schéma est CLASSÉE dans `CHAMPS_DU_SCHEMA` » (`src/features/decision/engine/expressionsNoeud.ts`), sans
échappatoire — `proprietesDuSchema()` lit `Object.keys(schema.properties)` littéralement et compare à
`CHAMPS_DU_SCHEMA['(racine)']` ; un champ non classé fait échouer G1, donc `npm test`, donc N0.

Vérifié en lisant le code, pas supposé :

1. Le conteneur `exports` lui-même demanderait UNE ligne dans `CHAMPS_DU_SCHEMA['(racine)']`
   (`exports: 'inerte'`, même famille que `alertes`/`familles`/`contraintes`/`criteres_entree`, qui sont
   eux-mêmes des conteneurs inertes — leurs sous-définitions portent la classification réelle).
2. Si `{critere, quand, valeur}` devient une définition NOMMÉE (`$ref`, sur le modèle de
   `reglePreremplissage`, ce que la forme la plus propre et la plus relisible par le référent demande),
   **G1** exige aussi de classer `regleExport.quand` (`'affichage'` serait le classement cohérent — même
   famille que `reglePreremplissage.quand` : évalué hors du moteur de décision, aucun effet sur
   `signatureVue`) — et alors **G2** (même fichier, lignes 108-298) exige que `fragmentsDuNoeud()`
   VISITE réellement ce nouvel emplacement (un ajout de code dans `expressionsNoeud.ts`, pas seulement une
   entrée de table) et que le nœud synthétique de `grammaire.test.ts` porte un marqueur dédié pour le
   prouver — deux fichiers d'`engine/` à modifier, pas un seul.

J'ai cherché une voie qui évite entièrement `engine/` : déclarer `{critere, quand, valeur}` **en ligne**
sous `exports.items` plutôt que comme définition nommée évite le volet G2/marqueur synthétique — mais PAS
le volet G1, puisque le conteneur `exports` reste une clé de `schema.properties` à la racine, et
`CHAMPS_DU_SCHEMA['(racine)']` doit la connaître dans tous les cas. Il n'existe donc AUCUNE façon de tenir
le mandat exact de T-149 (« exports à la racine ») sans toucher au moins une ligne d'`engine/
expressionsNoeud.ts`. Contourner ce dernier point (le classer ailleurs, ou par un mécanisme différent de
`properties`) aurait été bricoler le garde-fou plutôt que le respecter — exactement ce que « STOP, ne
bricole pas » interdit. Je n'ai donc rien écrit dans le dépôt.

C'est le déclencheur d'arrêt nommé explicitement par cette session : « le premier signe de dérapage est de
devoir toucher `engine/` ». Confirmé, pas supposé.

**Le coût réel, pour une reprise qui déciderait d'autoriser le franchissement** : strictement mécanique et
sans risque pour R1 — aucune donnée de session n'entre par ce chemin dans `evaluateNode`/`deriveCritere`/
`conditions.ts`, la classification `CHAMPS_DU_SCHEMA`/`fragmentsDuNoeud` ne sert qu'au banc (extraction de
seuils numériques pour les profils de test), jamais à la sélection d'options. Une ligne + (si définition
nommée) une seconde ligne + un marqueur de test. Ce n'est pas R1 qui est en jeu ici, c'est la consigne
explicite de périmètre — que j'ai choisi de respecter à la lettre plutôt que d'en réduire la portée moi-même.

### Les deux vérifications de limites de T-150 (faites — elles ne dépendent pas du blocage ci-dessus)

**1. `preremplissage.quand` et la comparaison à une cible de session — PAS bloquant, mécanisme identifié.**
`evaluateCondition` (`src/features/decision/engine/conditions.ts`, `evaluateAtomic`, lignes ~177-208) sait
DÉJÀ comparer deux critères NUMÉRIQUES entre eux (extension du 2026-07-27 pour `contraintes`/K3) —
`HbA1c_actuelle >= HbA1c_cible` serait donc évaluable tel quel si `HbA1c_cible` était un critère de
`prescription`. Mais le DSL n'a AUCUNE arithmétique (`+ - * /` n'existe que dans `derive`,
`engine/deriveCritere.ts`) : le seuil que le référent avait donné le 2026-07-27 pour « nettement au-dessus »
(écart ≥ 1 point — changelog K6 de `prescription.yaml`, entrée « K6 (2e volet) ») ne s'exprime pas par une
simple comparaison var-vs-var, il faut un ÉCART. Mécanisme vérifié disponible SANS toucher `engine/` :
1) déclarer `HbA1c_cible` comme critère de `prescription` (`type: nombre`, `partage: true`, bornes 6-9.5
IDENTIQUES à `insuline.yaml` pour I19/I32), alimenté par la reprise de session — pas nécessairement
saisissable, un `visible_si` peut le masquer, rien ne l'exige visible ; 2) un critère DÉRIVÉ numérique
(`derive: "HbA1c_actuelle - HbA1c_cible"`, `type: nombre`) portant l'écart, comparé ensuite à des littéraux
(`>= 1`, `> 0 AND < 1`) dans le `preremplissage.quand` de `position_vs_cible` — exactement l'idiome déjà
utilisé par `insuline.yaml` pour `ecart_sous_objectif_cible` (`derive` arithmétique générique depuis
T-133). **Ce mécanisme suppose que T-149 (le canal `exports` qui alimente `HbA1c_cible`) soit livré — ce
qui n'est pas le cas.**

**2. `position_vs_cible` est un `enum` à 4 valeurs — PAS bloquant, `preremplissage` sait le faire.**
Vérifié dans `src/features/decision/lib/formLayout.ts` (`appliquerPreremplissage`, garde-fou ligne ~219) :
`if (critere.type === 'enum' && !(critere.valeurs ?? []).includes(regle.valeur as string)) continue` —
poser une valeur d'énumération par `preremplissage` est un chemin DISTINCT de `derive` (qui ne sait
produire qu'un booléen ou un nombre, jamais un `enum` — c'est bien ce que le commentaire de
`position_vs_cible` dans `prescription.yaml`, ligne ~236, écarte, et cet écart ne s'applique pas à
`preremplissage`). Chemin légitime, déjà éprouvé par le garde-fou de code, confirmé disponible sans
toucher `engine/`.

**Verdict global T-150** : aucune des deux limites techniques n'est bloquante EN ELLE-MÊME. Le blocage réel
est en amont, sur T-149 (le canal `exports`), pour la raison G1/G2 ci-dessus — T-150 est entièrement
dépendant de T-149 et tombe avec elle.

### Docstring proposée pour `sessionCriteres.ts` (point 1) — rédigée, NON appliquée au fichier

Le point 1 actuel (« ce qui circule est une VALEUR SAISIE, jamais une CONCLUSION du moteur ») devient faux
le jour où `exports` existe. Reformulation prête à l'emploi pour la reprise :

> **1. ce qui circule est une VALEUR SAISIE, OU UNE CONCLUSION EXPLICITEMENT PUBLIÉE par un nœud
> (`Noeud.exports`, T-149) — jamais une inférence silencieuse du moteur.** Ce n'est PLUS la distinction
> que R1 protège, R1 en protège une plus fine : dans les DEUX cas, ce qui entre ici est une PROPOSITION
> MARQUÉE, jamais un fait acquis. Un export n'est PAS une sortie d'`evaluateNode` qui reboucle dans le
> moteur — c'est une lecture de la VUE (le nœud publie ce qu'il vient de conclure), versée en session par
> l'ÉCRAN, exactement comme une saisie l'est déjà : elle traverse ensuite le MÊME canal que toute valeur de
> session côté nœud receveur — `preremplissage`, jamais `derive` (§ mécanisme ci-dessous), marquée
> « · calculé, à vérifier », modifiable, jamais imposée. Le moteur lui-même (`engine/`) ne sait toujours
> rien de la session : il continue de ne recevoir que des `Criteria` ordinaires, qu'ils viennent d'une
> saisie, d'une reprise ou d'un export.

Le reste de la docstring de tête (points 2-3, « QUELLES VALEURS CIRCULENT », le paragraphe ⚠ « ÉTAT GLOBAL
MUTABLE ») reste vrai tel quel et n'a pas besoin d'être retouché.

### N0 — état exact mesuré (avant toute modification de ma part ; aucun fichier touché par cette session)

`npm test` → **1134 tests passés, 2 ÉCHOUÉS, 11 skip** (1147 au total). `npm run typecheck`/`npm run
build` non relancés (aucun changement de code de ma part à valider).

**Écart avec l'état de départ annoncé** (« 1136 passés, 11 skip, 0 échec ») : deux échecs préexistants,
constatés au tout premier `npm test` de cette session, AVANT que je touche quoi que ce soit —
`src/features/decision/engine/banc/invariants-contenu.test.ts`, bloc **I24** (« chaque clé de `motifs`
désigne une branche réellement présente dans l'option ») :
- « nœud insuline : aucune clé de `motifs` ne pointe dans le vide » — 5 options d'`insuline.yaml` portent
  une clé `"mcg_disponible == false"` qui ne correspond à aucun terme `OR` réel de leurs
  `conditions`/`prerequis` ;
- « nœud statine : … » — 2 options de `statine.yaml` portent une clé `"statine_deja_en_place == false"`,
  même défaut.

**Mise en garde sur la fiabilité de cette mesure** : en fin de `npm test` (592 s), `git status` a montré un
diff FRAIS sur `src/features/decision/engine/evaluateNode.statine.test.ts` (référence explicite « T-153,
P13/S7, D6 ») absent de mon premier `git status` en tout début de session — la session **S7, parallèle à
celle-ci, modifie donc activement `statine.yaml` pendant que je testais**, dans ce même répertoire de
travail (pas d'isolation par worktree entre S6 et S7, apparemment). Les deux échecs I24 ci-dessus portent
précisément sur `statine.yaml` (`statine_deja_en_place == false`) et `insuline.yaml`
(`mcg_disponible == false`) : ils sont donc VRAISEMBLABLEMENT un **instantané intermédiaire du travail en
cours de S7** (contenu à moitié réécrit au moment précis de mon passage), pas une régression stable et
préexistante que je pourrais attribuer avec certitude à une session antérieure. Dans les deux cas, ce
n'est pas à corriger ici : `invariants-contenu.test.ts` est explicitement dans la liste des fichiers à ne
pas toucher pour S6, et `statine.yaml`/`insuline.yaml` ne sont pas dans mon mandat. Signalé tel quel, avec
cette réserve de fiabilité, pour que la reprise ne le prenne ni pour argent comptant ni pour un signal à
ignorer.

### Ce que couvre le test d'intégration N1 de remplacement (T-150 étape 5)

**Aucun test écrit** — T-150 n'a jamais été atteint (dépendance directe à T-149). Le N1 navigateur reste
donc entièrement DÉFÉRÉ, comme prévu par la consigne, mais sans même la couverture RTL/jsdom de
substitution : il n'y a rien à substituer tant que le mécanisme `exports` n'existe pas.

### Où reprendre

La question à trancher AVANT tout code, hors de cette session (« Ne t'acharne pas », je n'ai pas ce
pouvoir ici) : *le coût mécanique d'un edit dans `engine/expressionsNoeud.ts` (+ `grammaire.test.ts` si
`regleExport` devient une définition nommée) est-il acceptable pour T-149 ?* Si la réponse est oui, le
reste de la conception est fait et directement réutilisable :

- **Schéma** : `exports` = tableau de `{critere: string, quand: string, valeur: string}` à la racine du
  nœud (mêmes conventions que `reglePreremplissage`, `valeur` toujours une CHAÎNE scalaire — jamais de
  forme tableau, un export publie une conclusion scalaire).
- **`node.types.ts`** : `interface RegleExport { critere: string; quand: string; valeur: string }` +
  `Noeud.exports?: RegleExport[]`.
- **`sessionCriteres.ts`** : nouvelle fonction `exporterConclusions(criteresEntree, regles, criteria,
  renseignes)`, en miroir de `memoriserCriteres` (même fichier, même style) : calcule `derives`
  (`calculerCriteresDerives`) et `effectifs` (`determinesEffectifs`) comme `appliquerPreremplissage`,
  regroupe les règles par `critere` (plusieurs conclusions distinctes peuvent cohabiter dans un seul
  `exports`), et pour chaque groupe, PREND LA PREMIÈRE RÈGLE DONT `quand` EST STRICTEMENT VRAIE — avec un
  ARRÊT (pas un `continue`) dès qu'une règle est `INDETERMINE`, pour ne jamais deviner sur une donnée
  manquante en sautant à une règle suivante qui pourrait n'être vraie que par accident (c'est plus strict
  que `appliquerPreremplissage`, qui utilise `.find()` — un choix délibéré, documenté, pour les exports).
  Sentinelle `quand: "default"` supportée EN PLUS (pas dans `preremplissage`) : traitée comme toujours
  vraie PAR CETTE FONCTION SEULE, jamais par `engine/evaluateNode.ts` — nécessaire parce que le repli du
  nœud `cible-glycemique` (option « Cible ≤ 7 % », `conditions: ["default"]`) n'a pas de négation propre
  écrivable proprement en DNF (vérifié : la négation complète des 3 autres options produit une expression
  à 3 branches de 5 termes ANDés chacune — dupliquerait toute la logique clinique du nœud dans son propre
  `exports`, un risque de divergence au premier changement d'un seuil). La valeur `valeur` (chaîne) est
  interprétée par une petite fonction miroir de `resoudreOperandeTernaire`
  (`engine/deriveCritere.ts`) : `"true"`/`"false"` → booléen, un nombre reconnu → nombre (nécessaire pour
  que `valeurCompatible` accepte la reprise sur un critère `nombre`), sinon → chaîne (`enum`).
- **`DecisionNodeScreen.tsx`** : appel juste après `memoriserCriteres(...)` dans `handleCriteriaChange`
  (aujourd'hui ligne 753), MÊMES arguments de base (`node.criteres_entree`, `avecRestauration`,
  `touchedAvecRestauration`) + `node.exports`. PAS d'appel dans `handleCriteriaEffacer` — `memoriserCriteres`
  ne s'y trouve pas non plus, cohérence délibérée avec l'existant.
- **`content/decision/noeuds/diabete-type-2/cible-glycemique.yaml`** : `exports` avec UNE règle par
  option (dans l'ordre du tableau `options`, donc AVANT le mécanisme `ordered-first-match` du moteur —
  aucune dépendance à l'ordre de résolution, la remarque du plan sur ce point est respectée) :
  - `esperance_vie == limitee AND fragilite == true OR esperance_vie == limitee AND comorbidite_grave ==
    true` → `"9"` (option « Cible < 9 % », conditions distribuées en forme normale disjonctive) ;
  - `fragilite == true OR comorbidite_grave == true OR esperance_vie == limitee OR antecedent_cv == true`
    → `"8"` (reprise telle quelle, déjà une disjonction plate) ;
  - `age < 70 AND anciennete_diabete_annees < 5 AND esperance_vie == longue AND antecedent_cv == false AND
    comorbidite_grave == false AND fragilite == false` → `"6.5"` (reprise telle quelle, déjà une
    conjonction plate) ;
  - `default` → `"7"` (repli, cf. la sentinelle ci-dessus).
- **`content/decision/noeuds/diabete-type-2/prescription.yaml`** : critère `HbA1c_cible` (nombre,
  6-9.5, `partage: true`, masqué — `visible_si` toujours faux ou simplement pas de `groupe` visible selon
  ce que permet le moteur du formulaire, À VÉRIFIER au moment de l'implémentation) ; critère dérivé
  `ecart_hba1c_vs_cible` (`derive: "HbA1c_actuelle - HbA1c_cible"`) ; `preremplissage` sur
  `position_vs_cible` avec DEUX règles seulement (`nettement_au_dessus` si `ecart_hba1c_vs_cible >= 1`,
  `au_dessus` si `ecart_hba1c_vs_cible > 0 AND ecart_hba1c_vs_cible < 1`) — `a_l_objectif`/`sous_objectif`
  restent explicitement NON couverts (même choix que le mécanisme K6/D28 abandonné le 2026-07-29 pour
  cette même paire de valeurs : la frontière déclenche la déprescription, on ne la devine pas ; c'est une
  non-couverture EXPLICITE et documentée, pas une omission — satisfait la consigne « couvertes ou
  explicitement non couvertes »).
- **Invariant I33** : nouveau `describe` dans `coherence-inter-noeuds.test.ts` (à côté d'I32) — pour
  chaque nœud portant `exports`, pour chaque règle, au moins un AUTRE nœud du domaine doit déclarer
  `critere` comme `partage: true` (`HbA1c_cible` sur `insuline.yaml` le fait déjà, et le satisferait pour
  `cible-glycemique`'s export dès que `prescription` le déclare aussi).
- **Test d'intégration N1 de remplacement** (style `sessionCriteres.remount.test.tsx`) : monter
  `DecisionNodeScreen` sur `cible-glycemique` avec un profil réel (âge 64, ancienneté 11, RAS), lire
  l'option retenue (« Cible ≤ 7 % »), démonter, monter `DecisionNodeScreen` sur `prescription`, saisir
  HbA1c 7,7, vérifier que `position_vs_cible` affiche « Au-dessus » PRÉ-REMPLI avec la mention
  « · calculé, à vérifier », puis cliquer une autre valeur et vérifier que la mention disparaît. Second
  test nommé explicitement pour l'étape 5 de T-149 : ouvrir `prescription` SANS passer par
  `cible-glycemique` d'abord — `position_vs_cible` doit rester un champ vide, comportement identique à
  avant cette tâche.

Signaler aussi, à la reprise, la régression I24 constatée ci-dessus (`insuline.yaml`/`statine.yaml`),
totalement indépendante de ce blocage.
</content>

# P13 · S1 — Le patient de N25 repart avec sa carte : l'intention déclare l'absence de traitement   (rédigé par Opus)

> **Modèle : Sonnet · effort : xhigh · Vague : 1 (parallèle : oui — avec S2 et S4)**
> **Environnement : Desktop (navigateur in-app requis — la vignette N25 se rejoue à l'écran)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-04 · Branche : —

## ⚠ Porte d'entrée — vérifier avant de commencer

**T-138 exige l'arbitrage n° 1 du référent** (`index.md` §Arbitrages : « Intention = Initier vaut-il
aucun traitement en cours ? »). S'il n'est pas rendu, **exécute T-137 et arrête-toi là** : T-137 a de
la valeur seule (elle documente et teste un comportement moteur aujourd'hui non tenu), T-138 n'en a
aucune sans la décision clinique qui la fonde.

## Le défaut, en une phrase

Un patient à HbA1c 11,4 %, glucotoxicité et cétonémie — le plus urgent du banc — reçoit **zéro carte**
et un écran qui réclame « Traitements en cours », critère dont l'intention *Initier* vient de
supprimer la section. L'option qui le sauve (« Insuline d'initiation ») existe, est bien écrite, et
attend un critère que le formulaire ne présente plus.

## Lire (commun à la session)

- `docs/decision/validation/revue-conception-fable-2026-08-04.md` — §1 point 1, §4.1 parcours C et D,
  §4.3 T1. **C'est le cahier des charges de cette session.**
- `src/features/decision/lib/formLayout.ts` — `appliquerPreremplissage` (l.143-186) **en entier, y
  compris sa docstring** : les trois garde-fous qu'elle énonce sont le contrat que T-137 étend sans
  l'affaiblir. Puis `valeurParDefaut` (l.50) et `reinitialiserChampsMasques` (l.217-260).
- `schema/decision/noeud.schema.json` — `definitions/reglePreremplissage` (le champ `valeur`,
  aujourd'hui `type: string, minLength: 1`) et `definitions/critereEntree` (`type`, `valeurs`).
- `src/features/decision/screens/DecisionNodeScreen.tsx` — l.497-557 **uniquement** : l'ordre exact
  `renseignesApres` → `reinitialiserChampsMasques` → `appliquerPreremplissage` → `setPreremplis`, et
  le fait que `preremplis` n'est PAS purgé des champs masqués (l.508 ne purge que `touched`).
- `content/decision/noeuds/diabete-type-2/prescription.yaml` — le critère `traitements_en_cours`
  (son `type`, ses `valeurs`, son `visible_si`) et l'option « Insuline d'initiation » (ses
  `conditions`, son `role`).
- `docs/decision/GRAMMAIRE-NOEUD.md` — **R1** (un état ne se déduit jamais d'une intention) et sa
  précision du 2026-08-04, **R7** (jamais se prononcer sur ce qu'on ignore), **R11** (proposition :
  la visibilité ne gouverne pas la décisivité). R1 est la règle que cette session frôle : lis-la
  avant d'écrire quoi que ce soit.

## Hors périmètre

- **Ne touche pas à `engine/conditions.ts`** ni à la sémantique ternaire (D20/D30).
- Ne modifie aucun autre nœud que `prescription.yaml`.
- Ne change pas `visible_si` de `traitements_en_cours` (la section reste masquée à l'initiation —
  c'est le comportement voulu, et l'arbitrage référent le confirme).
- Ne touche pas au bloc « en attente » de l'écran (`DecisionNodeScreen.tsx` l.1094-1188) : **c'est S2**.
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `index.md`, ni `VALIDATION.md`. Ni commit ni push.

---

## T-137 — Un pré-remplissage peut poser une LISTE (et « aucun » est une réponse)

### Objectif

Rendre exprimable, dans le contenu, la phrase « quand telle condition est vraie, ce critère de type
`liste` vaut *aucun élément* » — aujourd'hui impossible, `reglePreremplissage.valeur` n'acceptant
qu'une chaîne non vide.

### Décision clé

**Étendre `valeur`, ne pas créer un second mécanisme.** Le pré-remplissage porte déjà les trois
propriétés qu'il faut (proposé et non imposé, signalé à l'écran « · calculé, à vérifier », effacé dès
que le praticien répond) et il est déjà validé par le référent (K6, 2026-07-27). Un nouveau champ
`derive`-pour-listes dupliquerait le concept et ferait diverger les deux à la première évolution.

Forme retenue au schéma :

```jsonc
"valeur": {
  "oneOf": [
    { "type": "string", "minLength": 1 },
    { "type": "array", "items": { "type": "string" }, "uniqueItems": true }  // [] autorisé
  ]
}
```

**Trois garde-fous à AJOUTER dans `appliquerPreremplissage`, en miroir exact de celui qui existe déjà
pour les `enum`** (l.180, « la valeur pré-remplie doit appartenir aux `valeurs` déclarées ») :

1. une `valeur` tableau n'est acceptée que sur un critère `type: liste` ; une `valeur` chaîne n'est
   jamais posée sur un critère `type: liste` (aujourd'hui elle le serait, sans contrôle) ;
2. chaque élément du tableau doit appartenir aux `valeurs` déclarées du critère ;
3. la comparaison « déjà à cette valeur, ne rien faire » (l.181, aujourd'hui `===`) doit devenir une
   comparaison **de contenu** pour les tableaux — sinon `[] !== []` fait entrer le critère dans
   `preremplis` à chaque frappe, et la mention « · calculé » clignoterait.

**Ce que cette tâche NE change pas** : `!renseignes.has(c.nom)` (un critère répondu n'est jamais
touché — c'est R1), et le `=== true` strict sur le `quand` (une expression indéterminée ne pré-remplit
rien — c'est R7). Ces deux lignes sont la raison pour laquelle ce mécanisme est compatible avec R1 ;
elles ne bougent pas.

### Le comportement à RENDRE EXPLICITE (le cœur de la tâche)

`preremplis` n'est pas purgé quand un champ devient masqué, alors que `touched` l'est
(`DecisionNodeScreen.tsx:508`). Conséquence : **un critère masqué mais pré-rempli reste « déterminé »
pour le moteur** (`criteresRenseignes = touched ∪ preremplis`, l.287), donc les options qui en
dépendent cessent d'être en attente.

C'est exactement l'effet dont T-138 a besoin — et **aucun test ne le tient aujourd'hui**. Un
refactoring innocent de `reinitialiserChampsMasques` (qui, très raisonnablement, purgerait aussi
`preremplis`) rouvrirait N25 sans qu'aucun test ne rougisse. Cette tâche pose donc le test qui
transforme cet effet de bord en contrat.

⚠ **Ce comportement n'affaiblit pas R8** (« une valeur masquée ne doit pas piloter le moteur en
silence ») pour une seule raison, qu'il faut écrire dans le test : la valeur portée est celle que le
pré-remplissage vient de *poser*, elle est **égale à la valeur par défaut** que
`reinitialiserChampsMasques` impose (liste vide), et elle est **affichée** dans le résumé de section.
Rien n'est silencieux, rien ne survit à une saisie contraire. Si un contenu futur pré-remplissait un
champ masqué avec une valeur **différente** du défaut, ce raisonnement tomberait — d'où l'invariant
de la tâche suivante.

### Lire / Modifier

**Modifier** : `schema/decision/noeud.schema.json` (`reglePreremplissage.valeur`) ·
`src/features/decision/lib/formLayout.ts` (`appliquerPreremplissage`, et le type importé si le
`CritereEntree` typé TS porte `preremplissage`) · `src/features/decision/content/node.types.ts` (le
type de `valeur`) · `src/features/decision/lib/formLayout.test.ts` (les tests ci-dessous).

### Étapes

1. Étends le schéma, puis le type TS. `npm run typecheck` doit **rougir** aux endroits qui supposent
   une chaîne : c'est la carte des points à traiter, lis-la avant de corriger.
2. Implémente les trois garde-fous dans `appliquerPreremplissage`. Garde la structure existante —
   c'est une extension du `for`, pas une réécriture.
3. **Écris les tests AVANT de vérifier qu'ils passent, et vois-les rouges** (pratique héritée de P12,
   qui a payé trois fois) :
   - une règle `valeur: []` sur un critère `liste` pose la liste vide et cite le critère dans
     `preremplis` ;
   - la même règle sur un critère `enum`/`nombre` **ne pose rien** (garde-fou 1) ;
   - une `valeur: ["metformine", "inconnu"]` dont un élément n'est pas déclaré **ne pose rien**
     (garde-fou 2) ;
   - rejouer le pré-remplissage sur un critère déjà à `[]` ne le remet PAS dans `preremplis`
     (garde-fou 3, anti-clignotement) ;
   - un critère déjà `touched` n'est jamais pré-rempli (non-régression R1) ;
   - un `quand` indéterminé ne pré-remplit rien (non-régression R7).
4. **Le test de contrat** (celui qui protège T-138), dans `formLayout.test.ts` ou
   `DecisionNodeScreen`-level selon ce qui est atteignable : *un critère pré-rempli puis masqué reste
   dans `preremplis`, donc dans `criteresRenseignes`*. Nomme-le explicitement
   (« contrat T-138 : un pré-remplissage masqué reste déterminé pour le moteur ») et **commente-le
   avec le pourquoi** — c'est un test dont la raison d'être n'est pas devinable depuis son assertion.

### Validation

- **N0 auto (bloque le commit)** : `npm test` → vert · `npm run typecheck` → 0 erreur ·
  `npm run build` → OK. Le nombre de tests attendus est **1048 + les nouveaux** ; mesure machine
  libre (aucun serveur de dev en cours — cf. `STATUS.md`).
- **N1 visuel auto** : `—` (aucun changement d'écran à cette tâche).
- **N2 humain** : `—`.

### Si bloqué

Si l'extension du schéma fait rougir des tests de contenu **existants** (un `preremplissage` déjà
écrit dans un nœud) : ne les affaiblis pas. Signale — cela voudrait dire qu'un contenu actuel
s'appuie sur une tolérance que les nouveaux garde-fous retirent, et c'est une information, pas un
obstacle.

### Message de commit

`feat(engine): un pré-remplissage peut poser une liste, « aucun » compris (T-137)`

---

## T-138 — L'initiation déclare l'absence de traitement, et l'insuline d'initiation se rend

### Objectif

Que le parcours N25 produise la carte « Insuline d'initiation » **sans que le praticien ait à mentir
sur son intention**.

### Décision clé

Dans `prescription.yaml`, `traitements_en_cours` reçoit :

```yaml
preremplissage:
  - quand: "intention == initier"
    valeur: []
```

**Ce que cela dit, et ce que cela ne dit pas.** Cela dit : « à l'initiation, la valeur de départ de ce
critère est *aucun traitement* ». Cela ne dit **pas** que le moteur déduit un état d'une intention au
sens interdit par R1 — la nuance tient à trois propriétés, à vérifier une par une, et à écrire en
commentaire YAML au-dessus de la règle :

1. **le critère reste `saisi`**, jamais `derive` : il garde son type, son `visible_si`, et
   redevient pleinement saisissable dès que l'intention change ;
2. **la valeur est signalée** à l'écran (« · calculé, à vérifier ») partout où le champ est visible ;
3. **le praticien peut la contredire** — en changeant d'intention, ce qui est le geste juste : un
   patient qui prend déjà quelque chose n'est pas dans une intention *Initier*.

C'est la lecture que la précision du 2026-08-04 de R1 autorise explicitement (« la question directe
peut être pré-remplie, jamais escamotée ») — **à condition que l'arbitrage référent n° 1 soit rendu**.

⚠ **Le champ étant masqué à l'initiation, la propriété 2 n'est pas observable sur cet écran.** C'est
acceptable *ici* parce que la valeur posée est l'absence, qu'elle est identique au défaut, et que le
cartouche cessera de réclamer le critère. Ce n'est **pas** un précédent pour pré-remplir un champ
masqué avec une valeur non triviale — l'invariant de l'étape 5 est là pour l'interdire.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/prescription.yaml` (le critère
`traitements_en_cours` : la règle + son commentaire + bump de version et changelog, D5) ·
`src/features/decision/engine/banc/invariants-contenu.test.ts` (l'invariant de l'étape 5) ·
les instantanés de caractérisation si le comportement change (il DOIT changer : lis l'étape 4).

### Étapes

1. Écris la règle et son commentaire. **Bump `meta.version` et ajoute l'entrée de changelog**
   (D5, non négociable pour tout changement de contenu).
2. Rejoue **N25 au navigateur** : *Initier · HbA1c 11,4 · Nettement au-dessus · glucotoxicité Oui ·
   cétonémie Oui*. **Attendu** : la carte « Ajouter · Insuline d'initiation » apparaît, badgée
   `Mesure de sécurité`, avec son motif « HbA1c ≥ 10 et Symptômes de glucotoxicité et Cétonémie ».
   **Attendu aussi** : le cartouche ne réclame plus « Traitements en cours ». Consigne les deux dans
   le rapport de session — c'est la preuve que la session existe pour produire.
3. Vérifie le **sens inverse** (parcours C de la revue) : depuis *Initier*, basculer vers
   *Intensifier* doit rouvrir la section Traitements, **vide et pleinement saisissable**, sans que la
   valeur pré-remplie ait laissé de trace « répondue ». Si elle en laisse une, STOP : c'est
   l'interaction avec S3, et elle se traite là-bas, pas ici.
4. **Le golden master DOIT bouger** (`engine/banc/caracterisation.test.ts`) : tous les profils
   `intention: initier` gagnent potentiellement des options. **Relis le diff ligne à ligne** avant de
   l'accepter — c'est la consigne §4bis de `CONSTRUIRE-UN-MODULE.md`, et P12 a montré qu'un golden
   master accepté sans lecture masque exactement ce qu'il devrait révéler. Attendu : les options qui
   dépendent de `traitements_en_cours` en citation **négative** (`ne_contient_pas`) sortent de
   l'attente pour ces profils. Une option qui apparaîtrait chez un profil `initier` **sans** rapport
   avec ce critère est un signal d'alarme, pas un bonus.
5. **Pose l'invariant de banc I26** (`invariants-contenu.test.ts`) : *aucune règle `preremplissage`
   ne pose, sur un critère dont le `visible_si` est faux dans le même état, une valeur différente de
   `valeurParDefaut(critere)`*. Formulation générique (aucun nom de critère ni de nœud). C'est ce qui
   empêche que le raisonnement de tolérance ci-dessus soit invoqué demain pour un cas où il ne vaut
   plus. **Vois-le rouge une fois** (en posant temporairement une valeur non triviale) avant de le
   voir vert.

### Validation

- **N0 auto (bloque le commit)** : `npm test` · `npm run typecheck` · `npm run build` — tous verts,
  golden master **relu** et régénéré, pas simplement accepté.
- **N1 visuel auto** : le parcours N25 de l'étape 2 et le retour de l'étape 3, au navigateur,
  console propre, aucun débordement. → `/verif-visuelle`.
- **N2 humain** : **la mention de provenance manque là où le champ est masqué.** Le praticien en
  intention *Initier* ne voit nulle part que l'outil a posé « aucun traitement » pour lui. Deux
  formes possibles à soumettre : une ligne dans le résumé de la section masquée, ou une mention dans
  le cartouche d'ouverture (« Traitements en cours : aucun, déduit de votre intention »). À consigner
  dans `VALIDATION.md` avec les deux options — **ne tranche pas toi-même**.

### Si bloqué

- Si la carte n'apparaît toujours pas après l'étape 1 : **ne bricole pas le contenu de l'option**.
  Vérifie d'abord, dans cet ordre, (a) que `traitements_en_cours` est bien dans `preremplis` après le
  changement d'intention, (b) que `criteresRenseignes` le contient, (c) ce que
  `vue.enAttente` dit encore de l'option. Le diagnostic est dans l'un de ces trois points ; rends la
  main avec la réponse plutôt qu'avec un contournement.
- Si l'arbitrage référent n'a pas été rendu : **arrête-toi après T-137**, ne devine pas.

### Message de commit

`feat(content): l'initiation déclare l'absence de traitement — l'insuline d'initiation se rend (T-138, I26)`

---

## Fin de session

Dérouler `/fin-de-tache` (mode **solo**) — sauf si l'orchestrateur a demandé le mode vague parallèle,
auquel cas : ne toucher aucun fichier partagé, ne rien committer, et consigner le bilan et les points
N2 **dans ce fichier**.

**À consigner impérativement dans le bilan** : le tableau avant/après de la vignette N25 (cartes
rendues, contenu du cartouche), le diff du golden master résumé en une phrase (combien de profils,
quelles options gagnées), et la formulation exacte de I26.
</content>

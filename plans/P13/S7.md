# P13 · S7 — Contenu : ce que les nœuds disent d'eux-mêmes et ce qu'ils taisent   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 3 (parallèle : oui — avec S6)**
> **Environnement : — (contenu ; la vérification est textuelle, sauf T-153 qui se relit à l'écran)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-04 · Branche : — · **Dépend de S4** (dont le tableau de violations d'invariants
  dit quelles corrections restent à faire — sans lui, tu travailles à l'aveugle sur T-152)

## ⚠ Règle de session : tu corriges des FORMES, pas des faits

Cinq tâches de contenu, toutes de **rédaction** ou de **structure**. **Aucune ne change une conduite
clinique, un seuil, une posologie ou un niveau de preuve.** Si une correction t'amène à modifier un
chiffre ou une indication : STOP, signale — c'est un arbitrage référent, pas une tâche d'exécution.

**Rappel D5, non négociable** : tout nœud modifié reçoit un **bump de `meta.version` et une entrée de
changelog**. Cinq tâches sur quatre nœuds ⇒ vérifie à la fin que chaque fichier touché a bien les deux.

## Lire (commun à la session)

- `docs/decision/validation/revue-conception-fable-2026-08-04.md` — §6 (les trois nœuds explorés en
  seconde passe), §5.3 **I5**, §7 **P3**.
- `docs/decision/GRAMMAIRE-NOEUD.md` — **R6 volet rendu** (dont l'invariant « aucune référence ne
  parle du nœud »), **R8** (un fait de sécurité a un canal, et un seul), **P3** côté revue.
- **Le tableau des violations d'invariants produit par S4/T-146** : c'est la liste de travail de T-152.

## Hors périmètre

- Ne touche à **aucun** fichier `src/` : cette session est du contenu. Les corrections de rendu sont
  **S4**, et elles doivent être livrées avant.
- Ne modifie aucun `conditions`/`exclusions`/`prerequis` — **sauf** T-155, qui est explicitement une
  tâche de structure de critère et dont le périmètre est borné.
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `index.md`, ni `VALIDATION.md`. Ni commit ni push.

---

## T-151 — Les noms d'essais amputés de `statine.yaml` (D4)

### Objectif

Qu'aucune référence ne s'affiche en commençant par une virgule ou une parenthèse orpheline.

### Décision clé

**14 occurrences, toutes dans `statine.yaml`, aucune ailleurs** (vérifié) : le nom de l'essai manque
en tête du champ `titre`, qui commence donc par `, ` ou ` (`. Rendu observé : *« D'après : ,
sous-groupe diabète (18 686 diabétiques) : RR 0,79 par mmol/L »*.

Lignes concernées : **898, 903, 908, 913, 918, 923, 928, 933, 943, 958, 963, 973, 983, 988**.

**Le nom manquant se retrouve dans le contenu lui-même** — l'argumentaire du nœud, les
`avantages`/`inconvenients`, ou le PMID/DOI de la même entrée nomment l'essai (CARDS, HPS, CTT, LIPID,
4S, TNT, PROVE-IT, SPARCL, PROSPER, AURORA…). **Ne devine aucun nom** : si une entrée ne permet pas de
le retrouver de façon certaine dans le dépôt, laisse-la et signale-la. Une référence à demi inventée
serait bien pire qu'une référence amputée.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/statine.yaml` (les 14 `titre`, + bump +
changelog).

### Étapes

1. Pour chaque ligne, retrouve le nom dans le dépôt et **note d'où tu le tiens** (fichier + ligne).
   Le tableau de provenance est le livrable qui rend la correction vérifiable.
2. Corrige. Ne touche **que** le début du titre : le reste de la chaîne (chiffres, HR, NNT) ne bouge
   pas d'un caractère.
3. **Invariant I34** : *aucun champ `titre` de `references` ne commence par un caractère de
   ponctuation ou un espace*. Générique, tous nœuds — c'est ce qui empêche la réapparition.

### Validation

- **N0 auto (bloque le commit)** : `npm test` · `npm run typecheck` · `npm run build` → verts.
  Ajv doit rester vert (aucun changement de structure).
- **N1 visuel auto** : la carte statine haute intensité, panneau « État des preuves » déplié : les
  noms d'essais s'affichent. → `/verif-visuelle`. **N2** : `—`.

### Message de commit

`fix(content): les 14 noms d'essais amputés de statine (T-151, I34)`

---

## T-152 — Le registre changelog sort de l'écran praticien (I5)

### Objectif

Qu'une référence dise ce que dit la source, jamais ce que fait ou faisait le nœud.

### Décision clé

Deux occurrences confirmées, **toutes deux dans des champs rendus** de `insuline.yaml` :

- **l.1887** : `titre: "HAS 2024 …, grade AE — règle d'adaptation SYMÉTRIQUE : « … » . Le nœud
  n'avait encodé que la montée"`
- **l.1897** : `titre: "ebmfrance/Duodecim … — porte VERBATIM le « 3 matins de suite » de
  l'algorithme de titration affiché par ce nœud …"`

⚠ **Distingue les commentaires YAML des champs rendus.** `insuline.yaml:856` et `:2490` portent des
formulations voisines mais sont des **commentaires** (`#`) ou de la prose d'auteur non rendue : ils
sont **légitimes** et ne se touchent pas. Le registre de fabrication a toute sa place dans le fichier,
jamais dans l'écran. **Vérifie le champ avant de corriger, pas la ligne.**

**La règle de réécriture** : garder l'information (elle est juste et utile), en changeant de sujet.
« Le nœud n'avait encodé que la montée » devient une propriété de la **source** (« règle symétrique :
augmentation **ou** réduction de 1 à 2 UI »), ce qu'elle est déjà en réalité. Si l'auto-critique de
l'outil mérite d'être dite au praticien, son canal est le `cadrage` ou une alerte — pas un titre
d'essai (R8 : un fait, un canal).

**Le tableau de S4/T-146 (invariant I31) fait foi** sur l'exhaustivité : traite tout ce qu'il liste,
et lève les exemptions correspondantes.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/insuline.yaml` (+ les autres nœuds si S4 en a
listé) · les exemptions posées par S4 dans le fichier d'invariants.

### Étapes

1. Reprends le tableau de S4. Corrige chaque champ **rendu** ; laisse les commentaires.
2. Retire les exemptions devenues inutiles. **Une liste de dette qui ne rétrécit jamais devient du
   papier peint** (`CONSTRUIRE-UN-MODULE.md` §3) — c'est ici qu'elle rétrécit.
3. I31 doit passer **sans exemption** sur les nœuds traités. S'il en reste une, elle est nommée, datée
   et motivée.

### Validation

- **N0 auto (bloque le commit)** : `npm test` (I31 vert) · `typecheck` · `build`.
- **N1 visuel auto** : le panneau « État des preuves » du nœud insuline. → `/verif-visuelle`.
  **N2** : `—`.

### Message de commit

`fix(content): une référence dit ce que dit la source, pas ce que fait le nœud (T-152)`

---

## T-153 — À 60 fois la normale, le titre de la carte dit ce qu'il faut faire (D6)

### Objectif

Qu'une carte ne porte plus un titre que son propre encart doit démentir.

### Décision clé

Aujourd'hui, à CK > 50 N, la carte s'intitule *« Interrompre la statine 4 à 6 semaines et
réévaluer »* et son contenu écrit *« Ce n'est plus la séquence d'interruption-réintroduction décrite
ci-dessus »*. Le test des 20 secondes de l'audit est sans appel : **ce qui n'est pas dans le titre
n'est pas retenu** (P3).

**La forme retenue : deux options distinctes**, pas un titre variable. Le contenu ne sait pas faire
varier un `intitule` par bande de valeur, et l'inventer ici serait un mécanisme de plus. Or les deux
conduites **sont** cliniquement distinctes (suspension réévaluable vs urgence hospitalière) : ce sont
donc deux options, avec des `conditions` mutuellement exclusives sur `CK_x_normale`.

**Les seuils, les textes cliniques, l'avis spécialisé et la réhydratation ne changent pas d'un
mot** — ils sont déplacés, pas réécrits. La seule chose que tu crées est un **intitulé** pour la
seconde option, du type « Arrêter la statine — suspicion de rhabdomyolyse, avis spécialisé urgent ».

⚠ **`statine` est en `ordered-first-match`** : l'ordre de déclaration **est** la logique. Place la
nouvelle option de façon que la bande haute gagne pour ses patients, et **vérifie que la bande basse
n'est pas préemptée**. Relis à ce sujet l'encadré de R8 sur l'option terminale (« c'est l'ORDRE qui
protège, et ça se documente dans l'option protégée ») : documente l'ordre **dans l'option protégée**,
pas seulement dans celle qui protège.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/statine.yaml` (+ bump + changelog) ·
`engine/banc/` (les vignettes des trois bandes de CK).

### Étapes

1. Scinde l'option. Conditions mutuellement exclusives, ordre documenté des deux côtés.
2. **Vignettes aux trois bandes** : 2,3 N → carte haute intensité ; 6,2 N → « Interrompre… » ;
   60 N → « Arrêter — suspicion de rhabdomyolyse ». **Vues rouges avant** la scission pour la
   troisième.
3. Vérifie la **couverture** (chaque option se déclenche au moins une fois) et **I23** (jamais
   `applicable` et `enAttente` vides ensemble) — la scission d'une option est exactement le geste qui
   peut créer un trou de couverture.
4. Golden master : relis le diff. Attendu : les profils CK > 50 N changent d'intitulé, **et rien
   d'autre**.

### Validation

- **N0 auto (bloque le commit)** : `npm test` · `typecheck` · `build` → verts, couverture et I22/I23
  verts, golden master relu.
- **N1 visuel auto** : les trois bandes à l'écran (CK 450/1240/12000 avec borne haute 200).
  → `/verif-visuelle`.
- **N2 humain** : **l'intitulé de la nouvelle option** — c'est le seul texte clinique créé par cette
  session. À soumettre au référent dans `VALIDATION.md`, avec la formulation exacte.

### Message de commit

`fix(content): la rhabdomyolyse a sa propre carte, avec son titre (T-153)`

---

## T-154 — La rétinopathie proliférante a son alerte (D11)

### Objectif

Réparer une asymétrie : deux drapeaux du même groupe de sécurité, un seul a son alerte.

### Décision clé

Sur `rhd-activite-physique`, l'alerte « neuropathie / mal perforant » est un modèle du genre (elle
déclare même que l'outil retire plus que la source). **Le critère `retinopathie_non_stabilisee`
existe, il écarte bien la pratique structurée — mais aucune alerte ne dit au praticien ce qu'il doit
en faire.**

**Le contenu de l'alerte n'est pas à inventer** : il doit venir d'une source déjà présente dans le
nœud (`sources`, `argumentaire`, ou les `references` de l'option écartée). **Si aucune source du nœud
ne porte la conduite à tenir devant une rétinopathie proliférante, n'écris rien** : signale que
l'alerte demande une collecte, et la tâche s'arrête là. C'est la discipline §P4 de
`CONSTRUIRE-UN-MODULE.md` — on ne comble pas un silence par une phrase plausible.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/rhd-activite-physique.yaml` (+ bump +
changelog) — **ou rien**, si la source manque.

### Étapes

1. Cherche la conduite dans les sources **du nœud**. Consigne ce que tu trouves, avec la référence.
2. Si trouvé : écris l'alerte, **canal `alertes` de nœud** conditionnée sur le drapeau (R8 : elle est
   vraie pour ce patient-là, pas pour tous ⇒ pas un `cadrage`). Vérifie I6 (jamais `quand: default`)
   et I7 (un libellé prohibitif implique un garde-fou — ici l'exclusion existe déjà).
3. Si non trouvé : **ne crée rien**, écris le constat dans le bilan.

### Validation

- **N0 auto (bloque le commit)** : `npm test` · `typecheck` · `build` → verts.
- **N1 visuel auto** : profil rétinopathie cochée, l'alerte s'affiche. → `/verif-visuelle`.
- **N2 humain** : **le texte de l'alerte**, s'il est écrit — texte clinique, relecture référent.

### Message de commit

`feat(content): la rétinopathie proliférante a son alerte de sécurité (T-154)`

---

## T-155 — Un dérivé agrégatif se rend par ses composants vrais (D7)

### Objectif

Qu'une ligne d'écartement cesse d'énumérer quatre possibles quand deux sont vrais.

### Décision clé

Constat de la revue : *« écarté : Signe imposant un avis avant la pratique structurée (limitation,
ischémie d'effort, rétinopathie, pied) »* — chez un patient dont **seuls** la rétinopathie et le pied
sont déclarés. **Le correctif de rendu de S4 ne suffit pas ici** : c'est l'agrégat lui-même qui a
effacé l'information avant le rendu, et un dérivé n'a qu'une valeur booléenne à montrer.

**Deux voies, à trancher sur pièce** — mesure le coût des deux avant de choisir :

- **(a) décomposer dans l'expression d'écartement** : l'`exclusions` de l'option cite les quatre
  drapeaux en disjonction plutôt que le dérivé. Le rendu par branches vraies de S4 fait alors
  exactement ce qu'il faut, **sans aucun code**. Le dérivé reste utilisable ailleurs s'il sert.
- **(b) donner au dérivé un libellé par composant** — nouveau mécanisme de contenu, coût moteur.

**(a) est très probablement la bonne**, et elle est gratuite. Ne prends (b) que si (a) casse quelque
chose que tu peux nommer.

⚠ **Vérifie l'équivalence logique** avant/après : la disjonction explicite doit être **exactement**
la définition du dérivé, sinon tu changes une conduite. Compare les deux expressions terme à terme,
et vérifie que le dérivé n'est pas utilisé ailleurs avec une autre portée.

### Lire / Modifier

**Modifier** : `content/decision/noeuds/diabete-type-2/rhd-activite-physique.yaml` (+ bump +
changelog).

### Étapes

1. Relève la définition exacte du dérivé, et **tous** ses usages dans le nœud.
2. Applique (a). Vérifie l'équivalence terme à terme.
3. **Golden master : il ne doit PAS bouger** côté décisions (mêmes options pour les mêmes profils),
   seulement côté **textes**. Si une option change d'état, l'équivalence est fausse — reviens en
   arrière.
4. Vérifie à l'écran, profil rétinopathie + pied : la ligne d'écartement ne nomme que ces deux-là.

### Validation

- **N0 auto (bloque le commit)** : `npm test` · `typecheck` · `build` → verts, golden master relu
  (textes seulement).
- **N1 visuel auto** : le profil de l'étape 4. → `/verif-visuelle`. **N2** : `—`.

### Message de commit

`fix(content): l'écartement pour signe d'alerte nomme les signes réellement présents (T-155)`

---

## Fin de session

Dérouler `/fin-de-tache` (mode **solo**) — sauf mode vague parallèle : ne toucher aucun fichier
partagé, ne rien committer, consigner bilan et points N2 **ici**.

**À consigner impérativement** : le tableau de provenance des 14 noms d'essais (T-151), la voie
retenue en T-155 et pourquoi, et **la liste des textes cliniques créés** (T-153 intitulé, T-154
alerte) — ce sont les seuls éléments de cette session qui demandent une relecture référent.

---

## Bilan de session

### État final N0

`npm test` → **1137 tests passés, 11 skip, 0 échec** (1148 total ; base 1136+11 avant session, +1 test
neuf : `evaluateNode.statine.test.ts` F-25b). `npm run typecheck` → 0 erreur. `npm run build` → OK
(532 ms, warning taille de chunk pré-existant, sans rapport avec cette session). Golden master relu et
mis à jour (`caracterisation.statine.txt`, `caracterisation.rhd-activite-physique.txt` et leurs
pendants `-indetermine`) — détail de la relecture en fin de bilan.

### T-151 — livrée intégralement (14/14)

Les 14 titres de référence amputés de `statine.yaml` sont corrigés : seul le nom de l'essai est
prépendu en tête de chaîne, rien d'autre ne bouge (chiffres, HR, NNT identiques au caractère près).
Provenance croisée systématiquement à deux sources indépendantes de `statine.argumentaire.md` (texte
narratif + PMID/DOI identique au `lien` du YAML) :

| id (`statine.yaml`) | Ligne `titre` | Nom restauré | Provenance (`statine.argumentaire.md`) |
| --- | --- | --- | --- |
| `cards` | 898 | CARDS | l.67 « CARDS 2004 » · l.371 DOI identique |
| `hps` | 903 | HPS | l.38 « HPS diabète 2003 » · l.359 DOI identique |
| `ctt-diabete` | 908 | CTT | l.37 « CTT méta diabète 2008 » · l.357 DOI identique |
| `ctt-more-vs-less` | 913 | CTT | l.48 « CTT more-vs-less 2010 » · l.358 DOI identique |
| `care` | 918 | CARE | l.39 « CARE diabète 1998 » · l.360 DOI identique |
| `lipid` | 923 | LIPID | l.40 « LIPID diabète 2003 » · l.361 PMID identique |
| `quatre-s` | 928 | 4S | l.41 « 4S diabète 1997 » · l.362 DOI identique |
| `tnt` | 933 | TNT | l.49 « TNT diabète 2006 » · l.363 DOI identique |
| `aspen` | 943 | ASPEN | l.69 « ASPEN 2006 » · l.372 DOI identique |
| `lodestar` | 958 | LODESTAR | l.116 « LODESTAR 2023 » · l.379 DOI identique |
| `ctt-age` | 963 | CTT | l.132 « CTT par âge 2019 » · l.380 PMID identique |
| `prosper` | 973 | PROSPER | l.134 « PROSPER » · l.381 PMID identique |
| `aurora` | 983 | AURORA | l.146 « AURORA (rosuvastatine, dialyse) » · l.383 PMID identique |
| `ctt-symptomes-musculaires` | 988 | CTT | l.173 « méta CTT sur données individuelles 2022 » · l.385 PMID identique |

**Découverte hors liste du plan, signalée sans y toucher** : 5 autres entrées de
`sources.references_primaires` de `statine.yaml` (`prove-it`, `ascot-lla`, `chang`, `ctt-bas-risque`,
`quatre-d`) commencent aussi par un espace suivi d'un caractère non alphabétique (tiret, deux-points,
mot en minuscule) — un défaut voisin mais **hors du périmètre littéral de T-151** (« les 14 titre »),
et qui ne correspond pas au « virgule ou parenthèse orpheline » de l'Objectif. Provenance vérifiée dans
`statine.argumentaire.md` pour 3 d'entre elles avec certitude totale (`prove-it`→PROVE-IT l.50 ;
`ascot-lla`→ASCOT-LLA l.70 ; `quatre-d`→4D, confirmé 6× ailleurs dans `statine.yaml`) — **non
corrigées**, le plan bornant strictement le Modifier à « les 14 titre ». `chang` (Chang 2013,
l.72 de l'argumentaire) et `ctt-bas-risque` (CTT, l.123 de `F-statine.md`) ont un nom certain mais un
texte de raccord incertain (mot manquant probable au-delà du nom) — laissées sciemment, discipline
« ne devine aucun nom ». **Signalé au référent pour une tâche de suite éventuelle, pas d'action prise.**

### T-152 — livrée partiellement : insuline 18/18, statine 8/8 BLOQUÉES (limite structurelle d'I24)

**Insuline (18 violations I28, 2 violations I31) : levées.** Cinq options du bloc MCG
(`insuline.yaml`) reçoivent un motif rédigé négatif — mais **pas sur l'atome isolé** comme tenté au
premier passage : `lib/conditionText.ts` (`texteDeBranche`) et l'invariant I24 indexent la branche `OR`
**entière** (toute la conjonction), jamais un atome au sein d'une conjonction plus large. Corrigé en
clé = branche complète (`"mcg_disponible == false AND gaj_basse == true"`, etc.), texte « absence de
MCG, glycémie à jeun … ». Les 2 titres de référence `insuline` (« encodé »/« VERBATIM ») sont réécrits,
sujet changé pour la source, aucune information perdue.

**Statine (8 violations I28) : tentées puis REVERTÉES — dette non résorbée.** `statine_deja_en_place
== false` n'apparaît, sur les deux options concernées, que dans leurs `exclusions` — jamais dans
`conditions`/`prerequis`. Un motif y serait fonctionnellement consommé au rendu écran (`describeReasons`
lit `option.motifs` pour les `exclusions` aussi, depuis T-144), **mais l'invariant I24**
(`branchesDeclarees`, `engine/banc/invariants-contenu.test.ts`) **ne scanne que `conditions`/
`prerequis`**, jamais `exclusions` — un motif y serait donc signalé « orphelin » à tort, et `npm test`
resterait rouge. Corriger I24 exige d'étendre sa logique aux `exclusions` : une modification de LOGIQUE
d'un fichier `src/` hors du seul périmètre explicitement autorisé cette session
(`rendu-textuel.test.ts`, retrait d'exemption). **STOP partiel** : aucun motif ajouté sur `statine.yaml`
pour cette branche ; les 8 exemptions I28 restent posées dans `rendu-textuel.test.ts`, avec le motif
exact et une piste de correction (étendre `branchesDeclarees`, tâche moteur, puis ajouter les deux
motifs déjà rédigés en attente : « dialyse, avant initiation de la statine » et le libellé déjà en
place ailleurs dans le fichier pour l'autre branche).

**Tableau I28/I31, état après cette session** :

| Invariant | Avant S7 | Après S7 | Détail |
| --- | --- | --- | --- |
| I28 | 26 (18 insuline + 8 statine) | **8** (statine uniquement) | 18 levées (insuline) ; 8 nommément exemptées, motif = limite I24 documentée ci-dessus, échéance = tâche moteur qui étend `branchesDeclarees` aux `exclusions` |
| I31 | 2 (insuline) | **0** | Les 2 levées |

### T-153 — livrée. Voie : scission en deux options (imposée par le plan, appliquée telle quelle)

La carte « Interrompre la statine 4 à 6 semaines et réévaluer » ne couvre plus que `CK_x_normale <= 50`
(borne ajoutée) ; une nouvelle carte couvre `CK_x_normale > 50`. Seuils/texte clinique/avis
spécialisé/réhydratation déplacés tels quels depuis l'ex-alerte de bande haute (seule la phrase qui
démentait le titre voisin, devenue sans objet, n'est pas reprise). Ordre : les deux conditions sont
**mutuellement exclusives par construction** (`<= 50` / `> 50`, aucun profil ne peut satisfaire les
deux) — documenté dans les deux options, aucune des deux ne protège l'autre par la position dans le
fichier. Vignettes `evaluateNode.statine.test.ts` : F-25 réécrite (CK=60 → nouvelle carte, plus
`OPT_INTERRUPTION`), F-25b ajoutée (CK=50 pile → reste sur « Interrompre… », contre-épreuve de la borne
exacte). Couverture (I22/I23) et golden master relus (détail plus bas) : aucune autre option ne change
de camp, prouvé à la fois par équivalence logique de partition (l'union des deux conditions = la
condition d'origine, positionnées comme un seul bloc contigu dans l'ordre `ordered-first-match`) et par
vérification automatisée exhaustive des 180 profils figés de `caracterisation.statine.txt`.

**Texte clinique créé (le seul de cette tâche, N2)** : l'intitulé de la nouvelle option —

> **« Arrêter la statine — suspicion de rhabdomyolyse, avis spécialisé urgent »**

**N1 visuel déféré** (navigateur indisponible cette session) : remplacé par (a) les deux vignettes
`evaluateNode.statine.test.ts` (F-25/F-25b) qui prouvent, sur les CK exacts 50/60 recommandés par le
plan, la carte affichée et le contenu de ses `avantages` ; (b) la relecture ligne à ligne du golden
master `caracterisation.statine.txt`/`-indetermine` sur les 180+15 profils figés — texte réellement
produit par `construireVueDecision`, pas une mise en page.

### T-154 — STOP : aucune source suffisante, rien écrit

Recherché dans `rhd-activite-physique.yaml` (`sources`, `argumentaire`, `cadrage`, `incertitudes`) et
son argumentaire exhaustif `rhd-activite-physique.argumentaire.md`. Seul fragment trouvé : « Rétinopathie
non stabilisée ou proliférante — R.27/R.28 (manœuvre à glotte fermée) » (l.129 de l'argumentaire) — un
TAG de mécanisme (pourquoi c'est contre-indiqué), pas une conduite à tenir (contrairement à la
neuropathie, qui a une source détaillant explicitement ce qui reste permis). Écrire une alerte à partir
de « manœuvre à glotte fermée » seul aurait exigé d'inventer les activités précises à éviter/permettre —
exactement le silence que §P4 de `CONSTRUIRE-UN-MODULE.md` interdit de combler. **Aucune alerte écrite,
aucun fichier modifié pour cette tâche.** Signalé : une collecte de source (le texte complet HAS R.27/
R.28, ou une source équivalente détaillant la conduite précise) est nécessaire avant qu'un futur lot
puisse écrire cette alerte sur le modèle de celle de la neuropathie.

### T-155 — livrée. Voie (a) retenue : décomposition dans l'expression d'écartement

**Voie (a) choisie**, conforme à la recommandation du plan (« très probablement la bonne, et elle est
gratuite ») : l'`exclusions` de « Envisager un programme d'activité physique adaptée… » cite désormais
les quatre drapeaux (`limitation_physique_connue`, `symptomes_ischemie_effort`,
`retinopathie_non_stabilisee_ou_proliferante`, `neuropathie_ou_mal_perforant_plantaire`) en disjonction,
copie exacte du corps du `derive` de `verrou_effort` — vérifié terme à terme avant application. La
voie (b) (libellé par composant sur le dérivé) aurait exigé un nouveau mécanisme de rendu (coût moteur,
hors mandat contenu) ; la voie (a) ne coûte aucun code, le rendu par branches vraies existant déjà
depuis T-144 (S4). `verrou_effort` reste déclaré dans `criteres_entree` — non supprimé, car
potentiellement réutilisable, et son seul usage dans ce nœud (vérifié par recherche exhaustive avant la
décomposition) était précisément celui qui vient d'être décomposé.

**Effet de bord découvert et traité (I14)** : `symptomes_ischemie_effort` et
`retinopathie_non_stabilisee_ou_proliferante` n'étaient cités, avant ce lot, que dans le `derive` de
`verrou_effort` — deux entrées de dette connue de l'invariant I14 (`invariants-contenu.test.ts`,
« un drapeau qui n'agit qu'à travers un derive n'a pas de voix propre »). La décomposition leur donne
mécaniquement une citation hors `derive`, résorbant cette dette. Le fichier de test portant cette dette
(`invariants-contenu.test.ts`) est un fichier `src/`, hors du périmètre littéral « Modifier » de T-155
(`rhd-activite-physique.yaml` seul) — **écart signalé explicitement (N2 pour le coordinateur)** : j'ai
retiré les deux entrées devenues stales, un retrait mécanique de deux lignes analogue dans sa nature à
l'exemption explicitement autorisée sur `rendu-textuel.test.ts`, et directement exigé par le docstring
de l'invariant lui-même (« la dette ne peut pas se fossiliser ») — sans ce retrait, `npm test` reste
rouge sur une conséquence strictement mécanique et non recherchée du correctif de contenu. Aucune
question clinique n'est tranchée par ce retrait (les questions au référent posées par ces deux entrées
— une conduite propre pour l'ischémie d'effort / la rétinopathie — restent ouvertes, reportées dans le
commentaire de remplacement).

**Golden master relu** : 4 profils de la section « en clair » de `caracterisation.rhd-activite-physique.
txt` changent, tous du même patron exact (`verrou_effort == true` → l'énumération des composants
réellement vrais pour ce patient, rien d'autre dans le bloc ne bouge) ; vérification automatisée sur les
88 lignes compactes restantes confirme le même patron partout, sans exception. Section
`-indetermine` : 3 lignes ajoutées, purement additives (aucune option ne change de camp).

**N1 visuel déféré** : remplacé par la relecture du golden master ci-dessus (texte réellement rendu, sur
les profils figés déterministes) et par les invariants de couverture/sécurité (I22/I23, verts).

### Fichiers touchés (contenu + le strict nécessaire côté tests)

- `content/decision/noeuds/diabete-type-2/statine.yaml` — v1.24 → **1.25** (T-151, T-152 tenté+reverté,
  T-153).
- `content/decision/noeuds/diabete-type-2/insuline.yaml` — v0.46 → **0.47** (T-152).
- `content/decision/noeuds/diabete-type-2/rhd-activite-physique.yaml` — v0.15 → **0.16** (T-155).
- `content/decision/noeuds/diabete-type-2/rhd-alimentation.yaml` — non touché.
- `src/features/decision/engine/banc/rendu-textuel.test.ts` — retrait des 18 exemptions I28 insuline
  résorbées + des 2 exemptions I31 résorbées ; les 8 exemptions I28 statine restent (texte de motif mis
  à jour pour expliquer pourquoi).
- `src/features/decision/engine/banc/invariants-contenu.test.ts` — retrait de 2 entrées de dette I14
  devenues stales (effet de bord de T-155, cf. ci-dessus — **hors périmètre littéral, justifié et
  signalé**).
- `src/features/decision/engine/evaluateNode.statine.test.ts` — F-25 réécrite, F-25b ajoutée (vignettes
  de la scission T-153).
- `src/features/decision/engine/banc/__snapshots__/caracterisation.statine.txt`,
  `caracterisation-indetermine.statine.txt`, `caracterisation.rhd-activite-physique.txt`,
  `caracterisation-indetermine.rhd-activite-physique.txt` — golden master mis à jour et relu (détail
  ci-dessus).

### STOP rencontrés

1. **T-154** — aucune source suffisante dans le nœud pour écrire une conduite à tenir devant une
   rétinopathie proliférante ; rien écrit, collecte signalée comme préalable nécessaire.
2. **T-152 (statine, partiel)** — 8 des 26 violations I28 restent non résorbées : la correction exigerait
   d'étendre la logique de l'invariant I24 (`src/`) aux `exclusions`, hors mandat contenu de cette
   session. Dette nommément exemptée, piste de correction documentée.

### Points N2 (relecture référent)

1. **T-153** — l'intitulé « Arrêter la statine — suspicion de rhabdomyolyse, avis spécialisé urgent »
   (seul texte clinique créé par cette session qui a réellement été écrit dans le contenu livré).
2. **T-154** — aucun texte créé ; le référent doit décider s'il fournit une source détaillant la conduite
   (pour qu'un futur lot écrive l'alerte) ou s'il accepte l'asymétrie neuropathie/rétinopathie en l'état.
3. **T-152 (statine)** — 8 violations I28 non résorbées, bloquées par une limite structurelle d'I24
   (ne scanne pas `exclusions`). Nécessite un arbitrage : (a) une session à mandat moteur qui étend
   `branchesDeclarees`, suivie d'une session contenu qui ajoute les 2 motifs déjà rédigés en attente ;
   ou (b) accepter la dette telle quelle plus longtemps.
4. **T-155** — retrait de 2 entrées de dette I14 dans `invariants-contenu.test.ts` (fichier `src/`), hors
   du périmètre littéral « Modifier » de la tâche mais nécessaire pour un `npm test` vert ; nature du
   retrait = mécanique (2 lignes, aucune logique touchée), justifiée dans le commentaire laissé sur
   place. À valider par le coordinateur/référent comme un geste acceptable en mode vague.
5. **T-151** — 5 entrées supplémentaires de `statine.yaml` (`prove-it`, `ascot-lla`, `chang`,
   `ctt-bas-risque`, `quatre-d`) partagent un défaut voisin (titre commençant par un espace suivi d'un
   caractère non alphabétique), hors périmètre littéral de la tâche (« les 14 titre ») — signalé, non
   corrigé, candidat à une tâche de suite.
</content>

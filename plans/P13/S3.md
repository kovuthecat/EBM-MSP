# P13 · S3 — Une bascule d'intention ne détruit plus de saisie   (rédigé par Opus)

> **Modèle : Sonnet · effort : xhigh · Vague : 2 (parallèle : oui — avec S5)**
> **Environnement : Desktop (navigateur in-app requis — le défaut ne se voit qu'en aller-retour)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-04 · Branche : — · **Dépend de S1** (même fichier `formLayout.ts`)

## ⚠ Lis ceci avant tout le reste

**Le comportement que tu vas modifier est un garde-fou délibéré, documenté et testé.**
`reinitialiserChampsMasques` (`lib/formLayout.ts:217-226`) efface les champs masqués **pour empêcher
qu'une valeur invisible continue de piloter le moteur** — c'est l'application de R8, et c'est ce qui
évite qu'un patient soit « évalué comme traité alors que l'écran affirme le contraire ».
`lib/formLayout.test.ts` le tient en deux endroits (l.4 et l.116).

**Le correctif n'est donc PAS de désarmer ce garde-fou.** Il est d'ajouter, à côté, une mémoire de
restauration que le moteur ne voit jamais. Si à un moment de ta session une valeur masquée redevient
lisible par `evaluateNode`, tu as recréé le défaut d'origine — arrête-toi.

## Le défaut, en une phrase

*Optimiser · metformine · dose 2000 · intolérance digestive* → bascule vers *Initier* → retour vers
*Intensifier* : les sections Traitement et Tolérance reviennent **vides**, sans un mot. Le praticien
qui corrige son intention en cours d'entretien — cas réel et fréquent — perd des saisies sans le
savoir, ou conclut sur un dossier qu'il croit rempli.

## Lire (commun à la session)

- `docs/decision/validation/revue-conception-fable-2026-08-04.md` — §4.1 **parcours C**, §4.3 **T2**,
  §5.1 « bascule destructrice », §5.3 **I1**.
- `docs/decision/GRAMMAIRE-NOEUD.md` — **R12** (proposition : une bascule de primer ne détruit jamais
  une saisie) et le §`visible_si` de **R8** (les deux sens du même principe). Lis les deux : R12 sans
  R8 conduit droit au contresens décrit ci-dessus.
- `src/features/decision/lib/formLayout.ts` — `reinitialiserChampsMasques` **en entier** (l.217-300),
  y compris le second bloc `valeursARetirer` (A4/F : le masquage **à la maille de la valeur**, pour
  les critères `liste` avec `valeurs_visible_si`). Ce second bloc est le piège de la session : une
  valeur retirée d'une liste doit être restaurable **individuellement**, pas la liste entière.
- `src/features/decision/screens/DecisionNodeScreen.tsx` — l.497-557 (`handleCriteriaChange`) et
  l.560-600 (`handleEffacer`) : **les deux** appellent `reinitialiserChampsMasques`, les deux doivent
  mémoriser.
- `src/features/decision/lib/formLayout.test.ts` — l.1-10, l.94-160 : les tests qui tiennent le
  garde-fou. **Aucun ne doit être affaibli ni supprimé.**
- `content/decision/noeuds/diabete-type-2/insuline.yaml` — le critère `profil_nocturne` (l.229-239) :
  son `type: enum`, ses `valeurs` dont la première est `baisse_continue`, son `visible_si`.

## Hors périmètre

- **Ne touche pas à la mémoire de session** (`lib/sessionCriteres.ts`) : c'est un autre mécanisme,
  d'une autre portée (inter-nœuds), et c'est **S5/S6**. La mémoire de restauration de cette session
  est **locale à l'écran** et meurt avec lui.
- Ne modifie aucun `visible_si` de contenu.
- Ne mets à jour ni `STATUS.md`, ni `TASKS.md`, ni `index.md`, ni `VALIDATION.md`. Ni commit ni push.

---

## T-142 — Établir le diagnostic AVANT de corriger (le profil nocturne)

### Objectif

Trancher une hypothèse qui change le correctif : « Profil nocturne : Baisse continue » **survit-il**
réellement à la bascule, ou est-ce la **valeur par défaut d'un `enum` non répondu** qui s'affiche
comme un choix ?

### Décision clé

`valeurParDefaut` rend **la première valeur déclarée** pour un `enum` (`lib/formLayout.ts:54`), et la
première valeur de `profil_nocturne` est justement `baisse_continue`. L'hypothèse est donc forte — et
les deux issues n'ont pas le même remède :

| Si… | alors le défaut est… | et il se corrige… |
| --- | --- | --- |
| le champ est bien réinitialisé et hors `touched` | **d'affichage** : un `enum` non répondu paraît répondu | en T-143, dans le rendu du résumé de section et/ou de l'état visuel du champ |
| le champ garde réellement sa valeur ET reste `touched` | **de cascade** : le masquage s'arrête trop tôt | dans `reinitialiserChampsMasques` — et c'est un bug de sûreté, plus prioritaire que toute la suite |

**Ne corrige rien tant que tu n'as pas tranché.** Un correctif d'affichage posé sur un bug de cascade
masquerait un défaut de sûreté.

### Lire / Modifier

**Modifier** : rien à cette tâche — c'est un diagnostic. Il produit un constat, consigné dans le
rapport de session, qui décide de la forme de T-143.

### Étapes

1. `preview_start` sur `ebm-msp-dev`. Nœud **Insulinothérapie**.
2. Construis l'état : *Situation = Basale seule* · *MCG disponible = Oui* · TBR 9 · CV 41 ·
   *Profil nocturne = Baisse continue* · dose de basale 38.
3. Bascule *Situation → Naïf*, puis reviens à *Basale seule*.
4. **Mesure, ne juge pas** : pour `profil_nocturne`, relève (a) la valeur dans l'état du formulaire,
   (b) si le bouton correspondant porte `aria-pressed="true"` / `data-on`, (c) ce que dit le résumé
   replié de la section, (d) si le critère est cité comme renseigné dans le cartouche. Les points
   (a) et (b) peuvent diverger — **c'est exactement ce qu'on cherche à savoir**.
5. Conclus dans le rapport : issue 1 ou issue 2, avec les quatre mesures à l'appui.

### Validation

- **N0 auto** : `—` (aucune modification). **N1** : la mesure elle-même. **N2** : `—`.

### Si bloqué

Si les quatre mesures ne concordent pas entre elles, **rapporte-les telles quelles** sans trancher.
Une divergence entre valeur et affichage EST le résultat, pas un échec de mesure.

### Message de commit

`—` (tâche de diagnostic, aucun fichier modifié)

---

## T-143 — La visibilité change, l'état ne se perd pas

### Objectif

Qu'une bascule d'intention ou de situation soit **réversible sans perte** : pour toute saisie S et
toute bascule A→B→A, l'état final égale S.

### Décision clé

**Une mémoire de restauration, à l'écran, hors de `criteria`.** Trois propriétés, non négociables :

1. **le moteur ne la voit jamais.** Elle n'entre ni dans `criteria`, ni dans `touched`, ni dans
   `preremplis`, ni dans `criteresRenseignes`. Tant qu'un champ est masqué, il vaut son défaut pour
   `evaluateNode` — exactement comme aujourd'hui (R8 intact) ;
2. **elle est restaurée quand le champ redevient visible**, avec sa marque `touched` d'origine :
   une valeur restaurée est une valeur **répondue**, pas une valeur suggérée. C'est le point qui la
   distingue d'un pré-remplissage — ne les confonds pas, ils ont des mentions différentes à l'écran ;
3. **elle meurt avec l'écran.** `useRef` (pas un `useState` : sa mutation ne doit rien re-rendre) à
   la portée du composant de nœud, jamais un module. Le remontage par `key` (« Nouveau patient »,
   D33) doit donc la vider sans qu'on ait rien à écrire — **vérifie-le, ne le suppose pas**.

**Le cas de la liste partiellement masquée** (second bloc de `reinitialiserChampsMasques`,
`valeurs_visible_si`) : mémorise la **valeur retirée**, pas la liste entière. Sinon, restaurer
« insuline basale » dans une liste où le praticien a entre-temps coché autre chose écraserait sa
saisie. Si ce cas te paraît coûteux, **traite-le en second** et signale-le : le cas « champ entier »
couvre les deux parcours de la revue.

**Le sens de la restauration compte** : on restaure une valeur **saisie par le praticien**, jamais une
valeur qui n'a fait que transiter. Un champ jamais `touched` (donc à son défaut) n'a rien à mémoriser.

### Lire / Modifier

**Modifier** : `src/features/decision/lib/formLayout.ts` (`reinitialiserChampsMasques` renvoie déjà
`reinitialises` : elle doit désormais renvoyer aussi **les valeurs** effacées, ce qui suffit à
alimenter la mémoire sans changer sa logique) · `src/features/decision/screens/DecisionNodeScreen.tsx`
(la mémoire, et la restauration dans les **deux** points d'appel) ·
`src/features/decision/lib/formLayout.test.ts` (extension, aucune suppression) · un test d'écran pour
l'invariant A→B→A.

### Étapes

1. Fais renvoyer à `reinitialiserChampsMasques` les couples `{nom, valeur}` effacés — extension pure,
   les appelants existants ignorent le champ ajouté. **Aucun test existant ne doit bouger.**
2. Ajoute la mémoire dans l'écran, alimentée aux deux points d'appel (l.506 et l.577).
3. Implémente la restauration : au calcul suivant, tout champ **redevenu visible** dont un couple est
   mémorisé récupère sa valeur **et** sa marque `touched`, puis **sort de la mémoire** (ne restaure
   jamais deux fois : le praticien a pu changer d'avis entre-temps).
4. **L'invariant, vu rouge d'abord** — c'est le livrable central de la session :
   > pour toute saisie S sur un nœud, toute bascule de primer A→B→A ramène exactement S
   > (valeurs **et** `touched`).
   Écris-le sur les **deux** parcours réels : `prescription` (Optimiser → Initier → Intensifier, avec
   metformine + dose + intolérance) et `insuline` (Basale → Naïf → Basale, avec MCG + TBR + CV +
   dose). Ce sont ceux de la revue ; ils doivent rougir avant ton correctif.
5. **Vérifie que R8 tient toujours** : reprends `formLayout.test.ts:116` et ajoute-lui un compagnon —
   *pendant* le masquage, `criteria[nom]` vaut le défaut et le nom est hors de `criteresRenseignes`.
   La mémoire ne doit pas être observable par le moteur, et ça se teste.
6. Selon l'issue de T-142 : si le défaut du profil nocturne est **d'affichage**, corrige-le ici —
   un `enum` non répondu ne doit pas paraître répondu (aucun bouton en état sélectionné, et le résumé
   de section dit « non renseigné » plutôt que la première valeur). Si c'est un défaut de **cascade**,
   corrige-le d'abord et signale-le comme défaut de sûreté dans le bilan.
7. Rejoue les **deux** parcours au navigateur et consigne l'avant/après.

### Validation

- **N0 auto (bloque le commit)** : `npm test` · `npm run typecheck` · `npm run build` → verts.
  **Le golden master ne doit PAS bouger** : cette session ne change aucune sortie du moteur à état
  égal. S'il bouge, tu as rendu la mémoire visible au moteur — c'est le signal d'alarme n° 1.
- **N1 visuel auto** : les deux parcours de l'étape 7, console propre. → `/verif-visuelle`.
- **N2 humain** : **faut-il signaler la restauration au praticien ?** Une valeur qui réapparaît sans
  un mot est meilleure que sa disparition, mais une mention (« repris de votre saisie », qui existe
  déjà pour la session inter-nœuds) serait peut-être plus loyale. Deux options à soumettre, ne
  tranche pas. `VALIDATION.md`.

### Si bloqué

- Si la restauration produit un état incohérent (une valeur restaurée dont le `visible_si` dépend
  d'un champ que le praticien a changé entre-temps) : **ne restaure pas ce champ** et signale le cas.
  Mieux vaut une restauration incomplète qu'un état que l'écran ne sait pas expliquer.
- Si le cas « liste partiellement masquée » se révèle plus coûteux que prévu : livre le cas « champ
  entier », signale-le, ne bâcle pas le second.

### Message de commit

`fix(ui): une bascule d'intention ou de situation ne détruit plus les saisies masquées (T-143)`

---

## Fin de session

Dérouler `/fin-de-tache` (mode **solo**) — sauf mode vague parallèle : ne toucher aucun fichier
partagé, ne rien committer, consigner bilan et points N2 **ici**.

**À consigner impérativement** : la conclusion de T-142 (issue 1 ou 2, avec les mesures), la preuve
que le golden master n'a pas bougé, et le sort réservé au cas « liste partiellement masquée ».
</content>
